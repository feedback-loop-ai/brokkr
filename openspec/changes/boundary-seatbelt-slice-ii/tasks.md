## 1. Preserve the fence and prove native startup before lifetime

- [x] 1.1 Extend host-independent tests around the existing closed build-status seams, keeping Seatbelt reported as unbuilt slice (ii) and container as unbuilt slice (iii), and verify readiness cannot make either boundary offered or let run/resume/rerun write a journal before activation; make no dependent production Seatbelt change in this task. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.2 Implement the initial Rust-only macOS feasibility-probe scaffold for a proposed per-invocation transient launchd lease pair, including separate launchd-owned guard and payload jobs, lifecycle/report plumbing and disposable state, and verify that scaffold compiles on Linux through injected facts while production Seatbelt remains fenced. Record this checked task as prototype preparation only: it does not establish payload startup, isolated launchd ownership, private liveness, quiescence-before-cleanup or the no-survivor guarantee, which remain gated by tasks 1.9–1.17. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”]
- [x] 1.3 Add the initial native adversary inventory, selection plumbing and outside-observer scaffolding for ordinary children, setsid, double-fork, ignored signals, retained pipes, timeout, explicit cancellation, parent/server/wrapper exit, supervisor SIGKILL, guard interference, peer bootout, escape-job registration and the process-group-only negative control. Record the case names and injected rejection checks as prototype coverage only: they do not prove honest native triggers, survivor accounting, launchd containment or cleanup until tasks 1.14–1.17 repair and measure the native adapter. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.4 Validate and commit the bounded probe preparation unsigned while production Seatbelt remains unbuilt: run OpenSpec strict validation, git diff --check, format, locked all-features clippy and workspace tests, self and verify bundle compiles, and the release CLI build, recording external exact coverage as pending; verify the committed diff contains the probe, adversaries, observer, CI invocation and host-independent tests but no dependent production Seatbelt implementation or activation. This checked preparation does not claim Windows helper portability or native enforcement, both of which are tested by later tasks. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.5 Preserve native CI `34433461814` for candidate `6a19a6f` as a failed `SEATBELT-R3-STARTUP` prerequisite in `evidence-residuals.md`, including macOS 26.6.2 arm64, the direct `SIGABRT`, the launchd job's one crashed run, absent READY/heartbeat, and the conclusion that lifetime was not run; verify the inventory does not name Python failure as its cause, count registration/crash as quiescence, reopen R1–R4, or rediscover the already repaired Git-metadata and `/usr/include` runner prerequisites. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [x] 1.6 Replace the interpreted probe payload with one committed Rust test-support executable serving startup, payload, descendant, guard and supervisor roles, and add a fail-closed four-cell S0–S3 startup verdict model over direct/launchd ownership and exact-profile off/on. Verify host-independent evaluator tests compare helper/profile identity, require nonce-authenticated READY plus an ordinary-child identity and directed clean exit, classify each failed cell separately and mark lifetime not run. Record this checked scope as helper/model preparation only: native CI `34441725835` invalidated the former claims of isolated launchd execution, complete terminal observation and cross-platform helper portability. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.7 Implement the host-independent Gate B verdict model and initial adapter hardening for role-separated state, case-specific event vocabulary, guard/peer ordering, process-group control facts, complete lifecycle matching and fail-closed cleanup/report fields; verify injected mutation tests reject forged markers, missing controls, false survivor facts, lifecycle prefixes and harness cleanup presented as containment. Record this checked scope as model coverage only: it does not assert that the native adapter honestly observed each fact, and task 1.14 owns the remaining adapter repair after Gate A passes. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.8 Preserve native CI `34441725835` for candidate `8c53dce` as a second failed `SEATBELT-R3-STARTUP` measurement in `evidence-residuals.md`: record the GitHub `macos-latest` arm64 runner without inventing an unrecorded `sw_vers`, S0's unboxed helper/ordinary-child/clean-exit pass and helper digest, S1's exact-profile signal 6 before READY, the non-admitting `allow default` diagnostic, the order-dependent S2/S3 bootstrap-error-or-unknown facts, Gate B not run, and Windows's unresolved `getuid`/`getpgid` link. Verify the record neither infers the denied operation nor treats an invented terminal state, diagnostic success, launchd registration, or a non-Darwin failure as containment evidence. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [x] 1.9 Make the probe helper portable before another native claim by target-gating `getuid`, `getpgid`, `setsid`, signals and every other Unix ABI use or supplying off-target refusing stubs, while leaving the shared startup/lifetime verdict model compiled and exercised on Linux and Windows. Move the destructive launchd adapter behind one explicit native selection so generic parallel workspace tests run only host-independent models and can neither race a second per-user launchd sequence nor report a native skip as success. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.10 Repair Gate A launchd ownership and observation so every cell and repeated invocation receives a never-reused private root, payload label, plist, streams and report; prove exact-label absence before bootstrap and after bounded bootout; and record bootstrap, optional kickstart, loaded state, helper stages, READY, ordinary-child identity, return intent, raw `launchctl print`, run/crash counters, terminal state, bootout and final absence as separate facts. Change the identical-helper/argv evaluator to compare immutable executable, mode, nonce protocol and arguments structurally while treating the controller-generated cell root as an explicit typed variable; verify injected cases accept isolated roots but reject any other helper/argv drift. Verify unknown or unparsable fields, failed waits/cleanup, stale labels and order-dependent results fail the cell without synthesizing an OS exit from a counter or payload-authored intent. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.11 Add authenticated bounded startup stages around helper entry, private-directory setup, executable identity, ordinary-child spawn/observation/reap, READY and return intent, plus durable bounded stdout/stderr and public denial diagnostics where the runner permits them. Encode one-authority-at-a-time differentials as a typed diagnostic collection that names operation, target and consumer and reruns credential, host-write, network, guard and peer denial controls; verify no diagnostic, including `allow default` or a broad file/Mach/service/network grant, can satisfy a startup cell or enter the candidate profile. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.12 Run OpenSpec strict validation, git diff --check, format, locked all-features clippy and workspace tests on Linux, self and verify bundle compiles, and the release CLI build; ensure Windows compilation/linkage is covered by the existing remote matrix, preserve external exact coverage as pending, and commit the bounded Gate A repair unsigned with Seatbelt still unbuilt and no Gate B or production implementation. Verify CI preserves and uploads the startup report on failure and the commit records the exact controller invocation. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-availability — “A boundary the machine cannot build refuses at start”]
- [ ] 1.13 Have the controller run only Gate A on task 1.12's exact committed head using the existing real unboxed macOS CI host, literal `/usr/bin/sandbox-exec` and public unprivileged per-user `launchctl`; record candidate commit, `sw_vers`, architecture, compiler, bootstrap domain, helper/profile digests, exact command, all four selected cells, authenticated stages, positive and denial controls, each raw launchd observation, status/signal and durable log in `evidence-residuals.md`. All four cells must pass before task 1.14; otherwise commit the precise startup residual unsigned, keep Gate B not run and return to the owning Gate A task without blindly widening policy or retrying an unchanged failure. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [ ] 1.14 Only after task 1.13 passes, make every Gate B adapter fact observational rather than assigned: use bounded nonblocking/pollable channels; record non-reusable start identities; maintain and reap a complete child/holder ledger; execute distinct cancellation, parent/server/wrapper loss, supervisor-death, setsid, double-fork, ignored-signal and retained-output triggers over the intended transports; record authenticated guard/peer attacks after READY; and derive exact timestamped lifecycle, quiescence and cleanup facts before harness cleanup. Implement the safe real original-process-group `SIGKILL` negative control and verify the detached identity remains live until explicit cleanup. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 1.15 Verify task 1.14 with injected tests that falsify each observed fact and cleanup edge, rerun the applicable host-independent validation and bundle compiles, and commit the bounded Gate B adapter repair unsigned with Seatbelt still unbuilt and no dependent production implementation. Preserve the Gate A helper bytes, argv contract and profile digest; if any changes, return to task 1.13 before lifetime execution. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-availability — “A boundary the machine cannot build refuses at start”]
- [ ] 1.16 On task 1.15's exact committed head, have the controller rerun Gate A alone and preserve the complete startup report; verify the same helper/profile candidate passes all four isolated cells with no skips, diagnostics, stale labels, synthesized terminal state or revision mismatch before selecting Gate B. Any failure returns to its owning startup or adapter task and leaves lifetime not run. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [ ] 1.17 Only after task 1.16 passes, have the controller run the committed lifetime selection on that exact head and record the exact selected cases, both labels, distinct triggers, protected guard/peer observations, every descendant PID/start identity, public `bootout` outcomes, five-second disappearance, one-second heartbeat quiet, cleanup/unregister order, process-group negative control, exit status and durable CI log in `evidence-residuals.md`. A complete zero-skip pass authorizes task 2.1 but leaves integrated SEATBELT-R3 open until both production hands paths pass task 6.6; any failure is committed as an evidence-only residual and stops all dependent implementation without weakening the guarantee. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]

## 2. Build the sealed Seatbelt planner and lifetime-owned executor

- [ ] 2.1 After tasks 1.16 and 1.17 pass on the committed native candidate, extract common parsed hands facts into a sealed PreparedHands plan with distinct namespace and Seatbelt renderers, preserve the existing namespace argv byte-for-byte, and verify Linux unit tests exercise every dispatcher arm without compiling native enforcement out of coverage. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; “Semantic choices and native evidence gate acceptance of the slice”; gate-boundary-policy — “The argv of a site with hands follows the boundary and the class”]
- [ ] 2.2 Add engine-owned SeatAttemptState keyed by run, effect, attempt, site and member plus fresh per-call CallState, using opaque owner-only roots outside the worktree, binds, Git administration paths and peer roots; verify repeated calls retain only overlay state while other seats, members, resume and rerun attempts receive inaccessible distinct state. [Requirements: seatbelt-execution — “Commands start with the closed hands environment and private call state”; “Overlay writes persist for the seat and never modify the host lower layer”]
- [ ] 2.3 Normalize worktree, bind, mask, scratch, Git, result, execution-input and toolchain authority before SBPL rendering with deny-over-read-only-over-read-write precedence, alias/case/Unicode/link/overlap and replaceable-ancestor checks, and data-safe path encoding; verify ambiguous or unrepresentable layouts refuse before spawn and declaration order cannot uncover a deny. [Requirements: seatbelt-execution — “Read and write grants expose only the declared filesystem authority”; “All declared bind modes and masks retain their observable contract”]
- [ ] 2.4 Implement the production launchd lease-pair lifecycle proven by task 1.17, including liveness EOF, cancellation and timeout control, domain quiescence, guarded pipe drain, quarantine on uncertain identity, descriptor-rooted cleanup, and guard self-unregistration; verify injected outcome tests cover every lifecycle transition and never delete private state before quiescence. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”]
- [ ] 2.5 Render a default-deny Seatbelt profile and invoke only the trusted absolute system launcher through the lifetime lease, granting each system path, device, Mach service, socket, resolver, SDK or certificate exception only to a named consumer; verify positive controls work and negative controls show no exception proxies undeclared files, credentials, writes or network. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; “Read and write grants expose only the declared filesystem authority”; “Network is denied unless the hands declaration grants it”]
- [ ] 2.6 Construct payload environments from empty with only the declared hands keys, engine markers, overlay map, controlled PATH/toolchain variables and resolved unsigned Git identity, while keeping profiles and launch control state unwritable; verify planted parent loader, proxy, credential, agent, startup and launchd variables do not reach either hands path. [Requirements: seatbelt-execution — “Commands start with the closed hands environment and private call state”; “Execution inputs stay controlled without Linux mount assumptions”]
- [ ] 2.7 Enforce network false for IPv4, IPv6, TCP, UDP, listen and shared loopback across descendants, and make network true widen only network authority; verify owned nonce endpoints give live deny/allow controls and filesystem, credential, Mach-service and Unix-socket restrictions remain unchanged. [Requirements: seatbelt-execution — “Network is denied unless the hands declaration grants it”]

## 3. Implement binds, masks and private overlay locators

- [ ] 3.1 Implement arbitrary ro and rw binds from the normalized authority plan, including home expansion, missing optional sources, nested grants and protected-path conflicts; verify ro mutations fail, rw host mutations succeed only where declared, ordinary worktree access remains usable, and unsupported layouts name the affected declaration before payload execution. [Requirements: seatbelt-execution — “Read and write grants expose only the declared filesystem authority”; “All declared bind modes and masks retain their observable contract”]
- [ ] 3.2 Implement descriptor-relative no-follow inventory, copy, revalidation and atomic publication for every overlay, preserving the metadata required by shipped Cargo/npm users, recreating only safe internal symlinks and private hard-link relationships, and refusing external, racing, special-file or incoherent layouts; verify no copied object retains a host hard link and failed preparation executes no payload. [Requirements: seatbelt-execution — “Overlay writes persist for the seat and never modify the host lower layer”]
- [ ] 3.3 Emit BROKKR_HANDS_OVERLAYS as compact ordered JSON with exactly index, expanded declared_path and absolute locator, keep mappings stable only within one seat attempt, and point CARGO_HOME and NPM_CONFIG_CACHE at their matching generic locators; verify arbitrary, Cargo and npm overlays work without special-case authority and locator/scratch spelling does not change manifest identity. [Requirements: seatbelt-execution — “Commands start with the closed hands environment and private call state”; “Overlay writes persist for the seat and never modify the host lower layer”; “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 3.4 Enforce present masks by excluding secret identities and admitted aliases from snapshots and denying direct/alias opens with EACCES or EPERM, while absent masks remain absent and ordinary neighbors remain usable; verify read, truncate, unlink, link, symlink, rename, rename-over, directory replacement and lockfile replacement leave host bytes, types, modes, link identities and parent entries unchanged. [Requirements: seatbelt-execution — “All declared bind modes and masks retain their observable contract”]
- [ ] 3.5 Exercise overlay creates, edits, deletes, renames, executable/cache changes, later host-source mutation, cross-call persistence, cross-seat/attempt isolation, mask aliases and cleanup after normal/failure/timeout/cancellation outcomes; verify shipped Cargo and node/npm overlay declarations pass without host publication and cleanup follows proven quiescence. [Requirements: seatbelt-execution — “Overlay writes persist for the seat and never modify the host lower layer”; boundary-availability — “Only container remains unbuilt after Seatbelt implementation”]

## 4. Protect Git administration and pinned execution inputs

- [ ] 4.1 Discover and spawn-time revalidate a bounded Git administration graph with a controlled absolute Git executable and cleared environment, including primary and linked worktree routing, common/worktree directories, config and config.worktree presence/absence and lock targets, includes/includeIf, effective relative/absolute core.hooksPath, hook parents and the narrow object/ref/log/HEAD/index mutations required by commits; verify cyclic, unstable, replaceable or unprovable layouts refuse by exact conflict. [Requirements: seatbelt-execution — “Git metadata protection prevents host hook and configuration persistence”]
- [ ] 4.2 Apply independent profile protection to host hooks, config, includes, gitdir/commondir routing, absent targets, aliases and atomic-replacement destinations while permitting the narrow Git mutations needed by the worktree; verify direct writes, git config commands, cleared overrides, unlink/recreate, rename, parent replacement and hard/symlink aliases fail in both primary and linked worktrees and host bytes plus directory entries remain unchanged. [Requirements: seatbelt-execution — “Git metadata protection prevents host hook and configuration persistence”; “Git remains usable for unsigned commits in linked worktrees”]
- [ ] 4.3 Route ordinary Git to an empty private hooks directory with synthetic identity and signing disabled, then verify status, stage and unsigned commits update the intended primary and linked-worktree refs without running hook/signing sentinels, exposing original hooks, or enabling a later host Git command to execute planted code. [Requirements: seatbelt-execution — “Git remains usable for unsigned commits in linked worktrees”]
- [ ] 4.4 Resolve, revalidate and grant read/execute only to the actual engine binary and the declaring bundle script/helper layer covered by the existing pin, deny mutation and aliases without assuming /runtime/bundle, and verify inherited external scripts select their own helpers and result path while unrelated parent files remain inaccessible. [Requirements: seatbelt-execution — “Execution inputs stay controlled without Linux mount assumptions”; gate-boundary-policy — “An exec site with hands under harness or open is admitted only for pinned bytes”]
- [ ] 4.5 Add conflict tests for a worktree containing the bundle, engine or protected-input aliases and refuse only irreconcilable layouts before spawn; verify ordinary source/result writes continue and existing decision 0048 script-directory re-walk, helper and interpreter limitations remain unchanged. [Requirements: seatbelt-execution — “Execution inputs stay controlled without Linux mount assumptions”]

## 5. Thread Seatbelt through both hands paths and runtime surfaces

- [ ] 5.1 Extend hands serve and hands exec with explicit internal boundary plus opaque attempt-state transport, default omission only for legacy namespace callers, and refuse explicit harness, open, container or unknown values; verify both JSON and TOML MCP adapter forms carry identical Seatbelt facts and a failed launch runs no payload. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; gate-boundary-policy — “The argv of a site with hands follows the boundary and the class”]
- [ ] 5.2 Route workspace MCP commands and the entire boxed exec dispatch through the same Seatbelt planner/executor while leaving model harness processes outside it, and verify panel, sequence, fallback, model, exec and dialect invocation shapes all select Seatbelt without taking an unboxed prefix branch. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; gate-boundary-policy — “The gate law reads the boundary for sites that declare hands”; “The argv of a site with hands follows the boundary and the class”]
- [ ] 5.3 Preserve compile-time boundary resolution for run, resume and rerun and pass the pinned/discovered realm word into composition without adding scratch state to identity; verify seatbelt manifests compile and pin the word, resume uses the pinned world, rerun uses the discovered realm, and open evidence gates cannot be overridden by the realm. [Requirements: realm-boundary — “A run's boundary is the realm's, resolved at compile”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 5.4 Centralize runtime and CLI build status behind one canonical runtime authority, then implement shared trusted-launcher readiness facts for start and doctor: macOS only, literal /usr/bin in supplied PATH, root-owned non-writable nonsymlink /usr/bin/sandbox-exec and ancestors, and a bounded engine-authored allow/deny nonce probe; verify the still-closed status cannot disagree across surfaces and wrong-host, empty/planted PATH, lookalike, metadata failure, no-op launcher, timeout and stale low-level readiness all refuse without executing repository code, payloads or journal writes. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “doctor reports the implemented boundaries and their actual prerequisites”; “Only container remains unbuilt after Seatbelt implementation”]
- [ ] 5.5 Supervise MCP requests and DriverProcess execution so cancellation, stdin/server loss, wrapper/engine loss, deadlines and normal completion all use the native lease, continue bounded pipe draining, and return compatible outcomes; verify workspace command/timeout bounds remain 1–16,384 bytes and 100–600,000 ms, each captured stream/diagnostic stays within 262,144 bytes with explicit truncation, and oversized exec protocol frames fail explicitly; if this requires any new exec-visible limit or unaccepted semantic choice, stop and add a focused proposed decision before implementation. [Requirements: seatbelt-execution — “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 5.6 Preserve namespace, harness and open behavior plus gate law and pinned-script restrictions, and keep container refusing even with Docker/Podman present; verify shipped Codex harness gates, dsh/lanetally refusals, open model/exec cases, namespace argv, hands-less bundles and container pre-journal refusals retain their existing results. [Requirements: gate-boundary-policy — “The gate law reads the boundary for sites that declare hands”; “An exec site with hands under harness or open is admitted only for pinned bytes”; “The argv of a site with hands follows the boundary and the class”; boundary-availability — “Only container remains unbuilt after Seatbelt implementation”]

- [ ] 5.7 Run the applicable closed-fence host-independent format, clippy, locked all-features workspace tests and bundle compiles with Seatbelt still unbuilt; verify every check passes before preparing the full native matrix. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]

## 6. Demonstrate the closed-fence implementation on macOS

- [ ] 6.1 Add one required macOS native target and checked obligation matrix covering both workspace MCP and boxed exec for every applicable filesystem, environment, network, bind, overlay, mask, Git, input, lifetime, output, record and refusal family; wire it into existing macOS CI without skip-on-missing-tool and verify missing/unusable launcher, outer boxing, ignored/filtered obligations, zero selection, missing controls or revision mismatch fails the job. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.2 Run the complete applicable host-independent suite, then commit the closed-fence implementation and native matrix unsigned with Seatbelt still unbuilt; verify the worktree is clean and the commit SHA is the exact candidate consumed by tasks 6.3–6.7. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.3 On the closed-fence implementation commit, run the native matrix's outside-file, credential, ro/rw, alias/overlap, environment and network off/on cases through both entry points and attach complete host/command/control logs; verify SEATBELT-R1 and SEATBELT-R2 remain open unless every required direct and alias outcome passes with unchanged host state. [Requirements: seatbelt-execution — “Read and write grants expose only the declared filesystem authority”; “Commands start with the closed hands environment and private call state”; “Network is denied unless the hands declaration grants it”; “All declared bind modes and masks retain their observable contract”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.4 On that same commit, run the arbitrary/Cargo/npm overlay mutation, persistence, isolation, later-source, identity, link and cleanup cases through both entry points and attach complete evidence; verify no host write occurs and SEATBELT-R1 closes only when all controls pass. [Requirements: seatbelt-execution — “Overlay writes persist for the seat and never modify the host lower layer”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.5 On that same commit, run primary and linked-worktree unsigned-commit plus raw hook/config/include/routing read/write/replacement adversaries through both entry points, followed by host-side sentinel checks, and attach complete evidence; verify SEATBELT-R4 closes and full-peer eligibility begins only when ordinary Git and every independent protection case pass. [Requirements: seatbelt-execution — “Git metadata protection prevents host hook and configuration persistence”; “Git remains usable for unsigned commits in linked worktrees”; “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.6 On that same commit, rerun the timeout, cancellation, server/wrapper loss, supervisor-SIGKILL, setsid/double-fork, retained-output and scratch-order matrix through both entry points and attach identities, labels and quiet-period evidence; verify integrated SEATBELT-R3 closes only after the complete two-entry-point pass and no payload or output holder survives. [Requirements: seatbelt-execution — “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.7 Exercise explicitly selected low-level Seatbelt components under the closed fence and verify manifest/planned identity and record-rendering derivations with injected outcomes, while ordinary starts still refuse and no successful Seatbelt run record, gate result or full-peer marker is appended before activation. [Requirements: seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; realm-boundary — “A run's boundary is the realm's, resolved at compile”]

## 7. Activate only the proved implementation

- [ ] 7.1 After tasks 6.3–6.7 close every native and host-independent obligation on one committed implementation revision, change only the canonical build-status authority so Seatbelt becomes built and container remains unbuilt; verify all production verdicts derive from that one edit and no feature flag, environment value or test configuration can activate another path. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 7.2 Verify activated start and doctor agree on capable macOS, wrong-host, missing/unusable launcher, unsupported bundle policy and hands-less bundles, and that doctor runs no seat or journal mutation while reporting container readiness separately from its slice-III refusal. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “doctor reports the implemented boundaries and their actual prerequisites”]
- [ ] 7.3 Update init's macOS advice from pending/unbuilt to the proved Seatbelt declaration and doctor command while retaining namespace defaults, honest unavailable causes and the explicitly unboxed harness alternative; verify CLI snapshots cover built-ready, built-unavailable and non-macOS cases without selecting or editing a realm. [Requirements: boundary-availability — “init's warning speaks the vocabulary”]

## 8. Document, validate and hand off the complete slice

- [ ] 8.1 Update hands help, MCP description, prompts and the provider, recipe, driver, quickstart and architecture guides with the measured Seatbelt mechanism, both entry points, explicit overlay locator ABI, denied-read masks, protected Git behavior, network/shared-loopback limits, controlled inputs, output/deadline behavior and unsupported-layout refusals; verify examples distinguish accepted target semantics from measured evidence and never claim Linux namespace properties. [Requirements: boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”; “The guides document the boundary and never lose a section”]
- [ ] 8.2 Refresh doctor, journal/readout, repository-layout, extension-model, target-architecture and contracts documentation plus current shipped-bundle compile/refusal inventories, preserving every named section and historical version/channel fact; verify documentation tests and measured inventory pins agree with the activated behavior and container remains slice III. [Requirements: boundary-guides — “The guides document the boundary and never lose a section”; boundary-availability — “doctor reports the implemented boundaries and their actual prerequisites”]
- [ ] 8.3 Reconcile evidence-residuals.md with durable SEATBELT-R1–R4 evidence and finding lifecycle records without rewriting historical audit evidence, and verify every row names candidate revision, macOS version/architecture, launcher, compiler, exact command, case count, controls, exit status and durable log/CI link. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [ ] 8.4 Compare contracts, fixtures, policy/phase-machine.json and reference against the pre-change revision and verify frozen bytes are unchanged; if measurement proves a wire need, stop and return to design before adding a new version beside the frozen contract, then update only measured witness/compose pins and verify their digest tests explain the identity change. [Requirements: seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 8.5 Run openspec validate boundary-seatbelt-slice-ii --strict --no-interactive, cargo fmt --all -- --check, cargo clippy --workspace --all-targets --all-features --locked -- -D warnings, cargo test --workspace --all-features --locked, both self and verify bundle compiles, cargo build --release --locked -p brokkr-cli, and git diff --check; verify every command passes before committing the activation candidate. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 8.6 Commit the activation candidate unsigned after all executable, test, guide, evidence-status and measured-pin changes, and verify the worktree is clean and the commit contains no container implementation, frozen-byte edit, publication, integration or merge. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; boundary-guides — “The guides document the boundary and never lose a section”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 8.7 On that exact activation commit, rerun the complete required macOS matrix including ordinary run/resume/rerun, gates, both hands paths, records, doctor and readouts, and have the controller run scripts/coverage-exact.sh plus all final remote CI outside the workspace box; verify no native obligation is skipped, exact nonzero line/branch/function coverage remains 100% with the pinned compiler, and any failure reinstates the unbuilt verdict rather than inheriting older evidence. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “doctor reports the implemented boundaries and their actual prerequisites”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 8.8 Reconcile only the final activation evidence and finding records, tick every completed task, and commit that evidence-only amendment unsigned; verify its parent is the fully tested activation commit, the amendment changes no executable/spec/policy/frozen behavior, git status is clean, and publication, channel updates, integration/merge and closure remain controller-owned pending actions. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”; “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 8.9 On the exact evidence-only commit from task 8.8 as the final PR head, have the controller run all applicable remote CI again, including the required macOS native matrix, all three operating systems, MSRV, licenses, audit, packaging and exact coverage with the pinned compiler; verify every required check reports success for that exact head SHA and record this gate as pending until those remote results exist. Make no later commit after the successful gate; any failure or evidence correction returns to the owning task and repeats tasks 8.5–8.9 on the resulting new final head. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
