## 1. Preserve the fence and prove native startup before lifetime

- [x] 1.1 Extend host-independent tests around the existing closed build-status seams, keeping Seatbelt reported as unbuilt slice (ii) and container as unbuilt slice (iii), and verify readiness cannot make either boundary offered or let run/resume/rerun write a journal before activation; make no dependent production Seatbelt change in this task. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “A boundary this engine does not build refuses at start until its slice lands”; “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.2 Implement the initial Rust-only macOS feasibility-probe scaffold for a proposed per-invocation transient launchd lease pair, including separate launchd-owned guard and payload jobs, lifecycle/report plumbing and disposable state, and verify that scaffold compiles on Linux through injected facts while production Seatbelt remains fenced. Record this checked task as prototype preparation only: it does not establish payload startup, isolated launchd ownership, private liveness, quiescence-before-cleanup or the no-survivor guarantee, which remain gated by tasks 1.9–1.33. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”]
- [x] 1.3 Add the initial native adversary inventory, selection plumbing and outside-observer scaffolding for ordinary children, setsid, double-fork, ignored signals, retained pipes, timeout, explicit cancellation, parent/server/wrapper exit, supervisor SIGKILL, guard interference, peer bootout, escape-job registration and the process-group-only negative control. Record the case names and injected rejection checks as prototype coverage only: they do not prove honest native triggers, survivor accounting, launchd containment or cleanup until tasks 1.30–1.33 repair and measure the native adapter. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.4 Validate and commit the bounded probe preparation unsigned while production Seatbelt remains unbuilt: run OpenSpec strict validation, git diff --check, format, locked all-features clippy and workspace tests, self and verify bundle compiles, and the release CLI build, recording external exact coverage as pending; verify the committed diff contains the probe, adversaries, observer, CI invocation and host-independent tests but no dependent production Seatbelt implementation or activation. This checked preparation does not claim Windows helper portability or native enforcement, both of which are tested by later tasks. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.5 Preserve native CI `34433461814` for candidate `6a19a6f` as a failed `SEATBELT-R3-STARTUP` prerequisite in `evidence-residuals.md`, including macOS 26.6.2 arm64, the direct `SIGABRT`, the launchd job's one crashed run, absent READY/heartbeat, and the conclusion that lifetime was not run; verify the inventory does not name Python failure as its cause, count registration/crash as quiescence, reopen R1–R4, or rediscover the already repaired Git-metadata and `/usr/include` runner prerequisites. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [x] 1.6 Replace the interpreted probe payload with one committed Rust test-support executable serving startup, payload, descendant, guard and supervisor roles, and add a fail-closed four-cell S0–S3 startup verdict model over direct/launchd ownership and exact-profile off/on. Verify host-independent evaluator tests compare helper/profile identity, require nonce-authenticated READY plus an ordinary-child identity and directed clean exit, classify each failed cell separately and mark lifetime not run. Record this checked scope as helper/model preparation only: native CI `34441725835` invalidated the former claims of isolated launchd execution, complete terminal observation and cross-platform helper portability. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.7 Implement the host-independent Gate B verdict model and initial adapter hardening for role-separated state, case-specific event vocabulary, guard/peer ordering, process-group control facts, complete lifecycle matching and fail-closed cleanup/report fields; verify injected mutation tests reject forged markers, missing controls, false survivor facts, lifecycle prefixes and harness cleanup presented as containment. Record this checked scope as model coverage only: it does not assert that the native adapter honestly observed each fact, and task 1.30 owns the remaining adapter repair after Gate A and B0 pass. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.8 Preserve native CI `34441725835` for candidate `8c53dce` as a second failed `SEATBELT-R3-STARTUP` measurement in `evidence-residuals.md`: record the GitHub `macos-latest` arm64 runner without inventing an unrecorded `sw_vers`, S0's unboxed helper/ordinary-child/clean-exit pass and helper digest, S1's exact-profile signal 6 before READY, the non-admitting `allow default` diagnostic, the order-dependent S2/S3 bootstrap-error-or-unknown facts, Gate B not run, and Windows's unresolved `getuid`/`getpgid` link. Verify the record neither infers the denied operation nor treats an invented terminal state, diagnostic success, launchd registration, or a non-Darwin failure as containment evidence. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [x] 1.9 Make the probe helper portable before another native claim by target-gating `getuid`, `getpgid`, `setsid`, signals and every other Unix ABI use or supplying off-target refusing stubs, while leaving the shared startup/lifetime verdict model compiled and exercised on Linux and Windows. Move the destructive launchd adapter behind one explicit native selection so generic parallel workspace tests run only host-independent models and can neither race a second per-user launchd sequence nor report a native skip as success. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.10 Implement the first isolated-root/label Gate A repair: give every cell and invocation a never-reused private root, payload label, plist, streams and report; require pre-bootstrap and post-bootout absence; retain raw `launchctl print`; stop synthesizing an OS exit from a counter or payload-authored intent; and compare immutable helper/argv structure while treating the cell root as typed data. Record this checked task as the `9f4c2c9` attempt only: native CI `34449331270` disproved its concrete-profile equality and complete launchd-observation claims, which tasks 1.14 and 1.16 reopen. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.11 Add the first authenticated startup-stage and typed diagnostic collection around helper entry, private-directory setup, executable identity, ordinary-child spawn/observation/reap, READY and return intent, while keeping `allow default` and every broad one-class differential non-admitting. Record this checked task as the `9f4c2c9` attempt only: native CI `34449331270` showed that all seven single-class diagnostics still abort before the first stage and identify no admissible operation or target, so task 1.15 owns the next discriminator. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.12 Run OpenSpec strict validation, git diff --check, format, locked all-features clippy and workspace tests on Linux, self and verify bundle compiles, and the release CLI build; ensure Windows compilation/linkage is covered by the existing remote matrix, preserve external exact coverage as pending, and commit the bounded Gate A repair unsigned with Seatbelt still unbuilt and no Gate B or production implementation. Verify CI preserves and uploads the startup report on failure and the commit records the exact controller invocation. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-availability — “A boundary the machine cannot build refuses at start”]
- [x] 1.13 Append native CI `34449331270` for candidate `9f4c2c9` to `evidence-residuals.md` as the third failed `SEATBELT-R3-STARTUP` prerequisite and record the named cause and bounded probe repair: passing generic macOS/Linux/Windows workspaces, S0 control, S1 signal 6 before stages, seven non-starting one-class diagnostics, non-admitting `allow default`, S2 observation/parsing refusal, S3 non-ready one-run/one-crash result, unobserved denial controls and Gate B not run. Record the S1/S3 profile-digest inequality as required typed-private-root substitution rather than authority drift, name the missing root-inode read as the pre-stage abort cause, and commit the evidence amendment unsigned without rewriting earlier measurements. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [x] 1.14 Replace raw concrete-profile equality with structural typed-root identity: digest the candidate profile with the per-cell `cell_root` replaced by the declared structural token, record every instantiated profile and its structural digest, and add injected tests that accept isolated private roots while still rejecting a real rule, role or helper mutation. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.15 Name and fix the pre-stage abort with minimal aperture: add the root-inode read `(allow file-read* (literal "/"))` to the candidate profile (root inode only, never recursive `(subpath "/")`) and add a bounded removal proof that strips exactly that rule and requires the same payload to fail closed before a Seatbelt startup cell may pass; keep every broad one-class allowance and `allow default` non-admitting and show the removal cannot enter the candidate. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.16 Make launchd startup observation lossless and fail-closed at the next native boundary: preserve a bounded sequence of distinct raw `launchctl print` samples with exit status, stdout and stderr; report the last parseable state when no terminal state is reached; and include the last raw sample in the cell refusal so a reaped or unparseable label is named as such. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Native lifetime feasibility precedes full implementation”; “Semantic choices and native evidence gate acceptance of the slice”]
- [x] 1.17 Add host-independent tests that falsify every new fact (root-inode rule present and not recursive, removal strips exactly one rule, normalized digest ignores the typed root but not a rule change, a ready Seatbelt cell without an observed blocking removal fails, and raw launchd samples render); run OpenSpec strict validation, `git diff --check`, format, locked all-features clippy and workspace tests, self and verify bundle compiles and the release CLI build; preserve external exact coverage as pending; and commit the changed Gate A candidate unsigned with Seatbelt still unbuilt, Gate B not run and no dependent production implementation. [Requirements: seatbelt-execution — “Probe payload startup is established separately”; “Semantic choices and native evidence gate acceptance of the slice”; boundary-availability — “A boundary the machine cannot build refuses at start”]
- [ ] 1.18 Add `pub const HOST_TOOLCHAIN_BINDS: &[&str]` to `hands.rs` holding the seventeen host-toolchain literals `box_argv` already renders read-only, in the same order with the `/usr/include` comment beside its entry, and have `box_argv` iterate it in place of its inline array, deleting the inline array so there is one list; verify the namespace argv stays byte-identical and a host-independent test renders `box_argv` over varied `HandsSpec`, home and `GitFacts` values and requires its `--ro-bind-try` sources to be exactly the constant, the home-expanded declared `ro` binds and `<common>/config`, and nothing else. [Requirements: seatbelt-execution — "The experimental startup template is an audited rule ledger"]
- [ ] 1.19 Add `tests/seatbelt_probe/controls.rs` holding the credential-read (`/etc/passwd`, `/etc/hosts`), data-volume credential-read (`/System/Volumes/Data/private/etc/passwd`) and host-write (`/private/tmp/brokkr-probe-denial-write`) control targets as the one source both the helper and the check read; have the helper include it through `#[path]` inside its attack functions in place of its hard-coded literals, and have the shared model declare it as a module so the check derives its path-valued denial-control-target input from the same constants. Verify the helper's attack targets and the check's control-target input are the identical constants by a host-independent test. [Requirements: seatbelt-execution — "The experimental startup template is an audited rule ledger" — "A unit that covers a denial-control target fails"; "The data-volume spelling of a credential stays denied"]
- [ ] 1.20 Add a typed `STARTUP_RULE_LEDGER` constant to the probe's shared model beside `STARTUP_NEGATIVE_ALLOWANCES`, encoding each entry as one normalized rule unit (one operation, at most one simple `literal`/`subpath`/`self` filter over `<cell-root>`, `<payload-root>` and `<helper>`) plus exactly one class — `Baseline { kind, justification }` with `kind` one of hands element (naming toolchain, system library with an optional typed correction, writable worktree, device set or shell), execution input or probe-harness need, or `DiagnosisAdmitted { operation, target, process, consumer, evidence, removal }` naming one `STARTUP_NEGATIVE_ALLOWANCES` entry — and enter the specification's twenty-three baseline entries (the seven named process units — `process-fork` and the six `process-exec` units on `<helper>`, `/usr/bin`, `/usr/libexec`, `/usr/local`, `/bin` and `/sbin` — the seven toolchain reads on `/usr/bin`, `/usr/lib`, `/usr/libexec`, `/usr/share`, `/usr/local`, `/bin` and `/sbin`, the two system-library reads, the helper read, the two probe-harness reads on `<cell-root>/inputs` and `<payload-root>`, the payload write and the three device reads) and the one root-inode diagnosis-admitted entry with its `root-inode-read` removal; add `PROGRAM_BINDS`, a fixed five-entry constant beside the ledger (`/usr/bin`, `/usr/libexec`, `/usr/local`, `/bin`, `/sbin`), for task 1.21 to confirm against `HOST_TOOLCHAIN_BINDS`; add a third typed table mapping every `fa7ece5` `sandbox_profile` unit to carried, withdrawn or narrowed with its reason, and dispose of the four `8c53dce` additions (`/private/var/tmp` and `/dev/dtracehelper` withdrawn; the helper literal and `/dev/random` baseline) while keeping the fa7 template verbatim in the probe's test data. Verify a host-independent test renders the frame plus one `allow` form per ledger entry in ledger order and that the renderer never groups, escapes, drops or duplicates a unit. [Requirements: seatbelt-execution — "The experimental startup template is an audited rule ledger" — "Every ledger entry carries its typed justification"; "The candidate's process authority is seven named units"; "The four 8c53dce additions keep their disposition"]
- [ ] 1.21 Implement the pure check as a function over the rendered concrete profile text, a `ledger: &[LedgerEntry]` parameter and a `removal_set: &[RemovalEntry]` parameter (the renderer and observer call sites pass the committed `STARTUP_RULE_LEDGER` and `STARTUP_NEGATIVE_ALLOWANCES` constants, so falsification tests can pass a varied ledger or removal set), `HOST_TOOLCHAIN_BINDS` (read directly, with no parameter for it, so no caller can hand it a copy) and a typed `CheckInputs { cell_root, payload_root, inputs_dir, helper }`, running the specification's three stages: (1) validate the concrete inputs in rule order and stop before any rewrite on failure, returning a private `ValidatedInputs` value that only this stage constructs and that the normalizer takes as its only source of concrete roots, so normalizing an unvalidated input does not compile; (2) parse the closed grammar (single-operation `allow` forms with only simple filters; a multi-operation form, `(with ...)` modifier, compound filter, other top-level form, comment, escape or duplicate unit refused by name) and normalize longest-validated-path-first; (3) judge each unit against a fixed two-part anchor keyed by its element or kind — an admitted-operations set matched by exact byte equality of the operation token, with no family or prefix logic, and a target test — over the table toolchain (`file-read*`; `process-exec` only when the named bind is one of the five `PROGRAM_BINDS`, confirmed as `HOST_TOOLCHAIN_BINDS` sources and refused by name otherwise) / system library (`file-read*` on `/System/Library`, `/System/Volumes/Preboot/Cryptexes/OS` or a recorded typed correction) / writable worktree (`file-write*` on exactly `<payload-root>`) / device set (`file-read*` on exactly one of `/dev/null`, `/dev/urandom`, `/dev/random`) / shell (unfiltered `process-fork`) / execution input (`file-read*` or `process-exec` on exactly `(literal "<helper>")`) / probe-harness need (`file-read*` on `<cell-root>/inputs` or `<payload-root>`); run a separate process rule over every unit of both halves requiring a `process-*` operation to equal one of the seven `PROCESS_UNITS` as a whole normalized unit, the only place a prefix appears and there only to refuse, so a diagnosis-admitted unit never adds process authority; apply the cover, data-volume and control-target rules to normalized and concrete forms; and compare the units against the disjoint union of the baseline and removal set — returning the checked template or an ordered `Vec<CheckRefusal>` with a named variant per specification refusal, including a failed operation anchor and a failed target anchor (each naming the unit and its element or kind), a `process-*` unit outside the seven, and a program bind that is not a `HOST_TOOLCHAIN_BINDS` source; confirm each `PROGRAM_BINDS` entry is a `HOST_TOOLCHAIN_BINDS` source through a small pure sub-function taking both lists as parameters, which the check calls with the real constants so the top-level function itself takes no parameter for either, and which a host-independent test calls directly with a program-binds list naming a source `HOST_TOOLCHAIN_BINDS` lacks to exercise the `CheckRefusal` variant the real five-entry constant can never trigger on its own; add the `spellings(path)` map (direct plus `/private` for an `etc`/`var`/`tmp` first component) that cell-root validation, the cover rule and the control-target list share while the toolchain anchor reads only the direct spelling; report stage 1 refusals in rule order, stage 2 in text order, stage 3 in template order then missing ledger/removal entries in ledger order, and within one unit follow the specification's refusal-list order, so a toolchain `process-exec` on `/usr/lib` reports its failed operation anchor and then the process rule. The checked template's normalized text — which substitutes all three placeholders `<cell-root>`, `<payload-root>` and `<helper>` — is the cross-cell identity source: retire `structural_profile_digest`'s cell-root-only `ROOT_TOKEN` substitution in favor of digesting this normalized text, and retarget `structural_profile_digest_ignores_the_typed_cell_root` to construct two cells whose payload-root and helper spellings differ as well as their cell root. Verify host-independent falsification tests for every scenario the specification names, including: the disjoint-union equality, a multi-operation form, a non-frame/non-allow top-level form, a modifier/compound-filter/unparseable text, the per-bind toolchain anchor, a respelled toolchain unit, a bind's image relabelled under another class, a declared `ro` bind (e.g. `~/.rustup`) and the git common `config` both failing as toolchain binds, a common directory under the payload root leaving the worktree unit valid, verdict invariance across `HandsSpec`/home/`GitFacts`, a mis-instantiated cell root, the fixed `<cell-root>/{payload,inputs}` layout, a non-canonical cell root or helper, a placeholder hiding a concrete target, a unit covering a denial-control target, the `/private` spelling comparison and its exclusion from any other class, the data-volume refusal under every class, a typed and bounded system-library correction, the seven-unit process authority, an unlisted or doubly classified unit, a listed unit the template lacks, every candidate baseline entry passing its two-part anchor, a baseline `/dev/null` write failing the operation anchor under every element, `file-write*` on `/usr/local` or `/System/Library` failing its element's operation anchor, `process-exec` on `/usr/lib`/`/usr/share`/`<payload-root>` and a filtered `process-fork` failing both their anchor and the process rule, the execution-input and probe-harness kinds' anchors (including an `spctl` exec also failing as a `process-*` unit outside the seven and fa7's whole `<cell-root>` read returning as neither kind), and member-named operations (`file-write-data`, `file-read-data`, `file-read-metadata`) failing their family anchor by exact name. [Requirements: seatbelt-execution — "The experimental startup template is an audited rule ledger"]
- [ ] 1.22 Repair `native.rs` observer duties into the specification's fixed order: canonicalize the host temporary base once; create the per-run probe root with an exclusive owner-only `DirBuilder` create (mode `0o700`, never `create_dir_all`); stage `<helper>` by creating `<probe-root>/bin` the same way, opening `<probe-root>/bin/seatbelt-probe-helper` with an exclusive `create_new` open, copying the bytes of the committed build (`CARGO_BIN_EXE_seatbelt-probe-helper`) into it and marking it executable, then confirming it is a regular file owned by the invoking user with exactly one link whose digest equals the build's (recording both digests in the report), never using the build's own second hard-linked file as `<helper>`; create each cell root with the same exclusive owner-only create, then `payload` and `inputs` inside it; confirm each is a directory owned by the invoking user; canonicalize the cell root and the staged helper and require exactly the input spelling — deleting the silent `unwrap_or(raw)` canonicalization fallback at `native.rs:252` so creation always precedes canonicalization with no substitution; pass `--helper <helper>` as the launch argument in the `sandbox-exec` argv, the launchd job's program arguments and S0's direct exec, so the helper is staged once per run outside every cell root and every cell, replay and diagnostic of that run starts the identical staged bytes at one spelling, and a staging defect therefore fails S0 and is never attributed to Seatbelt; build `CheckInputs` from these observed values, never from the renderer, and run task 1.21's check over each cell's concrete candidate profile before invoking `sandbox-exec`, failing the cell before any payload runs on a failed duty or a check refusal and naming it in the report. These duties, the check and the `--helper` argument apply to every native run under the candidate profile, not only Gate A's cells, replays and diagnostics: replace `run_case_inner`'s non-exclusive `remove_dir_all`/`create_dir_all` root creation with the same exclusive owner-only creates, and add `--helper <helper>` beside its existing `--root` argument on the payload, guard and peer launches it spawns for Gate B, so those launches are staged, checked and identically spelled the same way Gate A's are. Verify injected tests reject a canonicalization mismatch, a non-exclusive cell-root or helper-staging create, a helper with more than one link or a digest other than the build's, a non-owned directory and a check run from renderer-supplied rather than observer-supplied inputs, and confirm S0 execs the identical staged helper spelling unboxed. [Requirements: seatbelt-execution — "The experimental startup template is an audited rule ledger" — "The observer establishes what the string check cannot"]
- [ ] 1.23 Replace the helper's `Stdio::null()` ordinary-child spawn with explicit pre-opened streams (`child-stdin` read-only, `child-stdout`/`child-stderr` write-only on `/dev/null`), a `child-spawn` stage using those handles and a `child-observed` stage, recording the sub-stage reached and the raw OS error on failure; have the helper read the `--helper <helper>` launch argument task 1.22 passes before its first stage, exiting 2 with a named message and no recorded stage if it is absent, and exec exactly that spelling, never `current_exe` or `argv[0]`, at each of its seven `current_exe()` sites (the startup mode's ordinary child, its five payload attack cases and the `detach-child` role), so the argument stands as the typed `<helper>` substitution in the structural argv comparison and the cells still compare identical; add the four labelled direct-replay discriminating cells from the failing cell's validated root under the exact candidate — open `/dev/null` write-only with no spawn, spawn with inherited stdio, spawn with null stdio, and the ordinary mode plus exactly `(allow file-write-data (literal "/dev/null"))` — and wire a diagnosis-admitted `/dev/null` write entry with its own removal entry into the ledger only on an attribution these cells confirm; fail a cell whose spawn/exec sub-stage is refused under another resolved spelling of the helper on its own startup facts, recording the startup residual `SEATBELT-R3-STARTUP-helper-respelling` with that evidence and admitting nothing in either half. Verify host-independent tests localize a stage-`child-spawn` refusal to its sub-stage, confirm the literal-scoped predicate is individually removable, confirm the candidate carries no `/dev/null` write unit until an attribution enters it, confirm every descendant role execs only the passed `--helper` spelling and a helper started without it fails before its first stage, and confirm a respelled-helper refusal changes neither `<helper>` nor the ledger. [Requirements: seatbelt-execution — "Probe payload startup is established separately" — "Child-spawn refusal is localized to its sub-stage"; "A named child-spawn predicate is literal-scoped and removable"; "The experimental startup template is an audited rule ledger" — "An attributed /dev/null write is diagnosis-admitted"; "The helper re-executes through its validated spelling"; "An exec-side refusal of the helper under another spelling admits nothing"]
- [ ] 1.24 When a Seatbelt startup cell of the ledger candidate fails before `READY`, run each withdrawn or narrowed fa7 unit alone, in its fa7 form, as a labelled restoration diagnostic (the exact candidate plus that one unit), plus one further labelled cell restoring every such unit at once, all as direct `sandbox-exec` replays from the failing cell's validated root with no launchd label bootstrapped; if a diagnostic advances the stages, require native denial evidence naming the operation and a single-object target before any diagnosis-admitted entry with its own removal control enters the ledger, and rerun every denial control; refuse re-entry of the unfiltered process family, unfiltered `sysctl-read`, unfiltered `ipc-posix-shm`, host tmp reads, `(subpath "/System")`, `(subpath "/usr")` or the whole `<cell-root>` in their historical form. Verify these diagnostics never pass startup or admit authority by construction, and that a withdrawn unit returns only through this bounded path. [Requirements: seatbelt-execution — "The experimental startup template is an audited rule ledger" — "A withdrawn unit returns only through the bounded experiment"]
- [ ] 1.25 Add a bounded `/usr/bin/log show` invocation after each Seatbelt startup cell, removal replay and diagnostic, filtered to Sandbox events for its responsible process IDs and executable over the cell's time window, keeping at most a fixed count and size of raw events plus its own exit status, and recording unavailable or empty explicitly rather than inferring an operation allowed; add the credential target's `/System/Volumes/Data` spelling as a fourth startup denial control alongside credential-read, host-write and network-bind, and require the candidate to deny it as it denies the direct spelling. Verify a host-independent test asserts an empty or unavailable `log show` result is never read as a positive control. [Requirements: seatbelt-execution — "The experimental startup template is an audited rule ledger" — "The data-volume spelling of a credential stays denied"]
- [ ] 1.26 Replace the all-or-nothing `parse_launchd_print` with a brace-depth scanner over the single outermost block that gives each top-level field an independent parsed-or-unknown value, keeps raw evidence for nested entries under their block path, and returns unknown for a duplicated top-level key; require `state`, `runs` and `last exit code` and treat `successive crashes`, a terminating signal and `active count` as optional, failing the cell on a present nonzero crash counter or a present terminating signal, and stop inferring "clean" from an absent crash counter; move the three fa7 raw `launchctl print` samples into the probe's verbatim test data as regression fixtures (not `fixtures/`); update `StartupNegativeControl` to record each removal as not due, observed blocking, observed not blocking, or missing, owed only by a cell that reached `READY`, where blocking means the stripped replay reached no nonce-authenticated `READY` from fresh payload state. Verify host-independent tests replay all three fa7 samples and each specification scenario: an unknown field does not erase other facts, an omitted crash counter stays unknown, required terminal facts still fail closed, facts come only from the top-level dictionary, the running fa7 sample is non-terminal, nested/duplicated keys never manufacture a fact, and a print never substitutes for helper facts. [Requirements: seatbelt-execution — "Probe payload startup is established separately" — "An unknown launchd field does not erase other facts"; "An omitted crash counter stays unknown"; "Required launchd terminal facts still fail closed"; "Launchd job facts come only from the top-level dictionary"; "The running fa7 sample is non-terminal"; "Nested or duplicated keys never manufacture a job fact"; "A launchd print never substitutes for helper facts"; "A non-starting Seatbelt cell owes no removal verdict"; "A stripped replay that reaches READY is not blocking"]
- [ ] 1.27 Run OpenSpec strict validation, `git diff --check`, format, locked all-features clippy and workspace tests, self and verify bundle compiles, and the release CLI build; preserve external exact coverage as pending; and commit the ledger candidate (tasks 1.18–1.26) unsigned with Seatbelt still unbuilt and Gate B not run. Verify the committed diff contains exactly the one `hands.rs` constant edit, the probe-only ledger/check/controls/observer/child-spawn/restoration/denial-event/launchd changes and their tests, and no dependent production Seatbelt implementation. [Requirements: seatbelt-execution — "Probe payload startup is established separately"; "The experimental startup template is an audited rule ledger" — "The ledger candidate's startup is pending native measurement"; "Semantic choices and native evidence gate acceptance of the slice"]
- [ ] 1.28 Have the controller run only Gate A on task 1.27's exact committed head using the existing real unboxed macOS arm64 CI host, literal `/usr/bin/sandbox-exec` and public unprivileged per-user `launchctl`; record candidate and checked-out SHAs, `sw_vers`, architecture, compiler, bootstrap domain, helper/profile digests, exact command, all four selected cells, the root-inode and (if entered) child-spawn removal controls, the bounded `log show` denial events, positive controls, each raw launchd sample, status/signal and durable log in `evidence-residuals.md`. All four cells must pass with every entered removal control observed blocking before task 1.29; otherwise commit the precise startup residual unsigned per task 1.24's rules, keep Gate B not run and return to its owning task without a blind retry or broadening. [Requirements: seatbelt-execution — "Probe payload startup is established separately"; "Native lifetime feasibility precedes full implementation"; "Semantic choices and native evidence gate acceptance of the slice"; boundary-guides — "Seatbelt documentation names its mechanism and evidence limits"]
- [ ] 1.29 Only after task 1.28 passes, run the minimal B0 rejection screen on that exact candidate: prove an authenticated `setsid`/double-fork descendant is live, a separately launchd-owned guard remains alive to issue and report public `launchctl bootout`, and the same descendant identity is gone afterward. Record all identities, heartbeat, public operation and after-state. Any failure rejects the transient launchd lease candidate and records SEATBELT-R3 open; a pass authorizes task 1.30 only and is not lifetime feasibility proof or production authority. [Requirements: seatbelt-execution — "Native lifetime feasibility precedes full implementation"; "Semantic choices and native evidence gate acceptance of the slice"; boundary-guides — "Seatbelt documentation names its mechanism and evidence limits"]
- [ ] 1.30 After B0 passes, make every B1 adapter fact observational rather than assigned: use bounded nonblocking/pollable channels; record non-reusable start identities; maintain and reap a complete child/holder ledger; execute distinct ordinary completion, timeout, cancellation, parent/server/wrapper loss, supervisor-death, setsid, double-fork, ignored-signal and retained-output triggers over the intended transports; record authenticated guard/peer/escape-job attacks after READY; and derive exact timestamped lifecycle, causal launchd quiescence and cleanup facts before harness cleanup. Implement the safe real original-process-group `SIGKILL` negative control and verify the detached identity remains live until explicit cleanup. [Requirements: seatbelt-execution — "Native lifetime feasibility precedes full implementation"; "Deadlines and teardown bound the entire command lifetime"; "Output stays bounded without corrupting driver or MCP transport"; "Semantic choices and native evidence gate acceptance of the slice"]
- [ ] 1.31 Verify task 1.30 with injected tests that falsify each observed fact, causal-ownership claim and cleanup edge; rerun the applicable host-independent validation and bundle compiles; and commit the bounded Gate B adapter repair unsigned with Seatbelt still unbuilt and no dependent production implementation. Preserve Gate A helper bytes, template, substitution ABI, argv contract and admitted predicates; if any changes, return to task 1.28 before lifetime execution. [Requirements: seatbelt-execution — "Native lifetime feasibility precedes full implementation"; "Semantic choices and native evidence gate acceptance of the slice"; boundary-availability — "A boundary the machine cannot build refuses at start"]
- [ ] 1.32 On task 1.31's exact committed head, have the controller rerun Gate A alone and preserve the complete startup report; verify the same normalized authority and concrete substitutions pass all four isolated cells with no skips, stale labels, synthesized terminal state, unexplained predicate or revision mismatch before selecting Gate B. Any failure returns to its owning startup or adapter task and leaves lifetime not run. [Requirements: seatbelt-execution — "Probe payload startup is established separately"; "Native lifetime feasibility precedes full implementation"; "Semantic choices and native evidence gate acceptance of the slice"; boundary-guides — "Seatbelt documentation names its mechanism and evidence limits"]
- [ ] 1.33 Only after task 1.32 passes, have the controller run the committed complete B1 lifetime selection on that exact head and record the exact selected cases, both labels, distinct triggers, protected guard/peer observations, every descendant PID/start identity, causal public `bootout` outcomes, five-second disappearance, one-second heartbeat quiet, cleanup/unregister order, process-group negative control, exit status and durable CI log in `evidence-residuals.md`. A complete zero-skip pass authorizes task 2.1 but leaves integrated SEATBELT-R3 open until both production hands paths pass task 6.6; any failure is committed as an evidence-only residual and stops dependent implementation without weakening the guarantee. [Requirements: seatbelt-execution — "Native lifetime feasibility precedes full implementation"; "Deadlines and teardown bound the entire command lifetime"; "Semantic choices and native evidence gate acceptance of the slice"; boundary-guides — "Seatbelt documentation names its mechanism and evidence limits"]

## 2. Build the sealed Seatbelt planner and lifetime-owned executor

- [ ] 2.1 After tasks 1.32 and 1.33 pass on the committed native candidate, extract common parsed hands facts into a sealed PreparedHands plan with distinct namespace and Seatbelt renderers, preserve the existing namespace argv byte-for-byte, and verify Linux unit tests exercise every dispatcher arm without compiling native enforcement out of coverage. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; “Semantic choices and native evidence gate acceptance of the slice”; gate-boundary-policy — “The argv of a site with hands follows the boundary and the class”]
- [ ] 2.2 Add engine-owned SeatAttemptState keyed by run, effect, attempt, site and member plus fresh per-call CallState, using opaque owner-only roots outside the worktree, binds, Git administration paths and peer roots; verify repeated calls retain only overlay state while other seats, members, resume and rerun attempts receive inaccessible distinct state. [Requirements: seatbelt-execution — “Commands start with the closed hands environment and private call state”; “Overlay writes persist for the seat and never modify the host lower layer”]
- [ ] 2.3 Normalize worktree, bind, mask, scratch, Git, result, execution-input and toolchain authority before SBPL rendering with deny-over-read-only-over-read-write precedence, alias/case/Unicode/link/overlap and replaceable-ancestor checks, and data-safe path encoding; verify ambiguous or unrepresentable layouts refuse before spawn and declaration order cannot uncover a deny. [Requirements: seatbelt-execution — “Read and write grants expose only the declared filesystem authority”; “All declared bind modes and masks retain their observable contract”]
- [ ] 2.4 Implement the production launchd lease-pair lifecycle proven by task 1.33, including liveness EOF, cancellation and timeout control, domain quiescence, guarded pipe drain, quarantine on uncertain identity, descriptor-rooted cleanup, and guard self-unregistration; verify injected outcome tests cover every lifecycle transition and never delete private state before quiescence. [Requirements: seatbelt-execution — “Native lifetime feasibility precedes full implementation”; “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”]
- [ ] 2.5 Render a default-deny Seatbelt profile and invoke only the trusted absolute system launcher through the lifetime lease, granting each system path, device, Mach service, socket, resolver, SDK or certificate exception only to a named consumer; verify positive controls work and negative controls show no exception proxies undeclared files, credentials, writes or network. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; “Read and write grants expose only the declared filesystem authority”; “Network is denied unless the hands declaration grants it”]
- [ ] 2.6 Construct payload environments from empty with only the declared hands keys, engine markers, overlay map, controlled PATH/toolchain variables and resolved unsigned Git identity, while keeping profiles and launch control state unwritable; verify planted parent loader, proxy, credential, agent, startup and launchd variables do not reach either hands path. [Requirements: seatbelt-execution — “Commands start with the closed hands environment and private call state”; “Execution inputs stay controlled without Linux mount assumptions”]
- [ ] 2.7 Enforce network false for IPv4, IPv6, TCP, UDP, listen and shared loopback across descendants, and make network true widen only network authority; verify owned nonce endpoints give live deny/allow controls and filesystem, credential, Mach-service and Unix-socket restrictions remain unchanged. [Requirements: seatbelt-execution — “Network is denied unless the hands declaration grants it”]

## 3. Implement binds, masks and private overlay locators

- [ ] 3.1 Implement arbitrary ro and rw binds from the normalized authority plan, including home expansion, missing optional sources, nested grants and protected-path conflicts; verify ro mutations fail, rw host mutations succeed only where declared, ordinary worktree access remains usable, and unsupported layouts name the affected declaration before payload execution. [Requirements: seatbelt-execution — “Read and write grants expose only the declared filesystem authority”; “All declared bind modes and masks retain their observable contract”]
- [ ] 3.2 Implement descriptor-relative no-follow inventory, copy, revalidation and atomic publication for every overlay, preserving the metadata required by shipped Cargo/npm users, recreating only safe internal symlinks and private hard-link relationships, and refusing external, racing, special-file or incoherent layouts; verify no copied object retains a host hard link and failed preparation executes no payload. [Requirements: seatbelt-execution — “Overlay writes persist for the seat and never modify the host lower layer”]
- [ ] 3.3 Emit BROKKR_HANDS_OVERLAYS as compact ordered JSON with exactly index, expanded declared_path and absolute locator, keep mappings stable only within one seat attempt, and point CARGO_HOME and NPM_CONFIG_CACHE at their matching generic locators; verify arbitrary, Cargo and npm overlays work without special-case authority and locator/scratch spelling does not change manifest identity. [Requirements: seatbelt-execution — “Commands start with the closed hands environment and private call state”; “Overlay writes persist for the seat and never modify the host lower layer”; “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 3.4 Enforce present masks by excluding secret identities and admitted aliases from snapshots and denying direct/alias opens with EACCES or EPERM, while absent masks remain absent and ordinary neighbors remain usable; verify read, truncate, unlink, link, symlink, rename, rename-over, directory replacement and lockfile replacement leave host bytes, types, modes, link identities and parent entries unchanged. [Requirements: seatbelt-execution — “All declared bind modes and masks retain their observable contract”]
- [ ] 3.5 Exercise overlay creates, edits, deletes, renames, executable/cache changes, later host-source mutation, cross-call persistence, cross-seat/attempt isolation, mask aliases and cleanup after normal/failure/timeout/cancellation outcomes; verify shipped Cargo and node/npm overlay declarations pass without host publication and cleanup follows proven quiescence. [Requirements: seatbelt-execution — “Overlay writes persist for the seat and never modify the host lower layer”; boundary-availability — “Only container remains unbuilt after Seatbelt implementation”]

## 4. Protect Git administration and pinned execution inputs

- [ ] 4.1 Discover and spawn-time revalidate a bounded Git administration graph with a controlled absolute Git executable and cleared environment, including primary and linked worktree routing, common/worktree directories, config and config.worktree presence/absence and lock targets, includes/includeIf, effective relative/absolute core.hooksPath, hook parents and the narrow object/ref/log/HEAD/index mutations required by commits; verify cyclic, unstable, replaceable or unprovable layouts refuse by exact conflict. [Requirements: seatbelt-execution — “Git metadata protection prevents host hook and configuration persistence”]
- [ ] 4.2 Apply independent profile protection to host hooks, config, includes, gitdir/commondir routing, absent targets, aliases and atomic-replacement destinations while permitting the narrow Git mutations needed by the worktree; verify direct writes, git config commands, cleared overrides, unlink/recreate, rename, parent replacement and hard/symlink aliases fail in both primary and linked worktrees and host bytes plus directory entries remain unchanged. [Requirements: seatbelt-execution — “Git metadata protection prevents host hook and configuration persistence”; “Git remains usable for unsigned commits in linked worktrees”]
- [ ] 4.3 Route ordinary Git to an empty private hooks directory with synthetic identity and signing disabled, then verify status, stage and unsigned commits update the intended primary and linked-worktree refs without running hook/signing sentinels, exposing original hooks, or enabling a later host Git command to execute planted code. [Requirements: seatbelt-execution — “Git remains usable for unsigned commits in linked worktrees”]
- [ ] 4.4 Resolve, revalidate and grant read/execute only to the actual engine binary and the declaring bundle script/helper layer covered by the existing pin, deny mutation and aliases without assuming /runtime/bundle, and verify inherited external scripts select their own helpers and result path while unrelated parent files remain inaccessible. [Requirements: seatbelt-execution — “Execution inputs stay controlled without Linux mount assumptions”; gate-boundary-policy — “An exec site with hands under harness or open is admitted only for pinned bytes”]
- [ ] 4.5 Add conflict tests for a worktree containing the bundle, engine or protected-input aliases and refuse only irreconcilable layouts before spawn; verify ordinary source/result writes continue and existing decision 0048 script-directory re-walk, helper and interpreter limitations remain unchanged. [Requirements: seatbelt-execution — “Execution inputs stay controlled without Linux mount assumptions”]

## 5. Thread Seatbelt through both hands paths and runtime surfaces

- [ ] 5.1 Extend hands serve and hands exec with explicit internal boundary plus opaque attempt-state transport, default omission only for legacy namespace callers, and refuse explicit harness, open, container or unknown values; verify both JSON and TOML MCP adapter forms carry identical Seatbelt facts and a failed launch runs no payload. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; gate-boundary-policy — “The argv of a site with hands follows the boundary and the class”]
- [ ] 5.2 Route workspace MCP commands and the entire boxed exec dispatch through the same Seatbelt planner/executor while leaving model harness processes outside it, and verify panel, sequence, fallback, model, exec and dialect invocation shapes all select Seatbelt without taking an unboxed prefix branch. [Requirements: seatbelt-execution — “Both hands entry points execute inside the declared Seatbelt boundary”; gate-boundary-policy — “The gate law reads the boundary for sites that declare hands”; “The argv of a site with hands follows the boundary and the class”]
- [ ] 5.3 Preserve compile-time boundary resolution for run, resume and rerun and pass the pinned/discovered realm word into composition without adding scratch state to identity; verify seatbelt manifests compile and pin the word, resume uses the pinned world, rerun uses the discovered realm, and open evidence gates cannot be overridden by the realm. [Requirements: realm-boundary — “A run's boundary is the realm's, resolved at compile”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 5.4 Centralize runtime and CLI build status behind one canonical runtime authority, then implement shared trusted-launcher readiness facts for start and doctor: macOS only, literal /usr/bin in supplied PATH, root-owned non-writable nonsymlink /usr/bin/sandbox-exec and ancestors, and a bounded engine-authored allow/deny nonce probe; verify the still-closed status cannot disagree across surfaces and wrong-host, empty/planted PATH, lookalike, metadata failure, no-op launcher, timeout and stale low-level readiness all refuse without executing repository code, payloads or journal writes. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “doctor names the boundaries this machine offers”; “doctor reports the implemented boundaries and their actual prerequisites”; “Only container remains unbuilt after Seatbelt implementation”]
- [ ] 5.5 Supervise MCP requests and DriverProcess execution so cancellation, stdin/server loss, wrapper/engine loss, deadlines and normal completion all use the native lease, continue bounded pipe draining, and return compatible outcomes; verify workspace command/timeout bounds remain 1–16,384 bytes and 100–600,000 ms, each captured stream/diagnostic stays within 262,144 bytes with explicit truncation, and oversized exec protocol frames fail explicitly; if this requires any new exec-visible limit or unaccepted semantic choice, stop and add a focused proposed decision before implementation. [Requirements: seatbelt-execution — “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 5.6 Preserve namespace, harness and open behavior plus gate law and pinned-script restrictions, and keep container refusing even with Docker/Podman present; verify shipped Codex harness gates, dsh/lanetally refusals, open model/exec cases, namespace argv, hands-less bundles and container pre-journal refusals retain their existing results. [Requirements: gate-boundary-policy — “The gate law reads the boundary for sites that declare hands”; “An exec site with hands under harness or open is admitted only for pinned bytes”; “The argv of a site with hands follows the boundary and the class”; boundary-availability — “Only container remains unbuilt after Seatbelt implementation”]

- [ ] 5.7 Run the applicable closed-fence host-independent format, clippy, locked all-features workspace tests and bundle compiles with Seatbelt still unbuilt; verify every check passes before preparing the full native matrix. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]

## 6. Demonstrate the closed-fence implementation on macOS

- [ ] 6.1 Add required macOS arm64 and x86_64 native jobs and one checked obligation matrix covering both workspace MCP and boxed exec for every applicable filesystem, environment, network, bind, overlay, mask, Git, input, lifetime, output, record and refusal family; wire both jobs into macOS CI without skip-on-missing-tool and verify missing/unusable launcher, outer boxing, ignored/filtered obligations, zero selection, missing controls or revision mismatch fails the job. If the controller cannot supply either architecture, keep that architecture as an activation-blocking residual or obtain an operator-accepted platform narrowing before proceeding. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.2 Run the complete applicable host-independent suite, then commit the closed-fence implementation and native matrix unsigned with Seatbelt still unbuilt; verify the worktree is clean and the commit SHA is the exact candidate consumed by tasks 6.3–6.7. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.3 On the closed-fence implementation commit and on both supported macOS architectures, run the native matrix's outside-file, credential, ro/rw, alias/overlap, environment and network off/on cases through both entry points and attach complete host/command/control logs; verify SEATBELT-R1 and SEATBELT-R2 remain open unless every required direct and alias outcome passes with unchanged host state on both architectures. [Requirements: seatbelt-execution — “Read and write grants expose only the declared filesystem authority”; “Commands start with the closed hands environment and private call state”; “Network is denied unless the hands declaration grants it”; “All declared bind modes and masks retain their observable contract”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.4 On that same commit and on both supported macOS architectures, run the arbitrary/Cargo/npm overlay mutation, persistence, isolation, later-source, identity, link and cleanup cases through both entry points and attach complete evidence; verify no host write occurs and SEATBELT-R1 closes only when all controls pass on both architectures. [Requirements: seatbelt-execution — “Overlay writes persist for the seat and never modify the host lower layer”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.5 On that same commit and on both supported macOS architectures, run primary and linked-worktree unsigned-commit plus raw hook/config/include/routing read/write/replacement adversaries through both entry points, followed by host-side sentinel checks, and attach complete evidence; verify SEATBELT-R4 closes and full-peer eligibility begins only when ordinary Git and every independent protection case pass on both architectures. [Requirements: seatbelt-execution — “Git metadata protection prevents host hook and configuration persistence”; “Git remains usable for unsigned commits in linked worktrees”; “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.6 On that same commit and on both supported macOS architectures, rerun the timeout, cancellation, server/wrapper loss, supervisor-SIGKILL, setsid/double-fork, retained-output and scratch-order matrix through both entry points and attach identities, labels and quiet-period evidence; verify integrated SEATBELT-R3 closes only after the complete two-entry-point pass on both architectures and no payload or output holder survives. [Requirements: seatbelt-execution — “Deadlines and teardown bound the entire command lifetime”; “Output stays bounded without corrupting driver or MCP transport”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 6.7 Exercise explicitly selected low-level Seatbelt components under the closed fence and verify manifest/planned identity and record-rendering derivations with injected outcomes, while ordinary starts still refuse and no successful Seatbelt run record, gate result or full-peer marker is appended before activation. [Requirements: seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; realm-boundary — “A run's boundary is the realm's, resolved at compile”]

## 7. Activate only the proved implementation

- [ ] 7.1 After tasks 6.3–6.7 close every native and host-independent obligation on one committed implementation revision, change only the canonical build-status authority so Seatbelt becomes built and container remains unbuilt; verify all production verdicts derive from that one edit and no feature flag, environment value or test configuration can activate another path. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 7.2 Verify activated start and doctor agree on capable macOS, wrong-host, missing/unusable launcher, unsupported bundle policy and hands-less bundles, and that doctor runs no seat or journal mutation while reporting container readiness separately from its slice-III refusal. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “doctor reports the implemented boundaries and their actual prerequisites”]
- [ ] 7.3 Update init's macOS advice from pending/unbuilt to the proved Seatbelt declaration and doctor command while retaining namespace defaults, honest unavailable causes and the explicitly unboxed harness alternative; verify CLI snapshots cover built-ready, built-unavailable and non-macOS cases without selecting or editing a realm. [Requirements: boundary-availability — “init's warning speaks the vocabulary”]

## 8. Document, validate and hand off the complete slice

- [ ] 8.1 Update hands help, MCP description, prompts and the provider, recipe, driver, quickstart and architecture guides with the measured Seatbelt mechanism, both entry points, explicit overlay locator ABI, denied-read masks, protected Git behavior, network/shared-loopback limits, controlled inputs, output/deadline behavior and unsupported-layout refusals; verify examples distinguish accepted target semantics from measured evidence and never claim Linux namespace properties. [Requirements: boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”; “The guides document the boundary and never lose a section”]
- [ ] 8.2 Refresh doctor, journal/readout, repository-layout, extension-model, target-architecture and contracts documentation plus current shipped-bundle compile/refusal inventories, preserving every named section and historical version/channel fact; verify documentation tests and measured inventory pins agree with the activated behavior and container remains slice III. [Requirements: boundary-guides — “The guides document the boundary and never lose a section”; boundary-availability — “doctor reports the implemented boundaries and their actual prerequisites”]
- [ ] 8.3 Reconcile evidence-residuals.md with durable SEATBELT-R1–R4 evidence and finding lifecycle records without rewriting historical audit evidence, and verify every row names candidate revision, macOS version/architecture, launcher, compiler, exact command, case count, controls, exit status and durable log/CI link. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
- [ ] 8.4 Compare contracts, fixtures, policy/phase-machine.json and reference against the pre-change revision and verify frozen bytes are unchanged; if measurement proves a wire need, stop and return to design before adding a new version beside the frozen contract, then update only measured witness/compose pins and verify their digest tests explain the identity change. [Requirements: seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 8.5 Run openspec validate boundary-seatbelt-slice-ii --strict --no-interactive, cargo fmt --all -- --check, cargo clippy --workspace --all-targets --all-features --locked -- -D warnings, cargo test --workspace --all-features --locked, both self and verify bundle compiles, cargo build --release --locked -p brokkr-cli, and git diff --check; verify every command passes before committing the activation candidate. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 8.6 Commit the activation candidate unsigned after all executable, test, guide, evidence-status and measured-pin changes, and verify the worktree is clean and the commit contains no container implementation, frozen-byte edit, publication, integration or merge. [Requirements: boundary-availability — “Only container remains unbuilt after Seatbelt implementation”; boundary-guides — “The guides document the boundary and never lose a section”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 8.7 On that exact activation commit, rerun the complete required macOS matrix on both arm64 and x86_64, including ordinary run/resume/rerun, gates, both hands paths, records, doctor and readouts, and have the controller run scripts/coverage-exact.sh plus all final remote CI outside the workspace box; verify no native obligation or architecture is skipped, exact nonzero line/branch/function coverage remains 100% with the pinned compiler, and any failure reinstates the unbuilt verdict rather than inheriting older evidence. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; “doctor reports the implemented boundaries and their actual prerequisites”; seatbelt-execution — “Seatbelt identity and evidence describe the path that actually ran”; “Semantic choices and native evidence gate acceptance of the slice”]
- [ ] 8.8 Reconcile only the final activation evidence and finding records, tick every completed task, and commit that evidence-only amendment unsigned; verify its parent is the fully tested activation commit, the amendment changes no executable/spec/policy/frozen behavior, git status is clean, and publication, channel updates, integration/merge and closure remain controller-owned pending actions. [Requirements: seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”; “Seatbelt identity and evidence describe the path that actually ran”]
- [ ] 8.9 On the exact evidence-only commit from task 8.8 as the final PR head, have the controller run all applicable remote CI again, including the required macOS native matrix, all three operating systems, MSRV, licenses, audit, packaging and exact coverage with the pinned compiler; verify every required check reports success for that exact head SHA and record this gate as pending until those remote results exist. Make no later commit after the successful gate; any failure or evidence correction returns to the owning task and repeats tasks 8.5–8.9 on the resulting new final head. [Requirements: boundary-availability — “A boundary the machine cannot build refuses at start”; seatbelt-execution — “Semantic choices and native evidence gate acceptance of the slice”; boundary-guides — “Seatbelt documentation names its mechanism and evidence limits”]
