## 1. Prove existing provider admission before changing the roster

- [x] 1.1 Add N0 in `crates/brokkr-runtime/src/bundle/model_policy_tests.rs` using `Fixture::compile_roots` / `Bundle::compile_under`: a valid, explicitly work-class agent listing Cargo/Git without hands, a temporary Codex adapter and namespace boundary. Canonicalize temporary roots and keep their `TempDir` alive on Linux/macOS; use valid charters, mappings, effort pins and unrelated capabilities, with no provider probe or frozen-fixture edits. Verify targeted Rust tests compare the complete existing diagnostic, including seat, agent, provider, model and the verbatim MORE-power reason, for bare and measured unsupported tool permissions and when a valid later candidate exists; never use `is_err()`. Follow design D3: retain existing production resolution unless a required regression demonstrates a gap, then make only the smallest repair preserving 0043/0046 semantics; a new semantic choice returns upstream for a proposed decision. Requirement: [boxed-work-provider-admission — “A tool-listed work seat without hands retains its exact refusal”](specs/boxed-work-provider-admission/spec.md).

- [x] 1.2 Add N1 by giving that same test-only agent hands while retaining its tool list and the Codex adapter's workspace fragment. Verify the targeted namespace compile succeeds with an explanatory expectation and asserts Astra/Codex, high effort, recorded hands and namespace, the complete workspace fragment and absence of generated tool-list arguments. Requirement: [boxed-work-provider-admission — “Namespace hands replace the tool list only through declared workspace support”](specs/boxed-work-provider-admission/spec.md).

- [x] 1.3 Add N2 with the same hands-declaring agent under namespace and valid adapters whose entire hands capability is absent, bare unsupported or measured unsupported. Verify each targeted compile refuses with the existing seat/agent/provider/model diagnostic, the unsupported-hands and harness-own-tools explanation, and any supplied measured reason; do not substitute a loader/tier error, delete only `workspace` from a capability object or treat `workspace: []` as absence. Requirement: [boxed-work-provider-admission — “Namespace hands replace the tool list only through declared workspace support”](specs/boxed-work-provider-admission/spec.md).

- [x] 1.4 Add D3's internal resolver control in `crates/brokkr-runtime/src/agents/tests.rs`: load and clone a valid adapter, set parsed `Adapter.hands` to `None` while retaining `harness.work`, and call boxed composition. Verify the targeted test asserts the unsupported-hands reason, proving harness support cannot rescue namespace admission; label this as an internal control rather than loadable adapter JSON. Requirement: [boxed-work-provider-admission — “Namespace hands replace the tool list only through declared workspace support”](specs/boxed-work-provider-admission/spec.md).

- [x] 1.5 Add P1 in `agents/tests.rs` with a valid hands-plus-tools agent and a Claude-shaped adapter mapping Cargo/Git through `--allowedTools` while its workspace fragment grants `mcp__brokkr__workspace` through that same flag. Verify the targeted test preserves the complete workspace fragment, exactly one MCP-only grant and no `Bash(cargo:*)` or `Bash(git:*)` arguments, so A1's two grant sources are distinguished by value and origin. Requirement: [boxed-work-provider-admission — “Namespace hands replace the tool list only through declared workspace support”](specs/boxed-work-provider-admission/spec.md).

- [x] 1.6 Add H1 to the compile matrix using the same Codex-only hands agent under harness with both workspace and `hands.harness.work` declarations. Verify the targeted positive test asserts the resolved candidate, recorded hands, harness boundary and writable work fragment, with no namespace workspace fragment or tool-list grant; retain that compiled setup for task 3.3. Requirement: [boxed-work-provider-admission — “Harness work is a separate case governed by the existing whole-chain rule”](specs/boxed-work-provider-admission/spec.md).

- [x] 1.7 Add H2 with workspace support intact and `hands.harness.work` absent or declared as a measured unsupported object (`{"unsupported": "<measured reason>"}`), following design D3's loadable cases. Verify targeted compile failures name the seat, chain link, provider, capability, existing writable-sandbox reason and decision 0046 rulings 1 and 4, including the measured reason when supplied; workspace support must not satisfy this separate rule. Returned finding J1: exclude the bare string `"unsupported"` from H2 because the existing loader rejects it with `needs 'work' as an array of strings` before boundary policy runs; any assertion of that input belongs to a separate loader-rejection case, without changing grammar. Rerun the existing boundary-policy controls to verify open and other-boundary outcomes remain unchanged. Requirement: [boxed-work-provider-admission — “Harness work is a separate case governed by the existing whole-chain rule”](specs/boxed-work-provider-admission/spec.md).

## 2. Apply and pin the shipped engine-smith declaration

- [x] 2.1 Add a focused declaration pin in `crates/brokkr-runtime/tests/roster.rs`, then update `agents/implementer-engine.json` to models exactly `["astra", "fable"]`, both efforts `high`, and design D2's exact workspace hands: network false, `~/.cargo` overlay masking both `credentials.toml` and `credentials`, and `~/.rustup` read-only. Remove the entire inactive `tools` object because hands supersede it on both providers; retain description, charter and limits. Verify the targeted roster test passes and pins no agent boundary, checkout-specific path, duplicate workdir bind or extra host-write/network grant; writable project access remains the existing automatic workdir mount. Requirement: [astra-engine-smith — “The engine smith hires the ruled chain through workspace hands”](specs/astra-engine-smith/spec.md).

- [x] 2.2 Add H3 in `bundle/model_policy_tests.rs` using a minimal work seat and the actual shipped engine-smith agent/adapters after task 2.1. Verify its harness compile refuses specifically at link 2/provider Claude for missing `hands.harness.work`, with the existing writable-sandbox reason, even though the Codex-only H1 passes. Do not omit Fable, invent a Claude work fragment or accept an unrelated earlier bundle refusal. Requirement: [boxed-work-provider-admission — “Harness work is a separate case governed by the existing whole-chain rule”](specs/boxed-work-provider-admission/spec.md).

## 3. Inspect independently expected production launch composition

- [x] 3.1 Extend `crates/brokkr-runtime/src/engine/boundary_tests.rs` to compile a minimal namespace work seat from the actual shipped agent/adapters and pass its resolved Astra candidate and recorded hands through production `compose_site`, using a canonical temporary workdir and result path inside it. Verify the targeted test independently expects the complete relevant ordered argv: `gpt-6-astra`, high effort, exactly `--sandbox read-only`, the engine executable registered as `mcp_servers.brokkr.command`, decoded `hands serve --workdir <canonical-root> --spec <literal-policy>` arguments, and the shipped MCP approval setting. Assert the exact network-false/two-bind/two-mask policy, no competing writable sandbox, harness work fragment, per-tool-list flag, retired Cargo/Git grant or unexpanded token anywhere. Expected values must not come from the serializer/helpers under test; run without provider, network or nested namespace. Requirement: [boxed-work-provider-admission — “The shipped engine smith's namespace launch is tested as composed”](specs/boxed-work-provider-admission/spec.md).

- [x] 3.2 Extend that actual-shipped composition regression for Fable/Claude, independently expecting `claude-fable-5-1`, high effort and the complete ordered workspace fragment `--tools <empty-argv-element> --strict-mcp-config --mcp-config <expanded-JSON> --allowedTools mcp__brokkr__workspace`. Verify the targeted test decodes the MCP server and independently asserts the engine executable, serve arguments, canonical workdir and complete literal policy from task 3.1, exactly one MCP-only grant, no Cargo/Git grants, harness fragment or unexpanded token. Preserve every existing adapter fragment byte; the empty argv element is not literal quote characters. Requirement: [boxed-work-provider-admission — “The shipped engine smith's namespace launch is tested as composed”](specs/boxed-work-provider-admission/spec.md).

- [x] 3.3 Add H1's launch control alongside those tests, starting from a fixture compiled under harness rather than routing a fabricated namespace candidate into another branch. Verify production composition carries exactly the declared `--sandbox workspace-write` fragment, no workspace MCP registration/fragment or tool-list grant, while the compiled record still names harness and its unenforced-by-Brokkr hands policy. Requirement: [boxed-work-provider-admission — “Harness work is a separate case governed by the existing whole-chain rule”](specs/boxed-work-provider-admission/spec.md).

## 4. Collect independent removal evidence and restore every mutation

- [x] 4.1 Create `implementation-evidence.md` inside this change with a compact removal table recording each targeted test/command, exact temporary mutation, selected test count, relevant assertion failure, restoration and passing rerun. For N0, separately bypass the no-hands capability refusal and alter its diagnostic; verify the otherwise-valid seat's unintended admission and changed wording each fail the intended assertion before restoring and rerunning. Apply this evidence protocol to every task in this group: one production source or shipped-declaration mutation at a time, no permanent mutants, no expected-answer/fixture-only edits, broken-build substitutes, unrelated errors or zero-test passes. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.2 Remove workspace emission/admission behavior for N1, and separately make hands consult the retired tool list for N1/P1. Verify N1 detects lost admission/fragment and P1 detects introduced Cargo/Git grants despite the shared flag spelling; record relevant failures, restore each mutation and record passing targeted reruns. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.3 Bypass the boxed workspace-capability requirement for N2 and the internal resolver control that retains harness support. Verify each required absent/unsupported case catches unintended admission for its own reason, including measured-gap cases; record each mutation and observed assertion failure, restore and rerun passing tests. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.4 Exercise H1 admission/composition and H2/H3 independently: temporarily prevent valid harness-work admission, remove its composed work fragment, bypass the missing-work capability check, and restrict whole-chain enforcement to the first link. Verify the corresponding positive or reason-bearing refusal assertion fails, including both absent and measured unsupported H2 work declarations and the actual Claude fallback; restore each mutation and record its passing rerun. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.5 For both actual-shipped namespace launches, independently remove MCP registration and corrupt its executable or `hands serve` arguments. Verify each corresponding literal launch assertion fails for the claimed change; restore each mutation and record the passing rerun without invoking a provider or a namespace. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.6 For both namespace launches, independently alter/omit the forwarded canonical workdir, network policy, each bind's membership/mode and each Cargo credential mask. Verify every corresponding independent policy assertion fails; restore each temporary change and record passing reruns for both providers. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.7 For Codex, separately remove/replace the read-only sandbox, remove/alter the MCP approval setting, inject the harness-work fragment and inject a per-tool-list flag into production composition. Verify the respective presence/absence or full-argv assertion fails for each mutation; restore and record passing reruns, preserving the distinction between launch evidence and native-provider enforcement. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.8 For Claude, remove/alter each required workspace-fragment component, specifically its MCP permission grant, and separately introduce a retired Cargo/Git grant. Verify the full-fragment and grant assertions detect these independent changes, including P1's same-flag control; restore every mutation and record passing reruns. Requirement: [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

- [x] 4.9 Temporarily remove shipped engine-smith hands, change each ruled chain/effort pin, and restore a tools object beside hands. Verify task 2.1's declaration regression detects each change. Restore the intended declaration, confirm no temporary mutation remains, and record passing targeted admission, composition and roster reruns before measuring any digest. Requirement: [astra-engine-smith — “The engine smith hires the ruled chain through workspace hands”](specs/astra-engine-smith/spec.md); [boxed-work-provider-admission — “Every claimed compile and composition protection has removal evidence”](specs/boxed-work-provider-admission/spec.md).

## 5. Measure affected identities and preserve independent rosters

- [x] 5.1 After every mutation is restored, compile all shipped bundles and recipes in their existing namespace configuration, including inherited users and ancestors; use actual successful compile output or test-reported values to update every moved pin represented in `crates/brokkr-runtime/tests/witness_digests.rs` and `crates/brokkr-runtime/src/bundle/compose_tests.rs`. Add issue #307 history entries to both collections explaining Astra/Fable, their effort pins and hands replacing tools; preserve old history and unchanged pins. Verify `witness_digests` (including `every_bundle_in_the_tree_compiles`) and the compose tests pass, and record measured changes in the evidence file; do not invent charter, adapter or engine-version changes. Requirement: [astra-engine-smith — “Changed hires and hands are witnessed by actual compiled identities”](specs/astra-engine-smith/spec.md).

- [x] 5.2 Run the `library_data`, `roster` and entire `gpt_flash_shape` integration suites, explicitly including `every_shipped_panel_seats_at_least_two_providers` and `tool_grants_keep_house_tools_explicit_and_effort_never_rises_on_fallback`. Verify all pass with the GPT/Flash-specific engine office and diversity/dead-tool rules unchanged. If an existing whole-bundle harness-refusal pin moves to another first refusal, update only its measured site/reason and rerun it, preserving focused H3's Claude-link refusal. Requirement: [astra-engine-smith — “Changed hires and hands are witnessed by actual compiled identities”](specs/astra-engine-smith/spec.md).

## 6. Document confinement and the limits of deterministic evidence

- [x] 6.1 Update `docs/guides/provider-adapters.md` to explain how this restricted work seat uses declared hands on a provider without native per-tool flags: namespace needs `hands.workspace`, writes go through MCP hands, and Codex's native sandbox stays read-only. Describe the existing empty-root, declared-bind and network boundary, explicitly reject command-list enforcement, retain Codex's native host-read and provider-traffic limits, preserve Claude's MCP grant, and distinguish the unboxed harness route and whole-chain work requirement. Verify the prose agrees with the tested launches and reconcile current roster claims such as every hands agent chaining Opus without rewriting dated measurements. Requirement: [astra-engine-smith — “The guide and decision record describe the confinement actually declared”](specs/astra-engine-smith/spec.md).

- [x] 6.2 Append a dated 2026-09-21 issue #307 operator note to `docs/decisions/0043-the-hands-are-one-tool.md`, withdrawing the earlier claim that hands enforce the Cargo/Git allow-list and recording the existing workspace-confinement and separate-path rulings. Verify the diff preserves its accepted status and historical rulings and creates no new per-tool, shell-command, transport or native-tool enforcement contract; this is application of accepted semantics, not acceptance of a new decision. Requirement: [astra-engine-smith — “The guide and decision record describe the confinement actually declared”](specs/astra-engine-smith/spec.md).

- [x] 6.3 Complete the evidence file's delivery account with the measured admission/composition/removal results and the exact tools-removal rationale. State that the ruling is expressible, not proved by a live Astra smith; record the controller's first post-landing Cargo/Git-through-Codex-hands, real-commit and verify-passing measurement as pending. Verify neither docs nor evidence claims a live run, new resume qualification, command filtering or host-read secrecy; record unavailable checks and namespace skips as pending evidence, and keep result notes descriptive rather than instructing a gate. Requirement: [astra-engine-smith — “Deterministic evidence does not stand in for the first live smith”](specs/astra-engine-smith/spec.md).

## 7. Validate and commit the implementation before folding

- [x] 7.1 Run `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`; fix in-scope failures and record commands/results in the evidence file. Verify both are green, without excluding added code or changing the warnings policy. Requirement: [astra-engine-smith — “Deterministic evidence does not stand in for the first live smith”](specs/astra-engine-smith/spec.md).

- [x] 7.2 Run `cargo test -p <crate> --all-features --locked` separately for `brokkr-core`, `brokkr-store`, `brokkr-protocol`, `brokkr-runtime`, `brokkr-bridge`, `brokkr-view` and `brokkr-cli`, then `cargo test --workspace --all-features --locked` and `cargo run --locked -p brokkr-cli -- compile --bundle bundles/self`. Verify every crate suite, including the whole runtime crate, the workspace suite and self compile is green; record failures, unavailable tools and skipped namespace proof honestly rather than counting them as successful evidence. Requirement: [astra-engine-smith — “Deterministic evidence does not stand in for the first live smith”](specs/astra-engine-smith/spec.md).

- [x] 7.3 Obtain host/CI execution of `bash scripts/coverage-exact.sh` outside the workspace box, where namespace boundary tests can execute. Verify its pinned compiler and unchanged literal nonzero 100% line/branch/function gate pass on the implementation revision, including every added production line and the script's existing test-source convention; attach command, revision and report evidence. If external execution is unavailable, leave this task unchecked and the evidence pending; do not lower the threshold, add exclusions, grant network or claim implementation completion. Requirement: [astra-engine-smith — “Deterministic evidence does not stand in for the first live smith”](specs/astra-engine-smith/spec.md).

- [x] 7.4 Run `openspec validate --all --strict --no-interactive` and `git diff --check`, and review the complete slice diff against its pre-implementation revision. Verify strict validation and whitespace pass; `contracts/`, `policy/phase-machine.json`, `policy/schemas/`, `fixtures/`, `reference/`, `extensions/` and the issue #226 ledger are unchanged; no Windows work, unmeasured identity pin or unauthorized semantic change entered the slice. Reconcile every requirement/scenario against its completed task and evidence before marking anything done. Requirement: [astra-engine-smith — “Deterministic evidence does not stand in for the first live smith”](specs/astra-engine-smith/spec.md).

- [x] 7.5 After tasks 1.1–7.4 pass, verify each completed task was ticked as it finished and commit the declaration, tests, measured pins, docs and evidence in the repository's issue-bearing message style without pushing. Verify the commit contains the reviewed changes, all temporary mutations are gone and the worktree is clean; retain the controller's post-landing live measurement as a named residual rather than treating it as a completed implementation task. Requirement: [astra-engine-smith — “Deterministic evidence does not stand in for the first live smith”](specs/astra-engine-smith/spec.md).

## 8. Fold the change into living truth as the final task

- [x] 8.1 Run the dialect's declared `openspec archive 2026-09-21-307-astra-engine-smith --yes` only after the preceding implementation work and checks are complete. Verify it moves this change under `openspec/changes/archive/` and folds both capability deltas into `openspec/specs/astra-engine-smith/spec.md` and `openspec/specs/boxed-work-provider-admission/spec.md`. In each living capability append exactly one ``- `<actual-archived-directory-name>` — folded <actual-YYYY-MM-DD>`` line beneath a `## Provenance` heading at the end, preserving all existing provenance. Run `openspec validate --all --strict --no-interactive`, `openspec validate --archived --strict --no-interactive` and `git diff --check`; verify green results, tick this final item in the archived ledger and commit the fold/provenance/ledger without pushing, leaving a clean worktree and every completed task ticked. On a later returned visit, reopen with `git mv` before amending and answer the finding in dependency order. Requirement: [astra-engine-smith — “The engine smith hires the ruled chain through workspace hands”](specs/astra-engine-smith/spec.md); [boxed-work-provider-admission — “Namespace hands replace the tool list only through declared workspace support”](specs/boxed-work-provider-admission/spec.md); [astra-engine-smith — “Deterministic evidence does not stand in for the first live smith”](specs/astra-engine-smith/spec.md); dialect `dialects/openspec.json` archive operation and `dialects/openspec/archive.md`.
