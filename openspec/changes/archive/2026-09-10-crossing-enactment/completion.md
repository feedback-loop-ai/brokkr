# Crossing enactment: completion and later archive amendment

## Close-out, 2026-09-10

The change folded decision 0057's whole Phase 2 mechanism — the v5
vocabulary and its pure refusals, the loader that resolves and verifies
at world load, the `run-manifest/v10` recording, the refusals at run
start, rerun, resume and compile, doctor's report, and the `realms` and
`muninn` readouts — into one living capability, `realm-crossing`. It
added no behavior beyond what slices (i)–(vi) already built and proved,
and the archived strict validator passed with every requirement carrying
a scenario.

## Later archive amendment — readout reconciliation, 2026-09-10

The review of slice (vi) found the archived delta no longer yielding the
living capability its `## Provenance` line names. The capability had
been refined after the first fold: a moved refusal names the publisher's
declared path rather than the resolved host location, the dossier
carries entry-level crossing content and a no-journal case, and Muninn
snapshots that evidence into its own record. The archived delta still
described the earlier text, so the one-way fold no longer held.

This change is reopened and amended under decision 0042 ruling 6 and its
September addenda. The archived delta
`specs/realm-crossing/spec.md` is reconciled so its requirements and
scenarios again yield the living capability; the proposal and tasks
account for the refinements, and the design records the
declared-versus-resolved path reading. The living spec and the archived
delta are edited together and agree — the delta's body differs from the
living capability only by its `## ADDED Requirements` header, its title
and the provenance line the fold appends. The change remains archived as
`2026-09-10-crossing-enactment`, and the archived strict validator
passes.
