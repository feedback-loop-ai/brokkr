## 1. Admitted-version type and exact-token check

*Serves: "Discovery identifies one owned local file" (content admission — the admitted set `{0, 3}`, exact spellings, recorded-token exactness scope). Design: D3.*

- [ ] 1.1 Add `enum DshVersion { Zero, Three }` (private to `transcript.rs`) and change `dsh_header`'s return type from `bool` to `Option<DshVersion>`, admitting numeric zero as `Zero` exactly as today. Verify: `cargo check -p brokkr-view` fails only at call sites still expecting `bool`, confirming the type change is isolated.
- [ ] 1.2 Generalize `zero_number_token` into a signature check over the recorded token (sign stripped, mantissa split from exponent, mantissa digits with the decimal point and leading/trailing zeros removed compared against a target signature — empty for zero, `"3"` for three), keeping the existing zero behaviour byte-for-byte. Verify: unit tests confirm `3`, `3.0`, `3e0`, `30e-1`, `0.3e1` match signature `3` and `3.1`, `-3`, `3e1`, `2.9999999999999999`, `3.0000000000000001` do not, while the zero cases are unchanged.
- [ ] 1.3 Update `dsh_header` to admit an exact numeric three, cross-checked against the recorded token via 1.2's signature, so the admitted set is closed at exactly `{0, 3}` and returns `Some(DshVersion::Three)`; every other numeric value, non-numeric value and missing `version` stays `None`. Verify: `dsh_header_version_matrix_admits_only_numeric_zero` extended into the two-version matrix (admitted `0, 0.0, 0e0, -0, 3, 3.0, 3e0, 30e-1, 0.3e1`; refused `null, false, "0", "3", [], {}, 1, 2, 4, -1, -3, 0.5, 3.1, 3e1, 2.9999999999999999, 3.0000000000000001`, and an absent version) passes.
- [ ] 1.4 Confirm `dsh_millis_exact`/`zero_number_token`'s use for `time`/`time0` exactness is untouched by 1.2's generalization (it must keep checking only the zero signature, never three). Verify: existing underflow regression tests at `transcript/tests.rs:2143` and `:2180` stay green unmodified.
- [ ] 1.5 Thread the admitted `DshVersion` from `project_dsh`'s header match through to the row-dispatch call so every later row is classified under the vocabulary of the version that admitted the file. Verify: `cargo check --workspace` compiles with the version parameter threaded end to end (compiles only after group 2 supplies the new `dsh_row`/`dsh_quiet_event` signatures; groups 1 and 2 land together in one commit).

## 2. Per-version row dispatch and the payload-refusal variant

*Serves: "DSH sessions expose assembled or provisional content once" (version-three fragment/attempt/payload/association/surfaceOp paragraphs) and "Partial records and read failures remain distinguishable" (per-version vocabulary enumeration). Design: D4, D6, D7, D8, D10.*

- [ ] 2.1 Add `DshRow::RefusedPayload`, mapped in `project_dsh` exactly as `DshRow::Refused` (`unrecognized += 1; refused = true;`), so the payload-refusal cause stays separable in code from a citation/packed-row refusal while producing the same observable outcome. Verify: `project_dsh`'s match stays exhaustive; `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` is clean.
- [ ] 2.2 Add a `version: DshVersion` parameter to `dsh_row`; under `Three`, return `DshRow::RefusedPayload` for `user/message`, `assistant/message` and `tool/result` before any payload, citation, time or marker read (blocks, `dsh_citations`, `surfaceOp`, `ignorable` all unread for these three kinds). Verify: a payload-refusal test with a `surfaceOp` replace marker and citations on the row shows they are neither applied nor reported, and an `ignorable: true` marker on the row does not omit it.
- [ ] 2.3 Under `Three`, keep `tool/call` on the existing arm, projecting `callId`/`name`/`arguments` carried on the event itself. Verify: a version-three session with a header and one `tool/call` projects that call with zero diagnostic counts.
- [ ] 2.4 Under `Three`, dispatch `assistant/attempt` to `DshRow::Omission` (counted, no content, read stays available), with or without `ignorable`. Verify: `tool/call`, `assistant/attempt`-with-stream, `tool/call` projects two calls in order with `unrecognized_records: 1` and one notice; the same with `ignorable: true` on the attempt yields the same result.
- [ ] 2.5 Write out the full 51-name version-three quiet list in `dsh_quiet_event(kind, version)`: the 44 version-zero quiet names other than `tool/code-dispatch` and `tool/code-dispatch-start`, plus `system/message`, `deliverables/presented`, `feedback/message-delete`, `feedback/message-put`, `subagent/catalog`, `tool/ptc-dispatch`, `tool/ptc-dispatch-start`. Leave the version-zero list (46 names) untouched. Verify: a version-three session with a `tool/call` followed by one row of each of `system/message`, `deliverables/presented`, `feedback/message-delete`, `feedback/message-put`, `subagent/catalog`, `tool/ptc-dispatch`, `tool/ptc-dispatch-start`, `todo/write`, `turn/end` and `request/header` projects the call with zero counts and no notice.
- [ ] 2.6 Under `Three`, dispatch `assistant/chunk`, `text-chunks`, `reasoning-chunks`, `tool-call-chunks`, `tool/code-dispatch` and `tool/code-dispatch-start` to `DshRow::Unrecognized` (required unknown; never decoded by the fragment or packed-row path). Verify: each, following a `tool/call`, refuses with one unrecognized count and its notice; the same row with `ignorable: true` instead projects the call once with one unrecognized count.
- [ ] 2.7 Confirm the eight version-three-only names (`assistant/attempt`, `deliverables/presented`, `feedback/message-delete`, `feedback/message-put`, `subagent/catalog`, `system/message`, `tool/ptc-dispatch`, `tool/ptc-dispatch-start`) stay `Unrecognized` under `Zero`, unchanged from today. Verify: a version-zero session with a readable `user/message` followed by each of those seven non-`assistant/attempt` names, without `ignorable`, refuses with one unrecognized count each; with `ignorable: true` each instead projects the user message once with one unrecognized count.
- [ ] 2.8 Add a set-relation unit test asserting the version-three quiet list equals the version-zero quiet list minus `tool/code-dispatch` and `tool/code-dispatch-start` plus the seven names of 2.5, and that neither list contains `assistant/attempt`, `assistant/chunk` or a packed-row tag. Verify: the test fails if either list is edited without the other.
- [ ] 2.9 Confirm `associate_dsh_tools` needs no version guard: under version three every projected event is a dedicated `tool/call`, so the existing embedded-copy-removal rule has nothing to remove by construction. Verify: no new branch added; existing association tests unchanged and green.

## 3. Discovery and sibling-file pinning

*Serves: "Discovery identifies one owned local file" (the versioned-name-is-decisive rule, the filename/version independence pairing, the seeded-header scenarios). Design: D9, D5 Q3.*

- [ ] 3.1 Add the two sibling scenarios beside the existing both-names test in `crates/brokkr-cli/src/ui/tests.rs` (`:3666`): a directory with `session.v3.jsonl` at version 3 beside `session.jsonl` at version zero reads the versioned name and projects under the version-three vocabulary; a directory with `session.v3.jsonl` at version 4 beside a readable `session.jsonl` at version zero refuses with the confirmed `session.v3.jsonl` path, zero counts and no turns, never falling through to the plain name. Verify: both tests pass with no `ui.rs` change.
- [ ] 3.2 Add the filename/version-independence triple: `session.jsonl` carrying a version-three header projects its `tool/call` under the version-three vocabulary; `session.v3.jsonl` carrying a version-zero header projects its message under the version-zero vocabulary; `session.v3.jsonl` carrying version 4 refuses. Verify: all three project/refuse as specified, each with zero counts where admitted.
- [ ] 3.3 Add the seeded-header tests: `isSeeded` true, false, a string, an object, null and absent under both a version-zero and a version-three header admit and change no row's classification; a candidate with version 3, `isSeeded: true` and `delegationDepth: 1` is not a candidate and yields `not-found` with null path/hint. Verify: tests pass; no classification differs across `isSeeded` values.

## 4. Projector unit tests for the version-three vocabulary and payload refusal

*Serves: "DSH sessions expose assembled or provisional content once" and "Partial records and read failures remain distinguishable" (the version-3 scenarios). Design: D12.*

- [ ] 4.1 Add the payload-refusal test (`dsh_v3_message_rows_refuse_until_their_definitions_are_read`) covering the delta's five files: a `tool/call` followed by a `user/message` with string content; an `assistant/message` with familiar `text`/`reasoning` blocks, `interrupted: true`, a `stream` array, a `usage` object and a trailing partial JSON append; an `assistant/message` with `surfaceOp: {"op":"replace",...}` and `sourceEventSeqs`; a `tool/result` naming the call's `callId` with matching `turn`/`step`; and the `user/message` case repeated with top-level `ignorable: true`. Verify: each returns `unsupported-format`, `unrecognized_records: 1`, `skipped_lines: 0`, `truncated: false`, exactly one notice and no turn; the same four message rows under a version-zero header keep their existing version-zero projection.
- [ ] 4.2 Add `dsh_v3_tool_call_alone_projects_as_a_wiring_proof`, with a doc comment stating it proves the version-three dispatch path reaches the `tool/call` arm and is not a claim that a real version-three session reaches a successful read today. Verify: a header plus one `tool/call` projects the call with zero counts.
- [ ] 4.3 Add the time-exactness test: a header spelling its version `3e0` with three `tool/call` rows whose `time` is `1e-400`, `1e3` and `-0.0` projects stamps `""`, `"1000"` and `"0"` in order, zero counts, no notice. Verify: test passes and the existing version-zero underflow regressions (`:2143`, `:2180`) are untouched.
- [ ] 4.4 Run the full existing DSH test suite in `crates/brokkr-view/src/transcript/tests.rs` and confirm every pre-existing test (version-zero fragments, packed rows, citations, association, compaction) is unmodified and green. Verify: `cargo test -p brokkr-view`.

## 5. CLI and TUI end-to-end coverage

*Serves: "Partial records and read failures remain distinguishable" (the located scenario's CLI text/JSON/TUI projection, presented through `transcript-command`/`transcript-tui`). Design: D12.*

- [ ] 5.1 Add the located end-to-end scenario to `crates/brokkr-cli/tests/transcript_command.rs`: one root's `session.v3.jsonl` projecting one `tool/call` through CLI text and CLI JSON, with `--turn 1` reaching it; a second root's `session.v3.jsonl` refusing with `unrecognized_records: 3`, the notice `unrecognized transcript records: 3`, and no turns. Verify: both cases pass in CLI text and CLI JSON form.
- [ ] 5.2 Add the matching scenario to the TUI test suite: the same two roots, with the refused root keeping its notices in the pane/error overlay and both reading doors (`--turn`, overlay) disabled. Verify: TUI tests pass.

## 6. The proposed decision

*Serves: house rule that a semantic change carries a `Status: proposed` decision, supplementing 0055 ruling 3. Design: D11.*

- [ ] 6.1 Re-read `docs/decisions/README.md` at commit time to confirm the next free number (0056 and 0060 may be claimed by other branches per the design's D11 note; do not reuse either without re-checking). Write `docs/decisions/<NNNN>-<slug>.md` with `Status: proposed`, recording D11's eight rulings: measured/closed/named admitted versions with reach as a per-read operation; per-version vocabulary; a payload projects only on its own writer's definitions; version three persists whole messages; the surface is not the transcript; `isSeeded` is inert; recorded-token exactness has a fixed scope (`version`, `time`, `time0` only); unmeasured parts refuse. Cite D2's package/file/digest table. Verify: file exists with `Status: proposed` and the eight rulings present.
- [ ] 6.2 Add the new decision's row to `docs/decisions/README.md`'s index table, following the existing row format. Verify: the row renders consistently with neighboring rows (title, one-line summary, `proposed`).

## 7. Guides sweep

*Serves: no requirement directly; keeps documentation coherent with the admitted set. Design: Migration Plan item 1.*

- [ ] 7.1 Grep `README.md`, `ARCHITECTURE.md`, `CONTRIBUTING.md` and `docs/guides/` for language that states or implies DSH version zero is the only admitted version (e.g. "version zero only"). Update only lines making that explicit claim to reflect the admitted set `{0, 3}`; the design's own sitting found none, so record the result of the re-check either way. Verify: `grep -rn "version zero only" README.md ARCHITECTURE.md CONTRIBUTING.md docs/guides/` output is reconciled with what the code now admits.

## 8. Gates, fold and commit

*Serves: house rules (tests/format/clippy/bundles/openspec validation) and the dialect's archive-as-final-task rule.*

- [ ] 8.1 Run `openspec validate --all --strict`, `cargo fmt --all -- --check`, `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`, `cargo test --workspace --all-features --locked`, `cargo run --locked -p brokkr-cli -- compile --bundle bundles/self`, and `cargo run --locked -p brokkr-cli -- compile --bundle bundles/verify`. Verify: every command exits clean; fix and re-run before proceeding.
- [ ] 8.2 Fold this change into the living truth with `openspec archive admit-dsh-session-v3 --yes`, updating `openspec/specs/transcript-reading/spec.md`'s three requirements and moving the change under `openspec/changes/archive/`. Verify: the living spec now states the `{0, 3}` admitted set, the per-version vocabulary and the payload refusal; `openspec validate --all --strict` still passes.
- [ ] 8.3 Record the exact-coverage gate as pending: it runs outside this box (host validation, boundary-namespace tests cannot nest in the box) and is not lowered by this change. Verify: no coverage threshold edited in this change; the pending state is noted in the activation commit message.
- [ ] 8.4 Commit all work (code, tests, the decision file, the README row, the archived change) unsigned, in the repository's message style, noting `#279` and that exact coverage and remote CI/publication/closure remain the controller's. Verify: `git status` is clean, `git log -1` shows the commit, and nothing is pushed.
