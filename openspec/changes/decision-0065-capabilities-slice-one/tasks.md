# Decision 0065, slice one — implementation tasks

Adopt the existing implementation and commits. This design return amends only
tasks affected by H1–H4, M1–M4, the macOS fixture and their final proofs, after
correcting [design.md](design.md). The proposal and six [spec deltas](specs/)
govern; design D3–D8 and D10 supply the repair mechanisms. Checked unaffected
work remains adopted. Reopened boxes identify incomplete repair obligations,
not a request to rebuild the original slice. Historical 40-of-43 completion is
recorded in [evidence.md](evidence.md); it is not the present repair status.

Before code repair, complete task 0.1. Within each affected task first reproduce
its finding red, then fix, then independently remove enforcement, read the
intended failure, restore and rerun. Work follows the existing dependency order.
Tests compare entire refusals/notices/report lines, complete final argv, or
specified identity comparisons; `is_err()`, status-only and substring checks
cannot establish these proofs. Task 9.4 records each finding's observations.

Accepted 0065 remains accepted; only the operator accepts the separate proposed
repair decision. H4 remains HIGH with `spec_defect=true`; D7's unsafe exception
and old task 6.1 completion basis are withdrawn here. This repository's v3 map
continues granting nothing. Production remains Rust under `crates/`. New test
inputs belong in temporary trees or owning suites, never `fixtures/`.

Do not modify existing contract versions, `policy/phase-machine.json`,
`policy/schemas/`, `fixtures/`, `reference/`, `extensions/`, the event envelope
or the issue-226 task ledger. Preserve historical evidence. Slice two owns MCP
brokers, gate capability policy, checkpoint enrichment and retained results;
slice three owns comparisons and `capabilities: equal`. This slice defines
MCP data and refuses its use. Linux and macOS are the only supported hosts.
Additional semantic rulings require a separate proposed decision; do not amend
accepted 0065 through an implementation exception.

Historical task-phase validation and implementation observations remain in git
history and evidence; no reopened obligation is proved by those earlier passes.
This coherence amendment creates no implementation or coverage result. The
security hold and archive task 12.1 remain open for council re-judgment.

## 0. Record the proposed repair decision before code changes

- [ ] 0.1 Add the next available numbered repair decision under `docs/decisions/`, with status `proposed`, leaving accepted 0065 untouched. Record H1–H4/M1–M4, especially H4's withdrawn active-input exception, the accepted design choices/alternatives, migration, evidence limits and concrete enforcement/test bindings from D3–D10. Verify agreement with the amended proposal/specs/design before implementation; no new grants or later-slice semantics. Requirements: [Known native powers require a valid delivered denial or refusal][NCR]; [Authored provider configuration cannot supply capability authority][RGR]; [Active instructions and policy cannot escape bundle identity][MPI]; house proposed-decision rule.

## 1. Publish additive contracts

- [x] 1.1 Add `contracts/tool-dialect.v1.schema.json` with D3's closed discriminated shape, identity/serves/tools, provider-native binding, reserved workspace hands, abstract-class equality annotation and separate disclosure metadata. Verify positive native/hands examples and schema rejection of unknown/mixed kinds, missing binding fields, invalid classes and egress vocabulary. Requirements: [A tool dialect binds one capability to exactly one implementation kind][TD1]; [Dialects describe their disclosure using the existing egress vocabulary][TD3]; [Reserved hands preserves the existing workspace authority][TD6].
- [x] 1.2 Complete that new tool-dialect contract's MCP branch with exactly one stdio argv or URL connection, named tools, pinned version, 0012 binding names and optional retention declaration; define the embedded Draft 7 restriction schema field. Verify both connection forms as data and schema rejection of mixed/incomplete forms and literal credential fields, without server/network access. Task 2.4 completes semantic binding/reference checks. Requirements: [The MCP contract is whole before its runtime exists][TD4]; [Restriction keys are defined by the selected dialect][TD5].
- [x] 1.3 Add `contracts/realms.v6.schema.json`, preserving v5 fields and admitting the per-realm grants map with optional tool/office lists and dialect-owned restriction values. Verify valid omission/empty maps and lists, invalid nulls/types/duplicates, and schema refusal of `capabilities` under every legacy version. Requirement: [Realms v6 adds grants without changing frozen versions][RG1].
- [x] 1.4 Add `contracts/run-manifest.v11.schema.json` with D7's required, structured capability section, explicit empty outcomes, per-candidate records, relative sources/digests and inactive grant context. Register all three new contracts additively in `crates/brokkr-runtime/tests/frozen_contracts.rs`; verify hand-built positive/negative v11 examples and unchanged hashes for every existing version. Requirement: [A new manifest version records capabilities per executable seat][MP1].

## 2. Load operator authority and pass its context explicitly

- [x] 2.1 Extend `brokkr-core/src/realms.rs` and runtime realm representation for v6 grants, preserving omitted versus explicit tool/scope lists. Check field presence before defaults so v1–v5 reject even `capabilities: {}`. Verify core realm tests for all six versions, absent map/field, empty maps, malformed data and unchanged hands/boundary fields. Requirements: [Realms v6 adds grants without changing frozen versions][RG1]; [Office scopes and tool subsets only narrow a grant][RG3].
- [x] 2.2 Introduce D2's explicit `CapabilityContext` and thread it through CLI compile/run/rerun, runtime compile entry points and semantic library lint. Resolve definition/dialect roots beside the active realm map, otherwise under the operated repository; convenience APIs must construct an explicit no-grant context. Verify mapped/unmapped and neighboring-realm context tests, and that recipe roots and agent-library overrides cannot replace operator authority. Task 4 connects authorization to every entry point. Requirements: [Realm context is resolved before capability authorization][RG5]; [Only an operator's realm selects concrete implementations][RG2].
- [x] 2.3 Implement the strict abstract-definition loader in a focused runtime `capabilities` module: safe names, contained paths including symlinks, filename/name equality, exactly name/classes, nonempty unique classes and deterministic duplicate/conflict rejection. Read/hash the same bytes and validate every definition in the root. Verify CQ2's valid operator-defined name, missing roots, absent/conflicting metadata, recipe-local impersonation and class-order equality with exact owning diagnostics. Requirement: [Capability classes belong to the abstraction][TD2].
- [x] 2.4 Implement strict selected-dialect loading and restriction validation, including containment, duplicate-key rejection in new authority data, serves/class agreement and same-buffer SHA-256. Promote the already locked `jsonschema` workspace dependency from runtime dev-only to production with default features disabled, for complete operator-schema validation; permit only local embedded Draft 7 references, reject external retrieval and reserved-key redefinition through references/composition. Verify valid/invalid schemas, unchanged values/array order, missing/escaping references and schema/loader agreement. Check MCP undeclared secret references and URL userinfo without echoing credentials or opening a secret store. Confirm no package/version upgrade and record the dependency-edge justification. Requirements: [A tool dialect binds one capability to exactly one implementation kind][TD1]; [Restriction keys are defined by the selected dialect][TD5]; [Only an operator's realm selects concrete implementations][RG2]; [The MCP contract is whole before its runtime exists][TD4].

## 3. Declare requests and native assessments

- [ ] 3.1 Extend `agents.rs`, `agents/load.rs` and every executable-site parser with abstract name-to-`requires | wants` requests. Semantic lint resolves every loaded request, including later subtractions, using task 2's operator definitions. Verify arbitrary valid abstraction names, invalid strengths/null/concrete grant objects/class overrides, and exact CQ2 lint versus compile missing-definition diagnostics before optional dropping or provider selection. Requirement: [Requests use abstract names and a closed strength vocabulary][SC1]. **Repair obligation:** M2/M3: use `parse_strict` on every recipe layer and agent source buffer before maps, including nested/selected/composed sites and repeated outer fields; exact raw-JSON duplicate diagnostics cover both strength orders and equal repetitions. Run `authority.definitions.lint(&library)` once for every actually loaded library before seat resolution/subtraction, including unseated agents, through `CompileError::Capability`; keep CLI lint shared. Pin all definition names consulted by that lint. Prove valid-library controls and independent parser/lint removals. Additional requirement: [Capability classes belong to the abstraction][TD2].
- [x] 3.2 Preserve omission versus explicit request maps: agent-backed omission inherits, an explicit unchanged-strength subset subtracts, `{}` removes all, and inline maps supply office asks. Verify addition/strength-change refusals and inherited required subtraction across ordinary, panel, sequence, selected and composed bodies; retain both stable office and execution-site identities. Requirement: [A seat can subtract but cannot widen its office][SC2].
- [ ] 3.3 Add strict adapter `native_capabilities` parsing for D5's known inventory versus reason-bearing unmeasured inventory, independent ON/OFF variants, evidence/limits, native tool sets, selection flag mappings and typed whole-object restriction transport. Verify missing halves/reasons, mixed variants, unexplained empty argv, duplicate keys and invalid selection mappings; absent older assessments become unmeasured, never verified empty. Requirement: [Native capability controls are adapter-owned evidence-bearing data][NC1]. **Repair obligation:** H1/H3: absent/legacy data may describe unmeasured knowledge but cannot satisfy known-provider denial. Validate every accepted plan representation with the provider composer and refuse malformed engine-plan members instead of decoding defaults. Preserve adapter duplicate enforcement and its exact removal regression.
- [x] 3.4 Ship operator `capabilities/web-search.json` and `web-fetch.json` as reads/egress definitions and only the three provider-native tool dialects in D9: Codex search, Claude search and Claude fetch. Add Codex's measured cold-default ON and exact OFF pair, Claude's declared search/fetch selection controls, and DSH/LaneTally's explicit commissioned unmeasured reasons; describe generic exec's unknown child inventory honestly. Shipped dialect restriction schemas admit only empty objects until a control exists. Verify library-data tests load each binding, tools/keys agree, evidence is scoped, and repository realms still grant nothing. Requirements: [Capability classes belong to the abstraction][TD2]; [Codex web-search OFF uses the controller's measured fragment][NC2]; [Other provider declarations preserve the commissioned uncertainty][NC5].
- [x] 3.5 Migrate `agents/researcher.json` to abstract web-search/web-fetch wants, retain local command restrictions and remove concrete web aliases; add the DATA-only sentence beside capability use in its charter and any other affected capability-using charter. Refuse nonempty legacy `tools.mcp`, even optional entries, and native-owned legacy allow aliases with full migration reasons; keep empty MCP lists valid. Verify strict library and charter tests, local restrictions unchanged and declarations contain no implementation choices. Requirements: [Legacy concrete permissions cannot grandfather a capability][SC7]; [Capability responses are data and confer no authority][MP4].

- [ ] 3.6 Canonicalize `agents/tests::Tree`'s temporary root once at creation, retain its cleanup guard, and derive all writes/expected paths from that stored root. Sweep the slice's new fixtures for the same lexical-root assumption; keep exact diagnostic assertions. Reproduce the alias-root failure and prove restoration, then record actual Linux and macOS results separately. Never glue a host prefix or canonicalize only the expected string. Requirement: [Refusal proofs assert the full reason][SC8] (scenario: macOS canonical fixture roots preserve exact diagnostics).

## 4. Resolve grants and deny independently

- [x] 4.1 Validate every selected-realm grant before seat compatibility: definition/dialect existence, serves/classes, tool subset, offices and restriction schema. Then refuse MCP grants with the complete slice-two diagnostic even for wants, no requests or `offices: []`, and refuse reserved hands with the 0043/0046 reason. Verify exact diagnostics and positive native controls without server launch; invalid data cannot become an optional drop. Requirements: [Any MCP realm grant refuses until slice two][SC6]; [Reserved hands preserves the existing workspace authority][TD6]; [Restrictions are validated, carried and pinned without engine interpretation][RG4].
- [x] 4.2 Implement the pure office-asks-minus-subtractions intersection with scoped realm grants. Resolve omitted versus empty scopes/tools exactly; produce named required refusals and deterministic complete wanted-drop notices, never enable an unused grant. Verify missing grant, wrong office, no tools, subtracted and no-ask cases from positive examples, including the exact D4 missing-grant reason. Requirements: [Compilation holds only the request and applicable grant intersection][SC3]; [Office scopes and tool subsets only narrow a grant][RG3].
- [ ] 4.3 Resolve provider/native-key/tool compatibility and exact expressibility of narrowed tools. Refuse required incompatibility and drop wanted incompatibility with candidate-specific reasons; a shared switch cannot enable an excluded tool. Preserve `resolve_report`'s whole-chain required refusal and existing fallback eligibility. Verify provider mismatch, missing native key, unmeasured/unsupported ON, fixed-set versus selectable subsets and independent candidate outcomes without dialect substitution. Requirement: [Provider compatibility cannot expand a holding][SC4]. **Repair obligation:** H3: call D6's shared fallible provider composer during admission; no provider may accept argv/selection/restriction forms it discards. Pin no unsupported successful plan; full compile refusals name provider/capability/form. Keep required versus optional incompatibility behavior and authorized ON positive controls.
- [ ] 4.4 Implement CQ1's valid-restriction precedence: required incompatibility refuses, wanted incompatibility drops the whole capability, unused/subtracted grants remain inactive and office exclusion precedes transport compatibility. A supported transport carries canonical JSON as one typed argument and retains the original structured value. Verify exact CQ1 refusal/notice strings, invalid-restriction refusal before optionality, inactive context with no invented notice/ON plan, and an expressible synthetic restriction round-trip. Requirement: [Restrictions are validated, carried and pinned without engine interpretation][RG4]. **Repair obligation:** H3/M4: assert supported restriction argv at the final serving command, including Claude, and use independently runnable required/optional/unused CQ1 tests; unsupported representations refuse at compilation. Optional notice proof is task 9.1.
- [ ] 4.5 Independently calculate OFF for every known unheld native tool, even without asks, after wanted drops or with a grant for another office. Measured unsupported OFF refuses with the full seat/realm/capability/provider/evidence cause and overrides optional drop success; unknown inventories remain unmeasured and cannot satisfy requests. Wire this resolution through every compile entry point. Verify boxed/unboxed no-ask, scope, subtraction, CQ1 unused/drop and measured-impossible cases with complete diagnostic equality. Requirements: [An impossible native denial is a compile refusal][SC5]; [Other provider declarations preserve the commissioned uncertainty][NC5]; [Refusal proofs assert the full reason][SC8]. **Repair obligation:** H1: replace security-relevant best-effort adapter loading with a preserved `Result` and D4's known-provider floor; missing/legacy/empty inventory, omitted known powers, unreadable/malformed data and unmeasured known OFF refuse with full site/realm/provider/capability/source cause. Include unrelated malformed adapters, no asks/grants, no map and every v1–v5 realm. Audit `native_controls::managed` and `codex_managed` retry suppression as well as `load_pin_adapters`. Unknown inventories cannot erase a known obligation. Additional requirement: [Known native powers require a valid delivered denial or refusal][NCR].

## 5. Carry one sealed outcome through every site

- [ ] 5.1 Extend `SiteFacts` and each executable `Candidate` with the resolver-owned outcome from D5: office/site, original asks/subtractions, held/not-held reasons, native control plan and candidate notices. Use the existing site walk; preserve office identity while `relocate_verify_facts` moves the whole family, and give generated helpers explicit empty outcomes. Verify site/selection/compose suites across ordinary, nested panel/sequence, selected, inherited and wrapped sites, including existing address-collision refusals. Requirements: [A seat can subtract but cannot widen its office][SC2]; [A new manifest version records capabilities per executable seat][MP1]. **Repair obligation:** H2/H3: preserve candidate-owned base arguments and managed fragments when composition appends them; generated/nested/inherited sites and whole-family relocation keep those facts coherent. Do not recover provenance by deleting matching strings.
- [ ] 5.2 Carry the selected outcome through every engine composite dispatch and private provider invocation input, keeping authored arguments separate from managed controls. Refuse a recognized model launch with missing computed authority; opaque custom drivers remain unmeasured. Verify synthetic primary/fallback and nested dispatches retain their own outcome and model input/result/context text cannot overwrite controls. Requirements: [Neither inline arguments nor fallback can override native denial][NC4]; [Capability responses are data and confer no authority][MP4]. **Repair obligation:** H1/H2/H3: carry D6's explicit fragment provenance through `Candidate`, `SiteSpawn` and private `launch_arguments` beside `native_controls`, including every fallback/composite path. Final adapters verify the actual argv matches expanded fragments and refuse missing/malformed engine authority/provenance before provider work. Prove user/context/results cannot erase or overwrite these fields to enter the by-hand path. Additional requirement: [Authored provider configuration cannot supply capability authority][RGR].

## 6. Bind manifests, start and resume to the authority

- [ ] 6.1 Retain v11 capability projection, all grants/dialects/native declarations/candidates and consulted definitions, including unseated loaded-agent requests, using existing relative sources and no expanded argv/secrets. **H4 specification correction:** D7's former allowance of unpinned active files is withdrawn. Share `unpinned_top_level` with an active-input check in `parse_role` and each layer's `compose::own_table`; refuse excluded charter/policy references before use, both normalized lexical and canonical targets, with actual declaring layer/site/kind/path attribution. Preserve independently pinned agent/dialect inputs, script refusal and unrelated unconsulted-definition stability. Prove four independent standalone/inherited charter/policy refusals before/after edits, alias cases, permitted relocation/identity movement and later-read integrity; remove each fence and observe the intended failure. Requirements: [A new manifest version records capabilities per executable seat][MP1]; [Active instructions and policy cannot escape bundle identity][MPI].
- [ ] 6.2 Retain capabilities through `bundle_manifest_from_run` and existing instance identity. Add independent digest tests for grant, office scope, tools, restriction, unused grant, dialect selection/bytes, consulted definition bytes and native-control declarations, plus identical-input stability. Verify dispatch-v2 preserves its explicit unsupported-key refusal before creating a run instead of stripping v11 authority; leave its frozen wire contract intact. Requirement: [Capability authorization participates in bundle identity][MP2]. **Repair obligation:** H4/M3: independently prove permitted charter and valid policy edits move standalone and ancestor/final identities; identical controls remain stable. Pin definitions consulted only by unseated loaded agents and prove their byte changes move identity. Preserve unconsulted-definition stability. Additional requirement: [Active instructions and policy cannot escape bundle identity][MPI].
- [ ] 6.3 Extend `Engine::start_in_world` and direct runtime start paths to compare compiled operated-realm authority and reproducible input identities before `create_run`. Verify a changed grant or definition/dialect input refuses with capabilities named and creates neither run row nor effect; an identical context starts and a neighboring realm's grant is irrelevant. Requirement: [Realm context is resolved before capability authorization][RG5]. **Repair obligation:** H4: retain/check the validated active path and its existing file/library/compose byte pin at later prompt/start consumption; changed targets/bytes cannot be read under the compiled identity. Prove standalone and inherited charter/policy cases, including aliases, with exact refusal attribution.
- [ ] 6.4 Reconstruct resume context from pinned grants/source identity, not the current map, and require matching consulted definition/dialect/adapter bytes. Verify a map-only edit cannot add a grant, unmapped runs retain their operated root, missing/changed inputs get the existing manifest-mismatch refusal with capabilities named, and pre-capability manifests refuse without historical rewriting. Requirement: [Realm context is resolved before capability authorization][RG5]. **Repair obligation:** H4: repeat excluded and changed permitted active-input cases on resume; retain existing mismatch behavior and no historical rewrite. Preserve the unmapped-operated-root test with its workspace decoy removed.

## 7. Compose native controls at the final launch boundary

- [ ] 7.1 Consume managed Codex controls in the final `codex_cold` path after engine boundary assembly. Verify exact final argv for inline and agent-backed seats, boxed and unboxed, denied and held; denial includes no asks, lost wants, scope exclusion, subtraction, legacy realm versions and a serving fallback. Assert the measured `-c`, `web_search="disabled"` pair and intact model/effort/sandbox/hands/result controls; held cases use declared ON without OFF. Requirements: [Codex web-search OFF uses the controller's measured fragment][NC2]; [Realms v6 adds grants without changing frozen versions][RG1]. **Repair obligation:** H1/H2/H3: extend the final-command matrix to missing/broken denial metadata and authored-server refusal, retaining positive managed hands/ON controls. Account for work/gate, primary/fallback, panel/sequence, selected/inherited, inline/agent and both boundaries as D10 specifies. Whole argv equality retains duplicates and order.
- [ ] 7.2 Add the selected managed disposition to actual eligible Codex `exec resume` before session/stdin positionals without sending it through the authored passthrough guard. Extend protocol tests and `tests/inline_resume.rs` for inline/agent-backed held and denied unboxed resumes: assert command, session, stdin `-`, sandbox, effort, `rejoining` and no refusal. Preserve version/session/accounting/config eligibility and boxed ineligibility; separately verify denied boxed cold fallback, never count it as resume proof. Requirement: [Eligible Codex resumes reimpose the capability control][NC3]. **Repair obligation:** H1/H3: carry validated controls into pre-work cold replacement or propagate fallible re-decoding; remove `.ok()` defaulting in `codex_managed`. Prove final actual resume and cold replacement separately, and exact refusals of invalid engine plans before invocation.
- [ ] 7.3 Reject authored native-control conflicts at compile and final invocation, including supported split/equal search/config/feature aliases and duplicate controls; source/capability must appear in the full reason. Verify flags are parsed in their argument positions, unrelated values are not false conflicts, a matching-looking authored OFF pair is not managed authority, and arbitrary `-c` still triggers the existing resume guard. Requirement: [Neither inline arguments nor fallback can override native denial][NC4]. **Repair obligation:** H2: add an inventory-independent authored-server check on source arguments and at final assembly. Refuse Codex whole `mcp_servers` tables/descendants, admitted config/key spellings, Claude/LaneTally MCP config and allowed-tool aliases/wildcards/counterfeit hands; retain inert-value controls. Audit DSH's bound route-only `--patch`, including nested `compat`, preserving the valid route case and rechecking bound bytes before staging; compile-refuse server/tool/extra/unknown shapes or other unclassifiable equivalent transports. No credential payloads in diagnostics. Positive engine-owned hands proves provenance without a server-name exception. Additional requirement: [Authored provider configuration cannot supply capability authority][RGR].
- [ ] 7.4 Aggregate Claude native include/allow/deny contributions with existing hands/local policy once, emitting each list flag once and preserving strict MCP plus workspace access. Verify search-only excludes fetch, neither/both held, exact subsets, unboxed explicit denial, local permission preservation, split/equal aliases, and complete duplicate/arity/conflict refusals. Mark tests as composition evidence; do not invent live Claude, DSH or LaneTally denial. Requirement: [Other provider declarations preserve the commissioned uncertainty][NC5]. **Repair obligation:** H3: compose `Controls.argv`, `selection` and supported restriction transport with the shared fallible provider helper. Normalize list argv/aliases before one list merge; preserve non-list values, refuse contradictions/uncomposable forms during compilation, and prove search OFF argv plus fetch OFF selection, held ON and restrictions in final cold/eligible resumed Claude/LaneTally commands. DSH cannot silently discard a nonempty accepted plan. Additional requirement: [Every accepted native control reaches the final command][NCC].

## 8. Expose the same facts in prompts, scaffolds and doctor

- [x] 8.1 Render capabilities from the serving outcome in `render_prompt`: explicit empty or named holdings, unmet wants, subtractions, known native denials and unmeasured reasons. Put the DATA-only instruction beside capability use. Verify primary/fallback prompts, CQ1 restriction-drop explanation, and instruction-shaped test data cannot change the pinned authority or launch plan; no neighboring office/candidate grant is advertised. Requirements: [Prompt capability statements reflect the serving seat's pinned holdings][MP3]; [Capability responses are data and confer no authority][MP4].
- [x] 8.2 Update `brokkr-cli/src/init.rs::adapter_json` to emit the same Claude native assessment as the shipped adapter. Verify `init_stacks`, `init_doctor` and library-data coverage compile generated no-grant scaffolds, keep native tools denied and report evidence limits; the generated path must not bypass the capability outcome. Requirements: [Native capability controls are adapter-owned evidence-bearing data][NC1]; [Realms v6 adds grants without changing frozen versions][RG1].
- [ ] 8.3 Extend doctor's per-realm declaration report using shared context/validation helpers: capability, dialect, tools, restrictions and all/named/no offices. Verify multiple realms, absent map, definitions that grant nothing, malformed/missing/conflicting metadata, valid but inexpressible restrictions and MCP slice-two diagnosis; failures leave independent realm/native information visible and no capability server is started. Requirements: [Doctor reports grants for every realm][CD1]; [Doctor explains unbuilt bindings without running capabilities][CD4]. **Repair obligation:** M1: qualify the restriction-incompatibility paragraph with actual OFF feasibility; a dropped want cannot promise denial from unsupported/unmeasured controls. Complete report-line expectations agree with H1 admission.
- [ ] 8.4 Compare installed harness declarations against each realm binding/scope, distinguish supported OFF, measured impossible OFF and unknown inventory/control, and retain evidence limitations. Verify complete relevant lines with supplied installed/absent facts, same-name different-provider grants, partial office scope, no-map defaults and malformed-map unknown authority; Claude remains adapter evidence, Codex resume unmeasured, and DSH/LaneTally retain their own reasons even when agent loading fails. Requirements: [Installed native capabilities absent from grants are explicit][CD2]; [Unknown inventories and live-control gaps remain unmeasured][CD3]. **Repair obligation:** M1: judge OFF before the matching-grant branch. Supported/composable OFF reports declared denial; unsupported OFF reports compile refusal; unmeasured known OFF preserves its reason, claims no denial and reports H1 refusal. Cover partial scope, empty offices/tools, unused/no-ask/subtracted and absent grants; mutate the grant-first bypass independently.

## 9. Prove that removing enforcement breaks the intended assertions

- [ ] 9.1 Run independent reversible removal experiments for grant/scope enforcement, provider compatibility, CQ1 restriction compatibility, impossible OFF, optional notice recording and the MCP fence; remove grant/scope from positive input controls too. Verify each mutation reaches and fails its intended full diagnostic or exact notice assertion, including optional/unused MCP cases, then restore and rerun green. Record test name, mutation, intended assertion and observed fail/restore result in delivery evidence; compile errors and unrelated failures count as no proof. Requirements: [Refusal proofs assert the full reason][SC8]; [Compilation holds only the request and applicable grant intersection][SC3]; [Provider compatibility cannot expand a holding][SC4]; [Any MCP realm grant refuses until slice two][SC6]; [An impossible native denial is a compile refusal][SC5]. **Repair obligation:** M4: split provider and CQ1 restriction compatibility tests into required, optional and unused cases. For each optional test the first substantive assertion compares the complete notice vector. Remove compatibility itself with notice recording/OFF intact and otherwise valid fixtures; record the intended notice failure and restored pass separately for provider and restriction checks. Historical M3/M4 required failures, fallback holdings and M6/M8 mutations are insufficient.
- [ ] 9.2 Remove cold OFF and actual eligible resume OFF separately and verify the final-argv denial matrix fails at the missing exact pair; independently replace ON with unconditional OFF and verify cold/resume admission assertions fail. Include the synthetic CQ1 restricted-want OFF composition and exact notice as independent removal checks. Restore every mutation and rerun the matrix green, retaining actual resume eligibility and boxed fallback distinctions. Record evidence without claiming provider enforcement or adding a permanent mutation framework. Requirement: [Denial and admission have removal proofs and bounded live claims][NC6]. **Repair obligation:** H1/H2/H3: include each accepted Claude representation/restriction, cold replacement, malformed-plan refusal and authored/managed separation. Retain final ON controls so blanket OFF fails; use D10's full matrix rather than plan-object evidence.
- [ ] 9.3 Remove grant, consulted-definition digest, dialect digest and restriction identity contributions independently. Verify each corresponding single-axis digest assertion fails while identical-input controls remain meaningful; restore all contributions and rerun identity/start/resume tests green. Record observations and ensure no experimental mutation remains. Requirement: [Capability authorization participates in bundle identity][MP2]. **Repair obligation:** H4/M3: independently remove active-role and policy exclusion fences and the newly consulted unseated-definition identity contribution; exact refusals/digest assertions must detect each change. Restore before measuring witness pins.

- [ ] 9.4 For each H1–H4/M1–M4 repair and the fixture correction, append the named red reproduction, fix, isolated removal, intended observed failure and restored pass to evidence. Use the council's concrete examples and D10's final-launch matrix; preserve independently the earlier adapter-duplicate, sequence Codex-to-DSH fallback and unmapped-resume/no-decoy removals. Verify no mutation remains, without a permanent mutation framework. Requirements: [Refusal proofs assert the full reason][SC8]; [Denial and admission have removal proofs and bounded live claims][NC6]; [Active instructions and policy cannot escape bundle identity][MPI].

## 10. Document migration and measure final pins

- [ ] 10.1 Update focused realm/agent/provider guides for v6, abstract requests, subtraction, native denial, doctor, manifest migration and data-only results. Add delivery notes listing the controller's owed Codex resumed OFF/ON, explicit ON/other versions, native inventory/profile precedence/ambient MCP exclusion, Claude live controls, DSH inventory/controls and LaneTally wrapper controls; identify generic exec uncertainty where reported. Verify every claim against declaration/measurement evidence, mark live gaps unmeasured, and describe later-slice ownership without broker/gate/comparison claims. Requirements: [Denial and admission have removal proofs and bounded live claims][NC6]; [Slice-one records do not claim later-slice behavior][MP6]. **Repair obligation:** Describe the repair migrations: invalid/legacy required metadata refuses, authored server configuration is removed, excluded active files relocate, duplicate request documents/unseated-agent typos are fixed. Retain ambient-MCP and live-measurement gaps; no claim of security-hold closure before council.
- [ ] 10.2 After final code, adapter, agent and charter bytes settle, actually compile every bundle covered by `tests/witness_digests.rs` and `bundle/compose_tests.rs`, including inherited bundles, `bundles/self` and `bundles/verify`; measure affected charter bytes for library witnesses. Re-pin only observed values and append 0065 slice-one history reasons for each movement. Verify the witness/compose/library tests pass against the measured outputs, leaving prior history intact. Requirement: [Digest pins are measured and their history remains truthful][MP5]. **Repair obligation:** H4: remeasure after the repaired code and inputs settle; append a concrete reason for each changed witness/compose digest, or record observed unchanged values. Prior compiles and expected values are not measurements of the repaired head.
- [ ] 10.3 Verify repository `realms.json` remains the existing v3 no-grant map, shipped researcher produces both wanted-drop notices with native OFF, and generated/inline/inherited bundles all have explicit capability identity. Review the scoped diff for protected/frozen paths and accidental later-slice implementation; fix any deviation and remeasure pins only if inputs changed. Requirements: [Realms v6 adds grants without changing frozen versions][RG1]; [Legacy concrete permissions cannot grandfather a capability][SC7]; [Slice-one records do not claim later-slice behavior][MP6]. **Repair obligation:** Repeat the protected-surface and scope review against the final repair diff; earlier observations do not certify later edits.

## 11. Complete the delivery checks and evidence

- [ ] 11.1 Run `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`; resolve failures and record actual exit results. Verify the dependency-edge change keeps the locked package versions, required compiler compatibility and license constraints. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation). **Repair obligation:** Repeat on the final repair head and record that revision; earlier green results are historical.
- [ ] 11.2 Run each crate suite explicitly with `cargo test -p <crate> --all-features --locked` for `brokkr-core`, `brokkr-store`, `brokkr-protocol`, `brokkr-runtime`, `brokkr-view`, `brokkr-bridge` and `brokkr-cli`, then `cargo test --workspace` and `cargo test --workspace --all-features --locked`. Verify every suite is green and record each result; unavailable commands or skipped boundary execution are not passes. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation). **Repair obligation:** Repeat on the final repair head, including the macOS fixture result only when observed; do not count Linux as macOS evidence or fix issue #255 here.
- [ ] 11.3 Run `cargo run --locked -p brokkr-cli -- compile --bundle bundles/self` and the equivalent `bundles/verify` compile plus any remaining task-10.2 compile; run `openspec validate --all --strict --no-interactive` and `git diff --check`. Verify final compiles match their pins and OpenSpec is green. Record actual evidence, including the controller measurement gaps, without writing instructions or exceptions into a gate. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).
- [ ] 11.4 Obtain candidate-bound `bash scripts/coverage-exact.sh` evidence on host/CI outside the nested workspace box, using the existing pinned compiler and unchanged literal-100% gate. Verify every added production line is hit and all existing exact line/branch/function checks pass; preserve the report and source revision, and state source-line, branch and logical-function covered/total counts separately, each nonzero and exactly equal. Attempt the authorized script on the final head; if the box cannot run it, the external result remains pending. An unavailable external result stays pending with this task unchecked. Do not lower thresholds, exclude production lines or call another passing check coverage evidence. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).

## 12. Council re-judgment before any fold

- [ ] 12.1 **Held open by the operator's repair commission. Do not archive or fold this change.** Local validation and completed repair proofs do not lift the security hold; council re-judgment is pending. The old instruction to archive immediately after task 11 is superseded. Commit completed artifacts in repository style and never push, without checking this box. Any later authorized archive/fold retains the dialect's append-only provenance rule and archived validation, but it is outside this repair visit. Requirements: [Slice-one records do not claim later-slice behavior][MP6]; [Digest pins are measured and their history remains truthful][MP5]; operator's explicit no-archive ruling.

[TD1]: specs/tool-dialect-contract/spec.md#requirement-a-tool-dialect-binds-one-capability-to-exactly-one-implementation-kind
[TD2]: specs/tool-dialect-contract/spec.md#requirement-capability-classes-belong-to-the-abstraction
[TD3]: specs/tool-dialect-contract/spec.md#requirement-dialects-describe-their-disclosure-using-the-existing-egress-vocabulary
[TD4]: specs/tool-dialect-contract/spec.md#requirement-the-mcp-contract-is-whole-before-its-runtime-exists
[TD5]: specs/tool-dialect-contract/spec.md#requirement-restriction-keys-are-defined-by-the-selected-dialect
[TD6]: specs/tool-dialect-contract/spec.md#requirement-reserved-hands-preserves-the-existing-workspace-authority
[RG1]: specs/realm-capability-grants/spec.md#requirement-realms-v6-adds-grants-without-changing-frozen-versions
[RG2]: specs/realm-capability-grants/spec.md#requirement-only-an-operators-realm-selects-concrete-implementations
[RG3]: specs/realm-capability-grants/spec.md#requirement-office-scopes-and-tool-subsets-only-narrow-a-grant
[RG4]: specs/realm-capability-grants/spec.md#requirement-restrictions-are-validated-carried-and-pinned-without-engine-interpretation
[RG5]: specs/realm-capability-grants/spec.md#requirement-realm-context-is-resolved-before-capability-authorization
[SC1]: specs/seat-capability-resolution/spec.md#requirement-requests-use-abstract-names-and-a-closed-strength-vocabulary
[SC2]: specs/seat-capability-resolution/spec.md#requirement-a-seat-can-subtract-but-cannot-widen-its-office
[SC3]: specs/seat-capability-resolution/spec.md#requirement-compilation-holds-only-the-request-and-applicable-grant-intersection
[SC4]: specs/seat-capability-resolution/spec.md#requirement-provider-compatibility-cannot-expand-a-holding
[SC5]: specs/seat-capability-resolution/spec.md#requirement-an-impossible-native-denial-is-a-compile-refusal
[SC6]: specs/seat-capability-resolution/spec.md#requirement-any-mcp-realm-grant-refuses-until-slice-two
[SC7]: specs/seat-capability-resolution/spec.md#requirement-legacy-concrete-permissions-cannot-grandfather-a-capability
[SC8]: specs/seat-capability-resolution/spec.md#requirement-refusal-proofs-assert-the-full-reason
[NC1]: specs/native-capability-controls/spec.md#requirement-native-capability-controls-are-adapter-owned-evidence-bearing-data
[NC2]: specs/native-capability-controls/spec.md#requirement-codex-web-search-off-uses-the-controllers-measured-fragment
[NC3]: specs/native-capability-controls/spec.md#requirement-eligible-codex-resumes-reimpose-the-capability-control
[NC4]: specs/native-capability-controls/spec.md#requirement-neither-inline-arguments-nor-fallback-can-override-native-denial
[NC5]: specs/native-capability-controls/spec.md#requirement-other-provider-declarations-preserve-the-commissioned-uncertainty
[NC6]: specs/native-capability-controls/spec.md#requirement-denial-and-admission-have-removal-proofs-and-bounded-live-claims
[CD1]: specs/capability-doctor/spec.md#requirement-doctor-reports-grants-for-every-realm
[CD2]: specs/capability-doctor/spec.md#requirement-installed-native-capabilities-absent-from-grants-are-explicit
[CD3]: specs/capability-doctor/spec.md#requirement-unknown-inventories-and-live-control-gaps-remain-unmeasured
[CD4]: specs/capability-doctor/spec.md#requirement-doctor-explains-unbuilt-bindings-without-running-capabilities
[MP1]: specs/capability-manifest-and-prompts/spec.md#requirement-a-new-manifest-version-records-capabilities-per-executable-seat
[MP2]: specs/capability-manifest-and-prompts/spec.md#requirement-capability-authorization-participates-in-bundle-identity
[MP3]: specs/capability-manifest-and-prompts/spec.md#requirement-prompt-capability-statements-reflect-the-serving-seats-pinned-holdings
[MP4]: specs/capability-manifest-and-prompts/spec.md#requirement-capability-responses-are-data-and-confer-no-authority
[MP5]: specs/capability-manifest-and-prompts/spec.md#requirement-digest-pins-are-measured-and-their-history-remains-truthful
[MP6]: specs/capability-manifest-and-prompts/spec.md#requirement-slice-one-records-do-not-claim-later-slice-behavior

[NCR]: specs/native-capability-controls/spec.md#requirement-known-native-powers-require-a-valid-delivered-denial-or-refusal
[NCC]: specs/native-capability-controls/spec.md#requirement-every-accepted-native-control-reaches-the-final-command
[RGR]: specs/realm-capability-grants/spec.md#requirement-authored-provider-configuration-cannot-supply-capability-authority
[MPI]: specs/capability-manifest-and-prompts/spec.md#requirement-active-instructions-and-policy-cannot-escape-bundle-identity
