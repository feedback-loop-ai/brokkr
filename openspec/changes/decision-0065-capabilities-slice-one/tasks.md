# Decision 0065, slice one — security-hold repair tasks

Adopt every commit on `slice-0065-capabilities`, including implementation
head `f0264a9b`, specification return `5c53a30f` and design return `6d47c120`.
The [proposal](proposal.md), six [spec deltas](specs/) and
[design Decisions D3–D10](design.md#decisions) govern this breakdown.
The [chief's ruling](../../../.forge/tasks/council-ruling-3c72a18a.md) is the
finding source; this journal supplies no separate `returned_from` field.
H4 remains HIGH with `spec_defect=true`. D7 has already withdrawn its unsafe
active-input exception, so no earlier artifact must change before these tasks
can be executed. The separate repair decision remains task 0.1, before code.

Scope is H1–H4, M1–M4 and the commissioned macOS fixture correction. Checked
tasks below are adopted work, not new proof or instructions to rebuild the
slice. Open tasks replace the design return's combined repair obligations.
Historical 40-of-43 status and measurements remain in [evidence](evidence.md);
neither they nor checked foundational tasks close a finding on the repair head.
No pending implementation task is checked by this tasks visit.

Execute open tasks in numbered order. Task 0.2 establishes red cases before
shared repairs can mask them. Group 3 repairs readers, fixture roots and
captures argument origin; group 4 supplies shared composition and compile
admission; group 5 carries the result through dispatch. Groups 6–8 integrate
identity, final launches and reporting. Group 9 records independent removal
proofs after integrated fixes. Within a finding: red at the intended assertion,
fix, isolated enforcement removal, intended failure, restore, green.
A shared helper passing is not final-command proof. Tick a task only when its
stated artifact/test obligation is observed; task 9.4 closes each finding's
complete evidence chain. Do not mark the repair complete while a required
proof or gate remains open. Archive task 12.1 is deliberately held separately.

Tests use whole diagnostic/notice/report-line equality, complete ordered final
argv (including duplicate detection), or specified identity comparisons.
`is_err()`, substring/status-only checks, build errors and unrelated setup
refusals are not commissioned proof. Use raw JSON for duplicate cases and
otherwise valid metadata/denial fixtures so later hardening cannot mask them.
No permanent mutation framework; restore every temporary mutation.

Production stays Rust under `crates/`. Do not edit existing contract versions,
`policy/phase-machine.json`, `policy/schemas/`, `fixtures/`, `reference/`,
`extensions/`, the event envelope or the issue-226 task ledger. Test inputs
belong in temporary trees or owning suites. Keep the repository's v3 realm
without grants. No dependency upgrade, release work, push, issue-255/PR-313
flake repair or later-slice implementation. MCP grants still compile-refuse;
hands/boundaries retain 0043/0046 behavior. Only Linux and macOS are hosts.

## 0. Establish the decision and red reproductions

- [ ] 0.1 Add the next available numbered repair decision under `docs/decisions/` with status `proposed`, leaving accepted 0065 unchanged. Record D3–D10 choices, rejected alternatives, H4's specification correction, migration and concrete enforcement/test bindings for all eight findings and the fixture. Check agreement with the proposal/specs/design before code; only the operator accepts a decision. Requirements: [Known native powers require a valid delivered denial or refusal][NCR]; [Authored provider configuration cannot supply capability authority][RGR]; [Every accepted native control reaches the final command][NCC]; [Active instructions and policy cannot escape bundle identity][MPI]; house proposed-decision rule.
- [ ] 0.2 Add and run named regressions for H1–H4, M1–M3 and the macOS alias-root failure against delivered behavior before changing enforcement. Reuse the council's exact reproductions and the observable rows below; record each intended assertion and actual red result in evidence, including source revision. Establish otherwise-valid controls first. For M4 the missing deliverable is an independent removal proof: isolate tests in 4.4 and observe the two compatibility-removal failures in 9.1, without inventing an unmutated behavior defect. Requirements: [Refusal proofs assert the full reason][SC8]; [Denial and admission have removal proofs and bounded live claims][NC6]; [Active instructions and policy cannot escape bundle identity][MPI]; [Provider compatibility cannot expand a holding][SC4].

This reproduction/closure index requires actual test names and red/removal/
restored observations in 9.4; it records no passing result.

| Finding | First failing observable / owning suite | Repair and closure tasks |
| --- | --- | --- |
| H1 | No-ask/no-grant inline Codex with absent, legacy or failed adapter load: full refusal or complete final OFF command; runtime bundle/capability launch, protocol native controls | 3.3, 4.5, 5.2, 7.1–7.2, 7.5, 9.2, 9.4 |
| H2 | Panel's `mcp_servers.ungranted.command`/args and Claude config plus `mcp__ungranted__fetch` get realm-only refusal; protocol/runtime launch suites | 3.8, 4.6, 5.1–5.2, 7.3, 7.5, 9.2, 9.4 |
| H3 | Claude argv search OFF plus selection fetch OFF, and held synthetic restrictions, survive final argv; protocol adapters/runtime launch suites | 3.3, 4.3–4.4, 5.1–5.2, 7.4–7.5, 9.2, 9.4 |
| H4 | Standalone charter, standalone policy, ancestor charter, ancestor policy each refuse under excluded trees; bundle/compose and engine/verb identity suites | 6.1–6.4, 9.3–9.4, 10.2 |
| M1 | Matching scoped/empty/unused grants with unsupported/unmeasured OFF cannot print denial; CLI doctor capability tests | 8.3–8.4, 9.4 |
| M2 | Raw duplicate request name or outer `capabilities` key refuses with source/key/location; bundle/compose/agent suites | 3.1, 9.4 |
| M3 | Valid seated worker plus undefined unseated loaded-agent request yields the complete agent-lint error; bundle agent/compose suites | 3.7, 6.2, 9.3–9.4 |
| M4 | Independent wants-only notice equalities fail on provider and restriction compatibility removals; runtime capabilities tests | 4.4, 9.1, 9.4 |
| macOS | Original repeated-native-key/uncomposable-selection equality fails under aliased fixture root; runtime agents tests | 3.6, 9.4, 11.2 |

## 1. Retain the adopted additive contracts

- [x] 1.1 Add `contracts/tool-dialect.v1.schema.json` with D3's closed discriminated shape, identity/serves/tools, provider-native binding, reserved workspace hands, abstract-class equality annotation and separate disclosure metadata. Verify positive native/hands examples and schema rejection of unknown/mixed kinds, missing binding fields, invalid classes and egress vocabulary. Requirements: [A tool dialect binds one capability to exactly one implementation kind][TD1]; [Dialects describe their disclosure using the existing egress vocabulary][TD3]; [Reserved hands preserves the existing workspace authority][TD6].
- [x] 1.2 Complete that new tool-dialect contract's MCP branch with exactly one stdio argv or URL connection, named tools, pinned version, 0012 binding names and optional retention declaration; define the embedded Draft 7 restriction schema field. Verify both connection forms as data and schema rejection of mixed/incomplete forms and literal credential fields, without server/network access. Task 2.4 completes semantic binding/reference checks. Requirements: [The MCP contract is whole before its runtime exists][TD4]; [Restriction keys are defined by the selected dialect][TD5].
- [x] 1.3 Add `contracts/realms.v6.schema.json`, preserving v5 fields and admitting the per-realm grants map with optional tool/office lists and dialect-owned restriction values. Verify valid omission/empty maps and lists, invalid nulls/types/duplicates, and schema refusal of `capabilities` under every legacy version. Requirement: [Realms v6 adds grants without changing frozen versions][RG1].
- [x] 1.4 Add `contracts/run-manifest.v11.schema.json` with D7's required, structured capability section, explicit empty outcomes, per-candidate records, relative sources/digests and inactive grant context. Register all three new contracts additively in `crates/brokkr-runtime/tests/frozen_contracts.rs`; verify hand-built positive/negative v11 examples and unchanged hashes for every existing version. Requirement: [A new manifest version records capabilities per executable seat][MP1].

## 2. Retain operator authority loading and explicit context

- [x] 2.1 Extend `brokkr-core/src/realms.rs` and runtime realm representation for v6 grants, preserving omitted versus explicit tool/scope lists. Check field presence before defaults so v1–v5 reject even `capabilities: {}`. Verify core realm tests for all six versions, absent map/field, empty maps, malformed data and unchanged hands/boundary fields. Requirements: [Realms v6 adds grants without changing frozen versions][RG1]; [Office scopes and tool subsets only narrow a grant][RG3].
- [x] 2.2 Introduce D2's explicit `CapabilityContext` and thread it through CLI compile/run/rerun, runtime compile entry points and semantic library lint. Resolve definition/dialect roots beside the active realm map, otherwise under the operated repository; convenience APIs must construct an explicit no-grant context. Verify mapped/unmapped and neighboring-realm context tests, and that recipe roots and agent-library overrides cannot replace operator authority. Task 4 connects authorization to every entry point. Requirements: [Realm context is resolved before capability authorization][RG5]; [Only an operator's realm selects concrete implementations][RG2].
- [x] 2.3 Implement the strict abstract-definition loader in a focused runtime `capabilities` module: safe names, contained paths including symlinks, filename/name equality, exactly name/classes, nonempty unique classes and deterministic duplicate/conflict rejection. Read/hash the same bytes and validate every definition in the root. Verify CQ2's valid operator-defined name, missing roots, absent/conflicting metadata, recipe-local impersonation and class-order equality with exact owning diagnostics. Requirement: [Capability classes belong to the abstraction][TD2].
- [x] 2.4 Implement strict selected-dialect loading and restriction validation, including containment, duplicate-key rejection in new authority data, serves/class agreement and same-buffer SHA-256. Promote the already locked `jsonschema` workspace dependency from runtime dev-only to production with default features disabled, for complete operator-schema validation; permit only local embedded Draft 7 references, reject external retrieval and reserved-key redefinition through references/composition. Verify valid/invalid schemas, unchanged values/array order, missing/escaping references and schema/loader agreement. Check MCP undeclared secret references and URL userinfo without echoing credentials or opening a secret store. Confirm no package/version upgrade and record the dependency-edge justification. Requirements: [A tool dialect binds one capability to exactly one implementation kind][TD1]; [Restriction keys are defined by the selected dialect][TD5]; [Only an operator's realm selects concrete implementations][RG2]; [The MCP contract is whole before its runtime exists][TD4].

## 3. Repair source readers and preserve composition inputs

- [ ] 3.1 **M2:** Strictly parse each original recipe layer in `bundle/compose.rs::read_layers` and agent source in `agents/load.rs::read_json` with `parse_strict` before map conversion. Refuse duplicate capability names and outer `capabilities` fields, both strength orders, equal repetitions and a later `{}`. Cover direct, nested panel, sequence, selected-case and inherited/overridden layers with raw JSON; assert full source/key/location diagnostics and valid inheritance/subtraction controls. Keep errors at the original source. Independently remove each reader's strict parsing, observe its intended equality fail, restore and pass. Requirements: [Requests use abstract names and a closed strength vocabulary][SC1]; [Refusal proofs assert the full reason][SC8].
- [x] 3.2 Preserve omission versus explicit request maps: agent-backed omission inherits, an explicit unchanged-strength subset subtracts, `{}` removes all, and inline maps supply office asks. Verify addition/strength-change refusals and inherited required subtraction across ordinary, panel, sequence, selected and composed bodies; retain both stable office and execution-site identities. Requirement: [A seat can subtract but cannot widen its office][SC2].
- [ ] 3.3 **H1/H3:** Retain strict adapter declaration validation and make `native_controls::managed` decode engine plans fallibly. Reject wrong types, malformed arrays/guards, missing selection mappings, missing known obligations and unmeasured required OFF instead of `filter_map`/default-empty recovery. Carry provider/disposition readiness through `Outcome::controls`; declared measured default ON stays distinct from missing control. Keep the by-hand interface separate from engine dispatch. Add exact malformed-plan refusals and valid argv/selection/default controls in protocol/native tests; do not change frozen wire schemas. Requirements: [Native capability controls are adapter-owned evidence-bearing data][NC1]; [Known native powers require a valid delivered denial or refusal][NCR]; [Every accepted native control reaches the final command][NCC].
- [x] 3.4 Ship operator `capabilities/web-search.json` and `web-fetch.json` as reads/egress definitions and only the three provider-native tool dialects in D9: Codex search, Claude search and Claude fetch. Add Codex's measured cold-default ON and exact OFF pair, Claude's declared search/fetch selection controls, and DSH/LaneTally's explicit commissioned unmeasured reasons; describe generic exec's unknown child inventory honestly. Shipped dialect restriction schemas admit only empty objects until a control exists. Verify library-data tests load each binding, tools/keys agree, evidence is scoped, and repository realms still grant nothing. Requirements: [Capability classes belong to the abstraction][TD2]; [Codex web-search OFF uses the controller's measured fragment][NC2]; [Other provider declarations preserve the commissioned uncertainty][NC5].
- [x] 3.5 Migrate `agents/researcher.json` to abstract web-search/web-fetch wants, retain local command restrictions and remove concrete web aliases; add the DATA-only sentence beside capability use in its charter and any other affected capability-using charter. Refuse nonempty legacy `tools.mcp`, even optional entries, and native-owned legacy allow aliases with full migration reasons; keep empty MCP lists valid. Verify strict library and charter tests, local restrictions unchanged and declarations contain no implementation choices. Requirements: [Legacy concrete permissions cannot grandfather a capability][SC7]; [Capability responses are data and confer no authority][MP4].
- [ ] 3.6 **macOS:** Repair `agents/tests::Tree::new` to retain its `TempDir` guard and one canonical root; derive all writes and complete expected paths from that root. Keep `a_native_declaration_with_a_repeated_key_or_an_uncomposable_selection_is_refused`'s full diagnostic equality. Sweep tests added by the original slice and this repair for the same lexical-root habit, recording checked helpers. Use an alias-root reproduction on a supported host, restore lexical behavior to expose the intended mismatch, then restore and pass; actual Linux/macOS runs are separately recorded in 11.2. Never glue a temp prefix or canonicalize only the expectation. Requirement: [Refusal proofs assert the full reason][SC8] (scenario: macOS canonical fixture roots preserve exact diagnostics).
- [ ] 3.7 **M3:** Immediately after `Bundle::assemble` actually loads a library, call existing `authority.definitions.lint(&library)` before seat resolution/subtraction; return deterministic `CompileError::Capability` agent diagnostics. Resolve every loaded agent's requires/wants, including unseated and later-subtracted asks, without loading otherwise unused libraries. Add those definition names to the manifest's consulted set. Prove the exact CQ2 agent-lint error with a valid seated worker plus invalid unseated researcher, direct/composed cases, valid-library control and retained CLI lint. Remove only the compile invocation, observe its named regression fail, restore and pass. Definition identity proof follows in 6.2. Requirements: [Capability classes belong to the abstraction][TD2]; [Requests use abstract names and a closed strength vocabulary][SC1]; [Capability authorization participates in bundle identity][MP2].
- [ ] 3.8 **H2 prerequisite:** In `agents::compose` and composed/chain/candidate data, capture the base driver/model/effort/local-policy vector separately when the engine hands fragment is appended. Inline vectors remain wholly authored. Preserve origin through candidate construction; do not reconstruct it by matching/deleting strings or recognizing a server name. Test identical-looking authored and engine fragments retain distinct origin while existing hands/model commands remain unchanged. This supplies group 4 admission and group 5 dispatch; it does not itself prove H2 closed. Requirements: [Authored provider configuration cannot supply capability authority][RGR]; [Reserved hands preserves the existing workspace authority][TD6].

## 4. Repair shared control composition and compiler admission

- [x] 4.1 Validate every selected-realm grant before seat compatibility: definition/dialect existence, serves/classes, tool subset, offices and restriction schema. Then refuse MCP grants with the complete slice-two diagnostic even for wants, no requests or `offices: []`, and refuse reserved hands with the 0043/0046 reason. Verify exact diagnostics and positive native controls without server launch; invalid data cannot become an optional drop. Requirements: [Any MCP realm grant refuses until slice two][SC6]; [Reserved hands preserves the existing workspace authority][TD6]; [Restrictions are validated, carried and pinned without engine interpretation][RG4].
- [x] 4.2 Implement the pure office-asks-minus-subtractions intersection with scoped realm grants. Resolve omitted versus empty scopes/tools exactly; produce named required refusals and deterministic complete wanted-drop notices, never enable an unused grant. Verify missing grant, wrong office, no tools, subtracted and no-ask cases from positive examples, including the exact D4 missing-grant reason. Requirements: [Compilation holds only the request and applicable grant intersection][SC3]; [Office scopes and tool subsets only narrow a grant][RG3].
- [ ] 4.3 **H3:** Implement shared fallible `compose_for_provider` in protocol native controls and call it during runtime candidate admission using 3.8's separate fragments. Consume every accepted argv, selection and restriction representation. For Claude list argv/aliases, normalize into one include/allow/deny merge with hands/local policy; preserve non-list values, check arity/duplicates/contradictions and exact subsets. Compile-refuse unsupported provider/form combinations, including a DSH nonempty plan it cannot consume, with the whole site/realm/capability/provider/form cause. Preserve required incompatibility and candidate-specific wants behavior, whole-chain required refusals and authorized ON controls. Unit/admission tests prove accepted forms; final argv integration is 7.4. Requirements: [Every accepted native control reaches the final command][NCC]; [Provider compatibility cannot expand a holding][SC4]; [Other provider declarations preserve the commissioned uncertainty][NC5].
- [ ] 4.4 **H3/M4:** Integrate supported restriction transport with 4.3, retaining the structured value and its canonical JSON in one typed argument; retain CQ1's invalid-grant, scope, requires/wants/unused precedence and valid independent OFF. Split `provider_compatibility_cannot_expand_a_holding` and `cq1_an_inexpressible_restriction_refuses_a_requirement_drops_a_want_and_idles_unused` into independently runnable required, optional and unused controls before compatibility-removal experiments. Each optional test's first substantive assertion compares the whole notice vector, with no required case or absent-element indexing ahead of it. Preserve exact CQ1 reasons, no invented unused notice/ON, and genuinely supported synthetic transport for 7.4. Run the split tests green; task 9.1 supplies independent red/restored proof, not an earlier required failure. Requirements: [Restrictions are validated, carried and pinned without engine interpretation][RG4]; [Provider compatibility cannot expand a holding][SC4]; [Every accepted native control reaches the final command][NCC].
- [ ] 4.5 **H1:** Replace security-relevant best-effort adapter loading with a preserved `Result` shared by pin and capability resolution. Separate optional effort exemptions from mandatory denial; audit suppressed errors affecting authorization, including `load_pin_adapters`, `site_capabilities`, `native_plan` and retry consumers. Apply D4's known-power floor (Codex search; Claude search/fetch), plus declared powers, without embedded switches or borrowed DSH/LaneTally inventories. Refuse absent roots/providers, legacy/omitted/empty metadata, omitted known powers, malformed/unreadable or unrelated broken adapter files and unmeasured known OFF when no valid denial exists. Assert whole source/load causes with site/office/realm/provider/capability for no-ask/no-grant inline work, no map, v1–v5 and v6 empty/omitted grants, absent definitions, scope, subtraction and optional drops; positive valid metadata delivers OFF. Use 4.3 to admit only composable plans through every compile entry point. Requirements: [Known native powers require a valid delivered denial or refusal][NCR]; [An impossible native denial is a compile refusal][SC5]; [Realms v6 adds grants without changing frozen versions][RG1]; [Other provider declarations preserve the commissioned uncertainty][NC5].
- [ ] 4.6 **H2:** Add the inventory-independent authored-configuration guard to the shared composer and compile admission using 3.8 provenance. Refuse Codex whole `mcp_servers` tables/descendants and admitted `-c`/`--config` split/equal/key spellings; Claude/LaneTally MCP config, allowed-tool/selector aliases, `mcp__*`, wildcard and counterfeit-hands admissions. Audit other accepted settings/config passthroughs and explicitly refuse equivalent transports that cannot be classified safely. Audit DSH `route_overlay::claim`/`validate`, including nested `compat`, preserving the bound contained digest-matching route-only overlay and model/effort/transcript/sandbox behavior; refuse server/tool/extra/unknown/unreadable shapes before staging. Exact diagnostics identify source/site/provider/control/realm-only cause without payloads or credentials. Include inert-value and legitimate hands/route controls. Final assembly and changed-byte checks follow in 7.3. Requirements: [Authored provider configuration cannot supply capability authority][RGR]; [Only an operator's realm selects concrete implementations][RG2]; [Reserved hands preserves the existing workspace authority][TD6].

## 5. Carry one candidate's authority and origin through dispatch

- [ ] 5.1 **H1–H3:** Carry the resolver outcome and separate base/managed vectors through `SiteFacts`, each `Candidate`, `Engine::compose`/`compose_site` and `SiteSpawn`. Preserve office and the entire family during wrapper relocation; generated helpers receive their own explicit outcomes. Test ordinary, panel, sequence, selected, inherited and nested sites, primary/fallback selection, and existing collision refusals. Retain subtraction and candidate-specific notices/holdings; never union controls or recover origin from matching text. Requirements: [A seat can subtract but cannot widen its office][SC2]; [A new manifest version records capabilities per executable seat][MP1]; [Neither inline arguments nor fallback can override native denial][NC4]; [Authored provider configuration cannot supply capability authority][RGR].
- [ ] 5.2 **H1–H3:** Expand hands placeholders only in the engine-owned fragment, then write private `launch_arguments` plus selected `native_controls` after user/context input merging in every ordinary/composite dispatch. At the driver boundary validate expanded vectors against actual extra argv; use 3.3's fallible decoder and 4.3/4.6's composer. Missing/null/malformed engine authority, unmeasured required OFF or absent/mismatched provenance refuses before provider work; it cannot fall into the by-hand interface. Verify fallback replaces both plan and fragments, and context/results/instruction-shaped data cannot overwrite either. Preserve the sequence Codex-primary/DSH-fallback regression; final command proofs follow in group 7. Requirements: [Known native powers require a valid delivered denial or refusal][NCR]; [Authored provider configuration cannot supply capability authority][RGR]; [Neither inline arguments nor fallback can override native denial][NC4]; [Capability responses are data and confer no authority][MP4].

## 6. Repair active-input identity before remeasuring pins

- [ ] 6.1 **H4 — specification correction adopted:** Replace the former completion basis with D7's enforced refusal. Share `unpinned_top_level` with an active-input validator using each declaring layer's canonical root. Call it for `parse_role` with actual site/case origin and every layer's `compose::own_table` before policy read/merge; a leaf override cannot hide an ancestor read. Check normalized lexical and canonical targets (`./`, `..`, absolute aliases, file/parent symlinks in both directions). External active inputs require their existing independent pin or refuse; preserve pinned agent/dialect inputs and the script fence. Prove four separate excluded-input cases: standalone charter, standalone valid policy, ancestor charter, ancestor valid policy; each refuses before and after its independent edit with full source/layer/site/kind/path/identity cause. Relocation controls compile. Independently remove role and policy fences, observe exact assertions fail, restore and pass. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [A new manifest version records capabilities per executable seat][MP1].
- [ ] 6.2 **H4/M3:** Retain v11 projection and capability identity through `bundle_manifest_from_run` and instance identity, including candidate plans and definitions consulted only by loaded unseated agents (3.7). Prove standalone and ancestor/final digests each move when permitted charter bytes or independently valid policy bytes change; identical controls stay stable. A consulted unseated-definition byte edit moves identity, while genuinely unconsulted definitions do not. Rerun existing independent grant/scope/tools/restriction/unused-grant/definition/dialect/native-declaration axes and dispatch-v2 unsupported-key refusal before a run row. No new identity inventory, expanded paths or secrets. Requirements: [Capability authorization participates in bundle identity][MP2]; [Active instructions and policy cannot escape bundle identity][MPI]; [A new manifest version records capabilities per executable seat][MP1].
- [ ] 6.3 **H4:** Carry validated active paths and existing file/library/compose pins into later prompt/start consumption. Refuse changed bytes or symlink targets instead of `render_prompt` reading fresh instructions under old identity. Retain compiled policy values and the operated-realm/start fence before `create_run`. Prove exact standalone/inherited charter/policy and alias refusals, matching-input success and no run row/seat effect on mismatch; a neighboring realm still supplies no authority. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [Realm context is resolved before capability authorization][RG5].
- [ ] 6.4 **H4:** Exercise excluded/newly excluded and changed permitted active inputs through resume's pinned source context and manifest-mismatch boundary. Preserve pinned grants despite map edits, missing/changed definition/dialect/adapter refusal, old-manifest refusal and historical journals. Rerun unmapped resume with only the operated repository able to reproduce authority: remove the workspace decoy as the existing regression requires. Assert complete mismatch causes and no fresh unpinned launch. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [Realm context is resolved before capability authorization][RG5]; [Refusal proofs assert the full reason][SC8].

## 7. Deliver controls at the final provider command

- [ ] 7.1 **H1:** Integrate validated composer output into `codex_cold` after boundary/hands assembly. Assert complete final argv for denied and held inline/agent-backed, boxed/unboxed seats: OFF is exactly `-c`, `web_search="disabled"`; declared ON has no conflicting OFF. Cover no asks, dropped wants, scope, subtraction, unused grants and legacy realms. Preserve model/effort/sandbox/hands/result controls and verify missing/broken metadata cases refuse before launch. This is composition proof scoped to the declared controller measurement. Requirements: [Codex web-search OFF uses the controller's measured fragment][NC2]; [Known native powers require a valid delivered denial or refusal][NCR]; [Realms v6 adds grants without changing frozen versions][RG1].
- [ ] 7.2 **H1/H3:** Use selected validated controls in actual eligible unboxed Codex `exec resume`, outside the authored passthrough allowlist and before session/stdin positionals. Carry those controls into pre-work cold replacement or propagate `codex_managed` decoding errors; eliminate its `.ok()`/empty fallback. Assert complete held/denied resume argv, offered session, stdin `-`, sandbox/effort, `rejoining` and no refusal; separately prove cold replacement and malformed-plan refusal. Retain arbitrary-config, version/session/accounting/boundary eligibility and exact boxed/gate ineligibility with denied cold fallback where applicable. No eligibility expansion or cold fallback counted as resume. Requirements: [Eligible Codex resumes reimpose the capability control][NC3]; [Known native powers require a valid delivered denial or refusal][NCR]; [Denial and admission have removal proofs and bounded live claims][NC6].
- [ ] 7.3 **H2:** Reapply 4.6's origin-aware guard during final provider assembly on every admitted adapter path. Prove exact authored-server/native-conflict refusals for split/equal aliases, whole tables, wildcards, counterfeit hands and nested/fallback inputs after argument expansion. Verify expanded authored/managed vectors agree; do not exempt matching-looking OFF or hands bytes. DSH must revalidate bound patch bytes before staging and refuse changed/unreadable/unknown overlays; retain the valid bound route. Exact final positive commands preserve engine hands under empty grants, strict configuration and local permissions; inert value positions stay inert. Where hooks exist assert no provider/server launch or staging on refusal. Requirements: [Authored provider configuration cannot supply capability authority][RGR]; [Neither inline arguments nor fallback can override native denial][NC4]; [Reserved hands preserves the existing workspace authority][TD6].
- [ ] 7.4 **H3:** Make `claude_launch` and the shared Claude/LaneTally cold/eligible-resume builder consume 4.3's complete result, including argv and restriction transports. Prove the council's argv WebSearch OFF plus selection WebFetch OFF becomes one effective deny list, both/neither/search-only holdings, authorized ON, exact subsets and supported synthetic nonempty restriction's exact encoded value. Preserve hands, strict MCP and local restrictions with each list flag once. Full compile-refusal tests cover uncomposable forms; successful transport assertions reach final argv rather than substitute an unsupported-form refusal. DSH cannot discard a nonempty accepted representation. Label composition evidence, leaving wrapper/live enforcement uncertainty intact. Requirements: [Every accepted native control reaches the final command][NCC]; [Other provider declarations preserve the commissioned uncertainty][NC5]; [Restrictions are validated, carried and pinned without engine interpretation][RG4].
- [ ] 7.5 **H1–H3 integration matrix:** Extend `runtime/tests/capability_launch.rs`, engine capability tests, protocol adapter/native-control tests and `tests/inline_resume.rs` with named cases covering every matrix row below. Inspect the complete command after engine/boundary assembly, using canonical fixture values without sorting/deduplicating argv. Assert exact refusals for unsupported shapes, ON positives beside OFF, served fallback prompt/notice agreement and separate actual-resume/cold-replacement outcomes. Record actual test names per row in evidence; a resolver plan alone closes none. Requirements: [Every accepted native control reaches the final command][NCC] (scenario: H1 through H3 cover every serving path); [Neither inline arguments nor fallback can override native denial][NC4]; [Prompt capability statements reflect the serving seat's pinned holdings][MP3]; [Denial and admission have removal proofs and bounded live claims][NC6].

| Launch dimension | Required observable coverage |
| --- | --- |
| Inline and agent-backed; work and gate | Empty/no-ask denied command or full missing-authority refusal; held ON and supported restriction controls where admissible. Preserve current gate eligibility, not slice-two policy. |
| Ordinary, panel member, sequence step | Primary and executable fallback retain their own origin/control outcome through dispatch; include the Codex-to-DSH sequence fallback. |
| Selected, inherited and nested/wrapped bodies | Same authored-source refusal and final serving controls, with stable site/office attribution and no relocation loss. |
| Boxed and unboxed | Native OFF independently of network; engine hands/strict config retained; authored MCP refused even without a box. |
| Cold, eligible resume, cold replacement | Complete cold commands, actual eligible resumed commands with session and `rejoining`, separately exact refusal plus denied fallback for ineligible shapes. |
| Control representation | Codex measured OFF/default ON; Claude argv, selection, mixed lists and supported restriction encoding; unsupported provider/forms refused at compile. |

## 8. Make reporting agree with admission and delivery

- [x] 8.1 Render capabilities from the serving outcome in `render_prompt`: explicit empty or named holdings, unmet wants, subtractions, known native denials and unmeasured reasons. Put the DATA-only instruction beside capability use. Verify primary/fallback prompts, CQ1 restriction-drop explanation, and instruction-shaped test data cannot change the pinned authority or launch plan; no neighboring office/candidate grant is advertised. Requirements: [Prompt capability statements reflect the serving seat's pinned holdings][MP3]; [Capability responses are data and confer no authority][MP4].
- [x] 8.2 Update `brokkr-cli/src/init.rs::adapter_json` to emit the same Claude native assessment as the shipped adapter. Verify `init_stacks`, `init_doctor` and library-data coverage compile generated no-grant scaffolds, keep native tools denied and report evidence limits; the generated path must not bypass the capability outcome. Requirements: [Native capability controls are adapter-owned evidence-bearing data][NC1]; [Realms v6 adds grants without changing frozen versions][RG1].
- [ ] 8.3 **M1:** Use shared denial assessment when doctor describes valid but inexpressible restrictions. Retain per-realm grant/dialect/tools/restrictions/scope and independent invalid-authority reporting; a dropped want cannot promise OFF when unsupported or unmeasured. Complete line tests cover declared grant versus actual seat outcome and exact requires/drop/inactive consequences, including MCP slice-two diagnosis without server execution. Requirements: [Doctor reports grants for every realm][CD1]; [Doctor explains unbuilt bindings without running capabilities][CD4]; [Restrictions are validated, carried and pinned without engine interpretation][RG4].
- [ ] 8.4 **M1:** Judge native OFF before doctor's matching-grant branch. For partial scope, empty offices, empty tools, unused/no-ask, subtracted and absent grants, assert complete supported/composable, unsupported and unmeasured report lines. Supported OFF reports declared denial within evidence limits; unsupported reports measured reason and compile refusal; unmeasured known OFF retains reason, no denial promise and H1's refusal. Preserve unknown inventory, invalid authority, same-name different-provider and installed/absent distinctions; DSH/LaneTally and Claude/Codex live limits remain explicit. Independently restore the grant-first bypass, observe exact scoped/empty/unused assertions fail, restore and pass. Requirements: [Installed native capabilities absent from grants are explicit][CD2]; [Unknown inventories and live-control gaps remain unmeasured][CD3]; [Known native powers require a valid delivered denial or refusal][NCR].

## 9. Record independent removal proofs on the repaired behavior

- [ ] 9.1 **M4:** Run the two separately runnable wants-only tests created in 4.4. Remove provider compatibility itself, then restriction compatibility itself in a separate experiment; leave notice recording/OFF composition intact and use otherwise-valid fixtures. Each optional test must reach and fail its whole expected notice-vector equality before any required-refusal or other substantive assertion. Restore each check and record the pass. Required and unused tests stay independent. Also rerun existing grant/scope/impossible-OFF/notice/MCP removals and grant/scope-deleted positive controls, with exact reasons/notices including optional/unused MCP. Record each mutation, test, intended assertion, actual failure and restored pass; historical M3/M4 required failures, fallback holdings and M6/M8 notice/OFF removals are not these optional proofs. Requirements: [Provider compatibility cannot expand a holding][SC4]; [Restrictions are validated, carried and pinned without engine interpretation][RG4]; [Refusal proofs assert the full reason][SC8]; [Compilation holds only the request and applicable grant intersection][SC3]; [An impossible native denial is a compile refusal][SC5]; [Any MCP realm grant refuses until slice two][SC6].
- [ ] 9.2 **H1–H3:** Independently remove known-provider denial admission, malformed-plan rejection, authored-server/provenance enforcement, cold OFF, actual resume OFF, cold-replacement control preservation, and each consumed Claude argv/selection/restriction contribution. Each corresponding full refusal or complete final-command assertion must detect its own missing enforcement. Separately replace authorized ON with unconditional OFF and retain CQ1 restricted-want notice/OFF removals. Restore and rerun after each mutation, retaining actual resume eligibility and positive hands/route controls. Record the exact matrix case and observation, never a plan-only or live-provider claim. Requirements: [Known native powers require a valid delivered denial or refusal][NCR]; [Authored provider configuration cannot supply capability authority][RGR]; [Every accepted native control reaches the final command][NCC]; [Denial and admission have removal proofs and bounded live claims][NC6].
- [ ] 9.3 **H4/M3:** Independently remove role and policy exclusion fences, permitted active-input identity protection and the consulted-unseated-definition contribution; observe full refusal or single-axis digest/integrity assertions fail. Preserve and rerun separate grant/definition/dialect/restriction identity removals and identical-input controls. Restore each contribution, rerun identity/start/resume green and confirm no mutation remains before measuring witness pins. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [Capability authorization participates in bundle identity][MP2]; [Capability classes belong to the abstraction][TD2].
- [ ] 9.4 Close the reproduction index with observed red/fix/removal/intended-failure/restored-pass evidence for all eight findings and the canonical-root correction. Link each observation to its actual named test and revision, and every launch-matrix row to full-command/refusal proof. Independently keep the three earlier removal-found gaps discriminating: adapter duplicate parsing; sequence fallback's own provider plan including Codex-to-DSH; unmapped operated-root resume with the decoy removed. Rerun their removals/restorations, audit mutation cleanup and retain historical measurements as historical. M4 closure requires both optional notice failures from 9.1. Requirements: [Refusal proofs assert the full reason][SC8]; [Denial and admission have removal proofs and bounded live claims][NC6]; [Active instructions and policy cannot escape bundle identity][MPI]; [Provider compatibility cannot expand a holding][SC4].

## 10. Document only the repair and measure actual pins

- [ ] 10.1 Update affected realm/agent/provider guidance for repaired legacy/missing denial metadata, removal of authored server configuration, relocation of excluded active inputs, strict request JSON and invalid unseated-agent requests. Retain abstract requests, DATA-only use and no-grant behavior. Describe the controller's unmeasured Codex resumed OFF/ON, explicit ON/other versions, inventory/profile precedence/ambient MCP exclusion, Claude live controls, DSH and LaneTally inventories/controls and generic exec uncertainty. Verify claims against observed evidence; argv success neither proves live enforcement nor clears the security hold. No new broker/gate/comparison claims. Requirements: [Denial and admission have removal proofs and bounded live claims][NC6]; [Slice-one records do not claim later-slice behavior][MP6]; [Capability responses are data and confer no authority][MP4].
- [ ] 10.2 After restored final code/data/charter bytes settle, actually compile every affected bundle witnessed by `runtime/tests/witness_digests.rs` and `bundle/compose_tests.rs`, including inherited bundles, `bundles/self` and `bundles/verify`; measure changed library charter bytes. Re-pin only observed digests, appending concrete 0065 repair reasons for each movement, including H4 relocations/identity corrections or newly consulted definitions when applicable. Record unchanged observations too; preserve historical reasons and never invent successful pins for refused inputs. Run witness/compose/library tests against these measurements. Requirement: [Digest pins are measured and their history remains truthful][MP5].
- [ ] 10.3 Review the final scoped diff against the adopted branch and original slice base for protected/frozen paths and later-slice creep. Confirm the repository remains v3 with no grants, the researcher loses both wants with applicable native OFF, and generated/inline/inherited bundles retain explicit authority identity. Verify engine hands, reserved-hands refusal and MCP compile fence remain intact. Correct only in-scope deviations and remeasure affected pins if inputs move. Requirements: [Realms v6 adds grants without changing frozen versions][RG1]; [Legacy concrete permissions cannot grandfather a capability][SC7]; [Reserved hands preserves the existing workspace authority][TD6]; [Slice-one records do not claim later-slice behavior][MP6].

## 11. Validate and commit the repair candidate

Commit restored completed work in repository message style; never push. Run
these gates against that candidate and record the source revision for each.
A correction creates a new candidate and requires affected checks again;
external coverage/CI results must name the final source head. Evidence notes
describe observations and never instruct, waive or replace a gate.

- [ ] 11.1 Run `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`; resolve failures and record actual exits. Keep dependency versions and required compiler/license constraints unchanged by the repair. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).
- [ ] 11.2 Run each crate suite with `cargo test -p <crate> --all-features --locked` for `brokkr-core`, `brokkr-store`, `brokkr-protocol`, `brokkr-runtime`, `brokkr-view`, `brokkr-bridge` and `brokkr-cli`, then `cargo test --workspace` and `cargo test --workspace --all-features --locked`. Record each actual result and obtain relevant Linux and macOS fixture/suite results on the repair candidate; Linux is not macOS evidence. Missing commands/skipped boundary execution remain pending. Attribute issue-255's known flake if observed without repairing it here or counting a failed suite green. Requirements: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation); [Refusal proofs assert the full reason][SC8] (scenario: macOS canonical fixture roots preserve exact diagnostics).
- [ ] 11.3 Run `cargo run --locked -p brokkr-cli -- compile --bundle bundles/self` and the equivalent `bundles/verify` compile; confirm final measured pins. Run `openspec validate --all --strict --no-interactive` and `git diff --check`, recording results for the final candidate. Keep repaired artifacts coherent and council/live measurement status explicit. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).
- [ ] 11.4 Run the authorized `bash scripts/coverage-exact.sh` on the final head with the unchanged pinned compiler and exact gate. Preserve its report/revision and report **source lines, branches and logical functions** separately as covered/total counts, each nonzero and exactly equal (literal 100%), including every added production line. If workspace restrictions prevent execution, obtain host/CI evidence outside the nested box; leave this task pending until that final-head result exists. Missing counts are unavailable, not zero/zero or rounded 100%. Do not exclude lines or lower thresholds. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).
- [ ] 11.5 Commit final task ticks, repair artifacts and observed evidence in repository message style. Check clean scoped status, adopted history retained, no leftover mutation and all required repair proofs/gates green before claiming implementation completion; required unavailable results leave their tasks open. Revalidate strict OpenSpec and diff cleanliness on the committed head; bind final-head external results to that actual revision, recording pending results honestly. Report the commit and three actual coverage counts, with task 12.1 explicitly open and security hold awaiting council. Requirements: [Digest pins are measured and their history remains truthful][MP5]; [Refusal proofs assert the full reason][SC8]; house commit/no-push rule.

## 12. Hold the final fold for council re-judgment

- [ ] 12.1 **Do not archive or fold in this commission.** The operator holds this final task open for council re-judgment; completed local repairs/gates do not clear the security hold. If subsequently authorized after that judgment, the dialect's final operation is `openspec archive decision-0065-capabilities-slice-one --yes`, folding the six deltas into living truth with append-only provenance, followed by archived strict validation and a commit. That later work is outside this repair visit, so leave this box unchecked. Requirements: [Slice-one records do not claim later-slice behavior][MP6]; [Digest pins are measured and their history remains truthful][MP5]; operator no-archive ruling and dialect archive operation.

[TD1]: specs/tool-dialect-contract/spec.md#requirement-a-tool-dialect-binds-one-capability-to-exactly-one-implementation-kind
[TD2]: specs/tool-dialect-contract/spec.md#requirement-capability-classes-belong-to-the-abstraction
[TD3]: specs/tool-dialect-contract/spec.md#requirement-dialects-describe-their-disclosure-using-the-existing-egress-vocabulary
[TD4]: specs/tool-dialect-contract/spec.md#requirement-the-mcp-contract-is-whole-before-its-runtime-exists
[TD5]: specs/tool-dialect-contract/spec.md#requirement-restriction-keys-are-defined-by-the-selected-dialect
[TD6]: specs/tool-dialect-contract/spec.md#requirement-reserved-hands-preserves-the-existing-workspace-authority
[RG1]: specs/realm-capability-grants/spec.md#requirement-realms-v6-adds-grants-without-changing-frozen-versions
[RG2]: specs/realm-capability-grants/spec.md#requirement-only-an-operators-realm-selects-concrete-implementations
[RG3]: specs/realm-capability-grants/spec.md#requirement-office-scopes-and-tool-subsets-only-narrow-a-grant
[RG4]: specs/realm-capability-grants/spec.md#requirement-restrictions-are-validated-carried-and-pinned-without-engine-interpretation
[RG5]: specs/realm-capability-grants/spec.md#requirement-realm-context-is-resolved-before-capability-authorization
[SC1]: specs/seat-capability-resolution/spec.md#requirement-requests-use-abstract-names-and-a-closed-strength-vocabulary
[SC2]: specs/seat-capability-resolution/spec.md#requirement-a-seat-can-subtract-but-cannot-widen-its-office
[SC3]: specs/seat-capability-resolution/spec.md#requirement-compilation-holds-only-the-request-and-applicable-grant-intersection
[SC4]: specs/seat-capability-resolution/spec.md#requirement-provider-compatibility-cannot-expand-a-holding
[SC5]: specs/seat-capability-resolution/spec.md#requirement-an-impossible-native-denial-is-a-compile-refusal
[SC6]: specs/seat-capability-resolution/spec.md#requirement-any-mcp-realm-grant-refuses-until-slice-two
[SC7]: specs/seat-capability-resolution/spec.md#requirement-legacy-concrete-permissions-cannot-grandfather-a-capability
[SC8]: specs/seat-capability-resolution/spec.md#requirement-refusal-proofs-assert-the-full-reason
[NC1]: specs/native-capability-controls/spec.md#requirement-native-capability-controls-are-adapter-owned-evidence-bearing-data
[NC2]: specs/native-capability-controls/spec.md#requirement-codex-web-search-off-uses-the-controllers-measured-fragment
[NC3]: specs/native-capability-controls/spec.md#requirement-eligible-codex-resumes-reimpose-the-capability-control
[NC4]: specs/native-capability-controls/spec.md#requirement-neither-inline-arguments-nor-fallback-can-override-native-denial
[NC5]: specs/native-capability-controls/spec.md#requirement-other-provider-declarations-preserve-the-commissioned-uncertainty
[NC6]: specs/native-capability-controls/spec.md#requirement-denial-and-admission-have-removal-proofs-and-bounded-live-claims
[CD1]: specs/capability-doctor/spec.md#requirement-doctor-reports-grants-for-every-realm
[CD2]: specs/capability-doctor/spec.md#requirement-installed-native-capabilities-absent-from-grants-are-explicit
[CD3]: specs/capability-doctor/spec.md#requirement-unknown-inventories-and-live-control-gaps-remain-unmeasured
[CD4]: specs/capability-doctor/spec.md#requirement-doctor-explains-unbuilt-bindings-without-running-capabilities
[MP1]: specs/capability-manifest-and-prompts/spec.md#requirement-a-new-manifest-version-records-capabilities-per-executable-seat
[MP2]: specs/capability-manifest-and-prompts/spec.md#requirement-capability-authorization-participates-in-bundle-identity
[MP3]: specs/capability-manifest-and-prompts/spec.md#requirement-prompt-capability-statements-reflect-the-serving-seats-pinned-holdings
[MP4]: specs/capability-manifest-and-prompts/spec.md#requirement-capability-responses-are-data-and-confer-no-authority
[MP5]: specs/capability-manifest-and-prompts/spec.md#requirement-digest-pins-are-measured-and-their-history-remains-truthful
[MP6]: specs/capability-manifest-and-prompts/spec.md#requirement-slice-one-records-do-not-claim-later-slice-behavior

[NCR]: specs/native-capability-controls/spec.md#requirement-known-native-powers-require-a-valid-delivered-denial-or-refusal
[NCC]: specs/native-capability-controls/spec.md#requirement-every-accepted-native-control-reaches-the-final-command
[RGR]: specs/realm-capability-grants/spec.md#requirement-authored-provider-configuration-cannot-supply-capability-authority
[MPI]: specs/capability-manifest-and-prompts/spec.md#requirement-active-instructions-and-policy-cannot-escape-bundle-identity
