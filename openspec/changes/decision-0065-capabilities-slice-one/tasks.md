# Decision 0065, slice one — security-hold repair tasks

Adopt every commit on `slice-0065-capabilities`, including `f97b7e77`,
`3b31c5de`, the second-hold specification `49fda5e6` and design
`90bf9d44`. The [proposal](proposal.md), six [deltas](specs/), [design Decisions](design.md#decisions)
and amended proposed [0066](../../../docs/decisions/0066-a-denial-is-something-the-launch-proves.md)
govern this ledger. The finding sources are the complete
[second](../../../.forge/tasks/council-ruling-25d222e6.md) and
[first](../../../.forge/tasks/council-ruling-3c72a18a.md) chief rulings;
the journal has no separate `returned_from`.

Second H1–H6, M1–M3, V1 and L1 are this return's scope. H5/H6 remain HIGH
specification defects (`spec_defect=true`); `has_security_residual=true`.
This tasks return orders the design obligations for implementation; it closes
no behavioral finding. An unchecked task is owed, not newly implemented. Checked foundational/first-repair tasks retain only
their historical scope; unqualified old H/M labels inside those checked tasks
refer to the first council. Their observations are preserved in [evidence](evidence.md).

The second specifications and design remove the bounded-scanner/verbatim,
lexical-folding and library-recompile-only exceptions. The disputed completion
rows 6.3, 7.4/7.5, 9.2 and 11.4 retain their numbers and remain open. Group 5 now implements resolution before group 6
binds and consumes its result; 6.1 retains the active-input regression proof.
Checked rows remain historical; their old cross-references describe that work,
not prerequisites for this return.
No earlier artifact currently prevents these obligations; evidence disproving
an owning requirement belongs upstream, never in a downstream exception.

Execute unchecked rows in numeric order; checked rows are adopted prerequisites,
not work to repeat. Task 0.2 establishes each behavioral red before shared
repairs; 0.3 establishes the supported M3 transport before its grammar is built.
Groups 3–4 parse, lower and admit; group 5 resolves/pins active inputs; group 6
carries and verifies the selected charter and supplies the actual prompt buffer;
group 7 proves final launch; group 8 proves reporting; groups 9–11 remove,
restore, document, validate and commit. A task closes on its own stated output,
not on a future row. Later integration/removal rows independently close their
additional obligations. Within each finding:
baseline red at its intended assertion, fix, isolated enforcement removal,
intended failure, restore, green. M3 owes the missing positive final-launch
proof and independent removals; V1 owes fresh exact equality; L1 is an evidence
audit. No fabricated runtime mutation is required for V1/L1. A shared helper
passing is not final-command proof. Implementation completion remains open
while any required proof or gate is pending. Task 12.1 stays open separately.

Before adding any fixture, retain its TempDir guard and canonicalize its root
once; derive both writes and expected paths from that root on Linux and macOS.
Tests use whole diagnostic/notice/report-line equality, complete ordered final
argv (including duplicate detection), or specified identity comparisons.
`is_err()`, substring/status-only checks, build errors and unrelated setup
refusals are not commissioned proof. Use raw JSON for duplicate cases and
otherwise valid metadata/denial fixtures so later hardening cannot mask them.
No permanent mutation framework; restore every temporary mutation.

Production stays Rust under `crates/`. Do not edit existing contract versions,
`policy/phase-machine.json`, `policy/schemas/`, `fixtures/`, `reference/`,
`extensions/`, the event envelope or the issue-226 task ledger. Test inputs
belong in temporary trees or owning suites. Keep the repository's v3 realm
without grants. No dependency upgrade, release work, push, issue-255/PR-313
flake repair or later-slice implementation. MCP grants still compile-refuse;
hands/boundaries retain 0043/0046 behavior. Only Linux and macOS are hosts.

## 0. Establish the decision and red reproductions

- [x] 0.1 Adopt proposed decision 0066 and amend rulings 3–6 plus enforcement bindings to this second-hold design: closed typed commands, restrictive/subtractive semantics, actual filesystem resolution, existing library pins at consumption and provider-aware doctor. Preserve first-council history, rulings 1/2/7/8/9, accepted 0065 and status `proposed`. This design amendment is complete; no code or finding closure is implied. Requirements: [Known provider commands have a closed argument grammar][RGP]; [Every accepted native control reaches the final command][NCC]; [Active instructions and policy cannot escape bundle identity][MPI]; [Library charter pins are enforced at consumption][MPL]; [Installed native capabilities absent from grants are explicit][CD2]; house proposed-decision rule.
- [ ] 0.2 **Second H1–H6/M1–M2:** Before enforcement changes, reproduce every named second-chief case as an independently accountable failing test at its exact refusal, whole argv, actual-file identity/consumption or complete doctor line. Record source revision and intended/observed assertion with otherwise-valid positive controls. Second M3 adds the supported compiled final-launch positive then independent removal; V1 retains the chief's failed baseline; L1 receives the documentary audit in 9.4. First-repair reds stay historical. Requirements: [Refusal proofs assert the full reason][SC8]; [Denial and admission have removal proofs and bounded live claims][NC6]; [Active instructions and policy cannot escape bundle identity][MPI]; [Library charter pins are enforced at consumption][MPL].
- [ ] 0.3 **Second M3 prerequisite:** Establish a supported production Claude restriction transport that can carry the whole canonical JSON object while retaining the held native capability. Record its concrete option, provider/version evidence, restriction semantics, supported cold/resume forms and intended fixture in evidence before implementing its grammar. The grant may be synthetic; the transport may not be fictitious `--search-policy`/`--search-restrict`, inert prompt prose or a test-only grammar exception. If no supported form can meet the positive scenario, return the owning specification/design defect upstream with the evidence before dependent implementation; an unsupported-form refusal cannot close M3. Requirements: [Restrictions are validated, carried and pinned without engine interpretation][RG4]; [Every accepted native control reaches the final command][NCC]; [Denial and admission have removal proofs and bounded live claims][NC6].

This second-hold index records obligations, not test results. H1–H6 and M1–M2
start at 0.2; M3 starts at 0.3; every closure is audited in 9.4 and 11.5.

| Finding | Observable / owning suite | Repair and consuming proof | Removal / closure |
| --- | --- | --- | --- |
| H1 | Attached/split/equal Codex config, full realm-only refusal and engine-hands positive; protocol/runtime launch | 3.9–3.13, 4.6–4.7, 7.3, 7.5 | 9.2 |
| H2 | Claude/LaneTally plugins, later MCP/wildcard list values, repeats and aliases | 3.9–3.13, 4.6–4.7, 7.3, 7.5 | 9.2 |
| H3 | Exact ambiguous-prompt refusal; inert/joined prompt values alongside effective OFF | 3.11, 3.13, 4.3, 4.7, 7.4–7.5 | 9.2 |
| H4 | Split/equal Read and explicit-empty OFF, full cold/eligible-resume argv with WebFetch denial | 4.3, 7.4–7.5 | 9.2 |
| H5 | Four standalone/inherited charter/policy escapes and contained-target/decoy controls | 5.1–5.2, 6.1–6.5 | 9.3 |
| H6 | Compile once, mutate library charter, dispatch without recompiling; no provider/changed prompt | 6.2–6.5, 7.5, 8.1 | 9.3 |
| M1 | Compatible Claude/LaneTally MCP subtraction plus native denial; exact hands conflict | 4.3, 4.6, 7.4–7.5 | 9.2 |
| M2 | Whole doctor lines and full matching compile causes for all grant shapes | 8.3–8.4 | 8.4, 9.4 |
| M3 | Real supported nonempty restriction compiled through final cold/actual-resume launch | 0.3, 4.4, 7.5–7.6 | 9.2 |
| V1 | Fresh final-head exact equality for all three whole-workspace measures | 11.1–11.4 | 11.5 |
| L1 | Reject workflow directions/prose false residual as authority; preserve checked evidence | 9.4, 10.3 | 11.5 |

The first-repair source parsing, floor, whole-library lint/consulted pins,
separate optional notices and canonical-root observations remain adopted in
their checked rows and evidence; they do not close neighboring second findings.

## 1. Retain the adopted additive contracts

- [x] 1.1 Add `contracts/tool-dialect.v1.schema.json` with D3's closed discriminated shape, identity/serves/tools, provider-native binding, reserved workspace hands, abstract-class equality annotation and separate disclosure metadata. Verify positive native/hands examples and schema rejection of unknown/mixed kinds, missing binding fields, invalid classes and egress vocabulary. Requirements: [A tool dialect binds one capability to exactly one implementation kind][TD1]; [Dialects describe their disclosure using the existing egress vocabulary][TD3]; [Reserved hands preserves the existing workspace authority][TD6].
- [x] 1.2 Complete that new tool-dialect contract's MCP branch with exactly one stdio argv or URL connection, named tools, pinned version, 0012 binding names and optional retention declaration; define the embedded Draft 7 restriction schema field. Verify both connection forms as data and schema rejection of mixed/incomplete forms and literal credential fields, without server/network access. Task 2.4 completes semantic binding/reference checks. Requirements: [The MCP contract is whole before its runtime exists][TD4]; [Restriction keys are defined by the selected dialect][TD5].
- [x] 1.3 Add `contracts/realms.v6.schema.json`, preserving v5 fields and admitting the per-realm grants map with optional tool/office lists and dialect-owned restriction values. Verify valid omission/empty maps and lists, invalid nulls/types/duplicates, and schema refusal of `capabilities` under every legacy version. Requirement: [Realms v6 adds grants without changing frozen versions][RG1].
- [x] 1.4 Add `contracts/run-manifest.v11.schema.json` with D7's required, structured capability section, explicit empty outcomes, per-candidate records, relative sources/digests and inactive grant context. Register all three new contracts additively in `crates/brokkr-runtime/tests/frozen_contracts.rs`; verify hand-built positive/negative v11 examples and unchanged hashes for every existing version. Requirement: [A new manifest version records capabilities per executable seat][MP1].

## 2. Retain operator authority loading and explicit context

- [x] 2.1 Extend `brokkr-core/src/realms.rs` and runtime realm representation for v6 grants, preserving omitted versus explicit tool/scope lists. Check field presence before defaults so v1–v5 reject even `capabilities: {}`. Verify core realm tests for all six versions, absent map/field, empty maps, malformed data and unchanged hands/boundary fields. Requirements: [Realms v6 adds grants without changing frozen versions][RG1]; [Office scopes and tool subsets only narrow a grant][RG3].
- [x] 2.2 Introduce D2's explicit `CapabilityContext` and thread it through CLI compile/run/rerun, runtime compile entry points and semantic library lint. Resolve definition/dialect roots beside the active realm map, otherwise under the operated repository; convenience APIs must construct an explicit no-grant context. Verify mapped/unmapped and neighboring-realm context tests, and that recipe roots and agent-library overrides cannot replace operator authority. Task 4 connects authorization to every entry point. Requirements: [Realm context is resolved before capability authorization][RG5]; [Only an operator's realm selects concrete implementations][RG2].
- [x] 2.3 Implement the strict abstract-definition loader in a focused runtime `capabilities` module: safe names, contained paths including symlinks, filename/name equality, exactly name/classes, nonempty unique classes and deterministic duplicate/conflict rejection. Read/hash the same bytes and validate every definition in the root. Verify CQ2's valid operator-defined name, missing roots, absent/conflicting metadata, recipe-local impersonation and class-order equality with exact owning diagnostics. Requirement: [Capability classes belong to the abstraction][TD2].
- [x] 2.4 Implement strict selected-dialect loading and restriction validation, including containment, duplicate-key rejection in new authority data, serves/class agreement and same-buffer SHA-256. Promote the already locked `jsonschema` workspace dependency from runtime dev-only to production with default features disabled, for complete operator-schema validation; permit only local embedded Draft 7 references, reject external retrieval and reserved-key redefinition through references/composition. Verify valid/invalid schemas, unchanged values/array order, missing/escaping references and schema/loader agreement. Check MCP undeclared secret references and URL userinfo without echoing credentials or opening a secret store. Confirm no package/version upgrade and record the dependency-edge justification. Requirements: [A tool dialect binds one capability to exactly one implementation kind][TD1]; [Restriction keys are defined by the selected dialect][TD5]; [Only an operator's realm selects concrete implementations][RG2]; [The MCP contract is whole before its runtime exists][TD4].

## 3. Repair source readers and preserve composition inputs

- [x] 3.1 **M2:** Strictly parse each original recipe layer in `bundle/compose.rs::read_layers` and agent source in `agents/load.rs::read_json` with `parse_strict` before map conversion. Refuse duplicate capability names and outer `capabilities` fields, both strength orders, equal repetitions and a later `{}`. Cover direct, nested panel, sequence, selected-case and inherited/overridden layers with raw JSON; assert full source/key/location diagnostics and valid inheritance/subtraction controls. Keep errors at the original source. Independently remove each reader's strict parsing, observe its intended equality fail, restore and pass. Requirements: [Requests use abstract names and a closed strength vocabulary][SC1]; [Refusal proofs assert the full reason][SC8].
- [x] 3.2 Preserve omission versus explicit request maps: agent-backed omission inherits, an explicit unchanged-strength subset subtracts, `{}` removes all, and inline maps supply office asks. Verify addition/strength-change refusals and inherited required subtraction across ordinary, panel, sequence, selected and composed bodies; retain both stable office and execution-site identities. Requirement: [A seat can subtract but cannot widen its office][SC2].
- [x] 3.3 **H1/H3:** Retain strict adapter declaration validation and make `native_controls::managed` decode engine plans fallibly. Reject wrong types, malformed arrays/guards, missing selection mappings, missing known obligations and unmeasured required OFF instead of `filter_map`/default-empty recovery. Carry provider/disposition readiness through `Outcome::controls`; declared measured default ON stays distinct from missing control. Keep the by-hand interface separate from engine dispatch. Add exact malformed-plan refusals and valid argv/selection/default controls in protocol/native tests; do not change frozen wire schemas. Requirements: [Native capability controls are adapter-owned evidence-bearing data][NC1]; [Known native powers require a valid delivered denial or refusal][NCR]; [Every accepted native control reaches the final command][NCC].
- [x] 3.4 Ship operator `capabilities/web-search.json` and `web-fetch.json` as reads/egress definitions and only the three provider-native tool dialects in D9: Codex search, Claude search and Claude fetch. Add Codex's measured cold-default ON and exact OFF pair, Claude's declared search/fetch selection controls, and DSH/LaneTally's explicit commissioned unmeasured reasons; describe generic exec's unknown child inventory honestly. Shipped dialect restriction schemas admit only empty objects until a control exists. Verify library-data tests load each binding, tools/keys agree, evidence is scoped, and repository realms still grant nothing. Requirements: [Capability classes belong to the abstraction][TD2]; [Codex web-search OFF uses the controller's measured fragment][NC2]; [Other provider declarations preserve the commissioned uncertainty][NC5].
- [x] 3.5 Migrate `agents/researcher.json` to abstract web-search/web-fetch wants, retain local command restrictions and remove concrete web aliases; add the DATA-only sentence beside capability use in its charter and any other affected capability-using charter. Refuse nonempty legacy `tools.mcp`, even optional entries, and native-owned legacy allow aliases with full migration reasons; keep empty MCP lists valid. Verify strict library and charter tests, local restrictions unchanged and declarations contain no implementation choices. Requirements: [Legacy concrete permissions cannot grandfather a capability][SC7]; [Capability responses are data and confer no authority][MP4].
- [x] 3.6 **macOS:** Repair `agents/tests::Tree::new` to retain its `TempDir` guard and one canonical root; derive all writes and complete expected paths from that root. Keep `a_native_declaration_with_a_repeated_key_or_an_uncomposable_selection_is_refused`'s full diagnostic equality. Sweep tests added by the original slice and this repair for the same lexical-root habit, recording checked helpers. Use an alias-root reproduction on a supported host, restore lexical behavior to expose the intended mismatch, then restore and pass; actual Linux/macOS runs are separately recorded in 11.2. Never glue a temp prefix or canonicalize only the expectation. Requirement: [Refusal proofs assert the full reason][SC8] (scenario: macOS canonical fixture roots preserve exact diagnostics).
- [x] 3.7 **M3:** Immediately after `Bundle::assemble` actually loads a library, call existing `authority.definitions.lint(&library)` before seat resolution/subtraction; return deterministic `CompileError::Capability` agent diagnostics. Resolve every loaded agent's requires/wants, including unseated and later-subtracted asks, without loading otherwise unused libraries. Add those definition names to the manifest's consulted set. Prove the exact CQ2 agent-lint error with a valid seated worker plus invalid unseated researcher, direct/composed cases, valid-library control and retained CLI lint. Remove only the compile invocation, observe its named regression fail, restore and pass. Definition identity proof follows in 6.2. Requirements: [Capability classes belong to the abstraction][TD2]; [Requests use abstract names and a closed strength vocabulary][SC1]; [Capability authorization participates in bundle identity][MP2].
- [x] 3.8 **First H2 prerequisite (retained):** At `agents::compose`, record the appended hands-fragment length; `Candidate::parts()` uses that captured boundary to recover the base and managed vectors. This observed positional provenance is the adopted implementation of the original separate-vector design. Inline vectors remain wholly authored. Preserve origin through candidate construction; do not reconstruct it by matching/deleting strings or recognizing a server name. Test identical-looking authored and engine fragments retain distinct origin while existing hands/model commands remain unchanged. This supplies group 4 admission and group 5 dispatch; it does not itself prove H2 closed. Requirements: [Authored provider configuration cannot supply capability authority][RGR]; [Reserved hands preserves the existing workspace authority][TD6].

- [ ] 3.9 **Second H1/H2/H3 grammar inventory:** Inventory shipped provider/wrapper invocations, hands fragments, managed controls and 0.3's supported restriction transport. Record each admitted option's aliases/forms, type, arity/empty policy, variadic boundary, repetition, subcommand/positional placement and authority effect, plus its provider/version evidence. Map current raw consumers in `native_controls.rs`, `adapters.rs`, runtime capability admission and agent composition to their replacement. No runtime help scraping, opaque remainder or adapter-defined grammar escape. Requirements: [Known provider commands have a closed argument grammar][RGP]; [Prompt values cannot absorb a composed control][NCP]; [Every accepted native control reaches the final command][NCC].
- [ ] 3.10 **Second H1:** Implement the shared typed nodes, source spans/origins and Codex grammar beside protocol native controls. Normalize all five config spellings, including attached `-cVALUE`, to one parsed assignment; model quoted/table/descendant keys and judge every repeat before reduction. Unknown keys with unbounded effects, malformed forms and unplaced tokens get complete token-specific causes with payload redaction. Unit and round-trip tests cover harmless config, native keys and hands syntax; semantic realm admission follows in 4.6. Requirements: [Known provider commands have a closed argument grammar][RGP]; [Authored provider configuration cannot supply capability authority][RGR].
- [ ] 3.11 **Second H2/H3:** Add Claude and LaneTally wrapper/forwarded grammar using the same typed nodes. Parse all supported list aliases/forms/values/repeats, empty lists, parentheses-aware tool patterns, plugin/MCP/settings channels and prompt scalars. The exact split prompt reproduction refuses; admitted joined or inert flag-looking text remains a value. Parse each origin to completion so neither a dangling value nor a terminator can absorb a later fragment. Test exact malformed/unknown/ambiguous causes and valid inert/local-pattern controls; LaneTally inherits no Claude inventory. Requirements: [Known provider commands have a closed argument grammar][RGP]; [Prompt values cannot absorb a composed control][NCP]; [Explicit restrictive tool lists retain their meaning][NCT]; [Subtractive tool lists never grant a capability][RGS].
- [ ] 3.12 Add DSH's supported invocation grammar without an opaque forwarded remainder. Retain the one bound, contained, digest-matched route-only patch through existing `route_overlay::claim`/`validate`, including nested content and pre-staging revalidation. Preserve legitimate model/effort/transcript/sandbox forms and fully refuse unknown, additional, unbound, server/tool or changed patches. A renamed adapter cannot hide a recognized harness. Requirements: [Known provider commands have a closed argument grammar][RGP]; [Authored provider configuration cannot supply capability authority][RGR]; [Other provider declarations preserve the commissioned uncertainty][NC5].
- [ ] 3.13 Replace managed-path raw option readers with the shared parse for selectors, restriction duplicates/arity, model/effort/sandbox extraction and resume eligibility. Keep the by-hand interface distinct; each process boundary uses this same parser. Preserve current session/version/accounting/boundary eligibility and exact ineligibility causes. Check the 3.9 consumer inventory for remaining raw scanners and exercise option-looking values against each affected reader. Final managed lowering and serialization are separate group-4/group-7 proofs. Requirements: [Known provider commands have a closed argument grammar][RGP]; [Prompt values cannot absorb a composed control][NCP]; [Eligible Codex resumes reimpose the capability control][NC3]; [Every accepted native control reaches the final command][NCC].

## 4. Repair shared control composition and compiler admission

- [x] 4.1 Validate every selected-realm grant before seat compatibility: definition/dialect existence, serves/classes, tool subset, offices and restriction schema. Then refuse MCP grants with the complete slice-two diagnostic even for wants, no requests or `offices: []`, and refuse reserved hands with the 0043/0046 reason. Verify exact diagnostics and positive native controls without server launch; invalid data cannot become an optional drop. Requirements: [Any MCP realm grant refuses until slice two][SC6]; [Reserved hands preserves the existing workspace authority][TD6]; [Restrictions are validated, carried and pinned without engine interpretation][RG4].
- [x] 4.2 Implement the pure office-asks-minus-subtractions intersection with scoped realm grants. Resolve omitted versus empty scopes/tools exactly; produce named required refusals and deterministic complete wanted-drop notices, never enable an unused grant. Verify missing grant, wrong office, no tools, subtracted and no-ask cases from positive examples, including the exact D4 missing-grant reason. Requirements: [Compilation holds only the request and applicable grant intersection][SC3]; [Office scopes and tool subsets only narrow a grant][RG3].
- [ ] 4.3 **Second H3/H4/M1:** Make shared `compose_for_provider` and compile admission consume 3.10–3.12's typed nodes for authored, hands and managed fragments. Distinguish explicit include None/empty/nonempty from additive selection and the engine baseline. Retain all named Read/empty OFF positives, merge compatible denials once, preserve MCP subtraction, and refuse incompatible limits/required holdings precisely. Preserve optional whole-drop notices/OFF and candidate-specific holdings. Remove raw-token `apply_selection` interpretation and non-list-verbatim forwarding. Unsupported forms, mappings and duplicates refuse during compile. Requirements: [Every accepted native control reaches the final command][NCC]; [Explicit restrictive tool lists retain their meaning][NCT]; [Subtractive tool lists never grant a capability][RGS]; [Provider compatibility cannot expand a holding][SC4].
- [ ] 4.4 **Second M3 transport implementation:** Using 0.3's established production transport, lower its synthetic grant's full canonical JSON value through the grammar with supported ON. Prove whole structured-value preservation, encoded-value equality and restriction-only manifest movement in the resolver/compile suite. Unsupported forms retain complete required-refusal, wants-drop/OFF and unused-inactive causes. Preserve the first-repair independent CQ1/provider-compatibility tests and 9.1's historical removals. This row proves admission/identity only; 7.6 owns final cold/resume positives and 9.2 owns final-delivery removal. Requirements: [Restrictions are validated, carried and pinned without engine interpretation][RG4]; [Every accepted native control reaches the final command][NCC]; [Capability authorization participates in bundle identity][MP2].
- [x] 4.5 **H1:** Replace security-relevant best-effort adapter loading with a preserved `Result` shared by pin and capability resolution. Separate optional effort exemptions from mandatory denial; audit suppressed errors affecting authorization, including `load_pin_adapters`, `site_capabilities`, `native_plan` and retry consumers. Apply D4's known-power floor (Codex search; Claude search/fetch), plus declared powers, without embedded switches or borrowed DSH/LaneTally inventories. Refuse absent roots/providers, legacy/omitted/empty metadata, omitted known powers, malformed/unreadable or unrelated broken adapter files and unmeasured known OFF when no valid denial exists. Assert whole source/load causes with site/office/realm/provider/capability for no-ask/no-grant inline work, no map, v1–v5 and v6 empty/omitted grants, absent definitions, scope, subtraction and optional drops; positive valid metadata delivers OFF. Use 4.3 to admit only composable plans through every compile entry point. Requirements: [Known native powers require a valid delivered denial or refusal][NCR]; [An impossible native denial is a compile refusal][SC5]; [Realms v6 adds grants without changing frozen versions][RG1]; [Other provider declarations preserve the commissioned uncertainty][NC5].
- [ ] 4.6 **Second H1/H2/M1:** Judge typed authored effects, not spellings: Codex attached/split/equal config keys and every repeated occurrence; Claude/LaneTally plugins, MCP/settings loading and every variadic include/allow value. Refuse unauthorized server/tool/native capability effects with full source/site/provider/control/realm cause, redacting payloads. Deny lists are subtraction, never grants. Engine hands keep captured origin; opaque authority channels refuse. Preserve DSH's bound contained digest-matched route-only patch and pre-staging byte/content validation, with positive model/effort/transcript/boundary controls. Requirements: [Authored provider configuration cannot supply capability authority][RGR]; [Known provider commands have a closed argument grammar][RGP]; [Subtractive tool lists never grant a capability][RGS]; [Reserved hands preserves the existing workspace authority][TD6].
- [ ] 4.7 Integrate typed admission across compile entry points and process-boundary input. Parse unexpanded authored/hands/managed origins independently, expand only typed value slots, then reparse/revalidate the same grammar at launch. Preserve checked provenance reassembly and strict native-plan decoding after every input merge; fallback replaces the plan. Test missing/mismatched provenance, structural substitutions, malformed authority and counterfeit hands with complete refusals before provider work. Render/parse consistency supplements group 7's independent literals. Requirements: [Known provider commands have a closed argument grammar][RGP]; [Known native powers require a valid delivered denial or refusal][NCR]; [Authored provider configuration cannot supply capability authority][RGR]; [Capability responses are data and confer no authority][MP4].

## 5. Resolve and pin active inputs before dispatch integration

- [ ] 5.1 **Second H5 resolver:** Replace optional exclusion logic with a fallible resolver of the original reference using its canonical owner root. Canonicalize before containment; never fold a symlink/parent pair first. Require a regular readable in-layer target and an applicable existing file-map pin, with separate authored-path and canonical-target excluded-tree checks. Wire `parse_role` and every `compose::own_table` through it, including overridden ancestors. Missing/unresolvable/unreadable/non-regular/unpinned inputs get their full source/site/kind/reference cause. Preserve independent library/dialect ownership and the script fence. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [Library charter pins are enforced at consumption][MPL].
- [ ] 5.2 **Second H5 same-buffer policy and identity:** Read/hash the resolved policy buffer against its owning file map, refuse a changed read, and parse that same buffer. Preserve contained aliases and existing v11 projection; no second file inventory or manifest version. Verify repeated identical inputs, independent permitted charter/policy edits, ancestor/final digest movement, lexical decoy controls, all capability identity axes, unseated consulted definitions, unconsulted stability and dispatch-v2 refusal. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [Capability authorization participates in bundle identity][MP2]; [A new manifest version records capabilities per executable seat][MP1].

## 6. Carry the selected pin and consume verified charter text

- [ ] 6.1 **Second H5 regression closure:** Run the four baseline reproductions through 5.1–5.2: standalone/inherited `alias/../charter.md` and `alias/../policy.json` with the named external symlink target, valid external edits and lexical decoys. Assert each full canonical-outside-layer refusal; no successful unchanged digest counts. Prove contained-target pin/read agreement and independently changed permitted bytes, plus prior excluded-tree/relocation positives. This establishes the fixed outcomes; 9.3 removes each protection independently. Requirement: [Active instructions and policy cannot escape bundle identity][MPI].
- [ ] 6.2 **Second H6 binding and carriage:** Bind each compiled charter to its Layer or Library owner, original reference, canonical target and existing expected digest, using group 5's resolved layer input or the selected agent's `charter_digest`. Preserve the original library reference as well as its canonical path. Carry that binding with the selected outcome and captured argv origin through SiteFacts/Candidate/SiteSpawn, ordinary/panel/sequence/selected/inherited/nested bodies and generated helpers. Fallback replaces all three; no union or longest-prefix owner guess. Preserve manifest projection and test external/nested libraries, owner overlap, collisions, subtraction and Codex-to-DSH fallback. Requirements: [Library charter pins are enforced at consumption][MPL]; [A new manifest version records capabilities per executable seat][MP1]; [Neither inline arguments nor fallback can override native denial][NC4]; [Authored provider configuration cannot supply capability authority][RGR].
- [ ] 6.3 **Second H5/H6 consumed pin:** At common `spawn_site`, re-resolve the selected 6.2 binding and verify owner containment, canonical target and read bytes against its existing expected digest before provider work. Missing owner/map/digest, changed or retargeted source (even equal bytes), missing/unreadable file and invalid UTF-8 refuse precisely; library uses `charter_digest` even outside or nested beneath recipe roots. Return verified text from the same hashed buffer through the actual dispatch/input seam after authored/context merging. In this same integration, managed `render_prompt` consumes that buffer without reopening `role_path` or suppressing failures; missing/malformed/replaced text refuses and explicitly charterless exec stays distinct. Compile once, edit library charter, dispatch without recompilation for standalone/inherited/external/owner-overlap cases. A recording driver proves the full refusal and no provider or changed-prompt consumption; unchanged/restored cases succeed. Recompile-only evidence cannot close this row. Requirements: [Library charter pins are enforced at consumption][MPL]; [Active instructions and policy cannot escape bundle identity][MPI]; [Capability responses are data and confer no authority][MP4].
- [ ] 6.4 Retain start and pinned-context resume fences while integrating second H5/H6 consumed-file/owner checks. Prove changed, newly excluded, retargeted and missing applicable charters refuse at their actual boundary, with unchanged/restored controls; no recompile-only substitute for 6.3. Preserve map-edit behavior, definition/dialect/adapter mismatch, historical journals and unmapped operated-root resume with workspace decoy removed. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [Library charter pins are enforced at consumption][MPL]; [Realm context is resolved before capability authorization][RG5].
- [ ] 6.5 Integrate the verified buffer and selected capability paragraph across every actual dispatch caller before final-command proofs. Ordinary/panel/sequence, primary/fallback, selected/inherited/nested and generated paths must send the returned private text, never a throwaway verified copy or an authored/context override. Prove the selected binding/holding/notice/prompt agree, by-hand rendering cannot mint managed provenance, and dialect instructions retain their contained pinned consumption route. Requirements: [Library charter pins are enforced at consumption][MPL]; [Prompt capability statements reflect the serving seat's pinned holdings][MP3]; [Capability responses are data and confer no authority][MP4]; [Active instructions and policy cannot escape bundle identity][MPI].

## 7. Deliver controls at the final provider command

- [ ] 7.1 **Second-hold integration:** **First H1:** Integrate validated composer output into `codex_cold` after boundary/hands assembly. Assert complete final argv for denied and held inline/agent-backed, boxed/unboxed seats: OFF is exactly `-c`, `web_search="disabled"`; declared ON has no conflicting OFF. Cover no asks, dropped wants, scope, subtraction, unused grants and legacy realms. Preserve model/effort/sandbox/hands/result controls and verify missing/broken metadata cases refuse before launch. This is composition proof scoped to the declared controller measurement. Requirements: [Codex web-search OFF uses the controller's measured fragment][NC2]; [Known native powers require a valid delivered denial or refusal][NCR]; [Realms v6 adds grants without changing frozen versions][RG1].
- [ ] 7.2 **Second-hold integration:** **First H1/H3:** Use selected validated controls in actual eligible unboxed Codex `exec resume`, outside the authored passthrough allowlist and before session/stdin positionals. Carry those controls into pre-work cold replacement or propagate `codex_managed` decoding errors; eliminate its `.ok()`/empty fallback. Assert complete held/denied resume argv, offered session, stdin `-`, sandbox/effort, `rejoining` and no refusal; separately prove cold replacement and malformed-plan refusal. Retain arbitrary-config, version/session/accounting/boundary eligibility and exact boxed/gate ineligibility with denied cold fallback where applicable. No eligibility expansion or cold fallback counted as resume. Requirements: [Eligible Codex resumes reimpose the capability control][NC3]; [Known native powers require a valid delivered denial or refusal][NCR]; [Denial and admission have removal proofs and bounded live claims][NC6].
- [ ] 7.3 **Second H1/H2:** Through production compilation and final expanded command construction, assert full causes for attached/split/equal config, quoted/table/descendant/repeated keys, plugin loading and every variadic/repeated admission value. Cover known providers, aliases and unknown/malformed grammar in each launch route. Preserve literal engine-hands positives and DSH bound-route controls with changed-byte pre-staging refusal. No provider/server starts on refusal. Requirements: [Authored provider configuration cannot supply capability authority][RGR]; [Known provider commands have a closed argument grammar][RGP]; [Reserved hands preserves the existing workspace authority][TD6]; [Every accepted native control reaches the final command][NCC].
- [ ] 7.4 **Second H3/H4/M1:** Compile fixtures through production then compare complete independent cold and actual eligible-resume Claude/LaneTally commands. Prove the exact ambiguous prompt refusal, inert/joined prompt preservation, split/equal Read and explicit-empty restrictions with independent WebFetch denial, the WebSearch deny-list positive retaining both denials, MCP subtraction/aliases/later values and precise hands conflicts. Preserve strict hands/local limits, exact subsets, independent inventory facts and full unsupported-form causes; DSH cannot discard an accepted nonempty control. Assert offered session and actual resumed shape where eligible; a replacement is a separate outcome. The held nonempty restriction proof is independently runnable in 7.6. Requirements: [Every accepted native control reaches the final command][NCC]; [Prompt values cannot absorb a composed control][NCP]; [Explicit restrictive tool lists retain their meaning][NCT]; [Subtractive tool lists never grant a capability][RGS].
- [ ] 7.5 Map every applicable launch route below for 7.1–7.4 and the selected charter to actual named tests that compile their fixture and assert a full independent ordered final-command literal or full refusal. Cover the second findings across cold/eligible-resume/replacement, boxed/unboxed, gate/work, primary/every executable fallback, ordinary/panel/sequence, inline/agent-backed/selected/inherited/nested paths. Canonical fixture substitutions only; no generated/sorted/deduplicated expectations. Keep ON positives, selected charter/notice/prompt agreement and no-provider-work checks. One matrix dimension never implies an untested caller. Record the supported-restriction transport row as pending until 7.6 adds its two named tests; 9.4 audits the completed matrix. Requirements: [Every accepted native control reaches the final command][NCC]; [Denial and admission have removal proofs and bounded live claims][NC6]; [Library charter pins are enforced at consumption][MPL]; [Prompt capability statements reflect the serving seat's pinned holdings][MP3].
- [ ] 7.6 **Second M3 positive boundary:** Compile 4.4's held, supported nonempty restriction fixture through real realm/candidate admission and `claude_command`/`claude_launch`. Separately assert whole ordered cold and actual eligible-resume command literals containing supported ON and the exact encoded restriction, plus original structured manifest data. Assert the offered session and resumed shape, never cold replacement. Expected vectors are independent literals; no manually fabricated Controls, resolver-only or `Composed.extra` assertion closes this task. These separately runnable tests are the starting green controls for 9.2's two final-delivery removals. Requirements: [Every accepted native control reaches the final command][NCC] (scenario: H3 a held restriction survives final composition); [Restrictions are validated, carried and pinned without engine interpretation][RG4]; [Denial and admission have removal proofs and bounded live claims][NC6].

| Launch dimension | Required observable coverage |
| --- | --- |
| Inline and agent-backed; work and gate | Empty/no-ask denied command or full missing-authority refusal; held ON and supported restriction controls where admissible. Preserve current gate eligibility, not slice-two policy. |
| Ordinary, panel member, sequence step | Primary and executable fallback retain their own origin/control outcome through dispatch; include the Codex-to-DSH sequence fallback. |
| Selected, inherited and nested/wrapped bodies | Same authored-source refusal and final serving controls, with stable site/office attribution and no relocation loss. |
| Boxed and unboxed | Native OFF independently of network; engine hands/strict config retained; authored MCP refused even without a box. |
| Cold, eligible resume, cold replacement | Complete cold commands, actual eligible resumed commands with session and `rejoining`, separately exact refusal plus denied fallback for ineligible shapes. |
| Control representation | Codex measured OFF/default ON; Claude argv, selection, mixed lists and supported restriction encoding; unsupported provider/forms refused at compile. |

## 8. Make reporting agree with admission and delivery

- [ ] 8.1 Verify reporting against group 6's already integrated consumed text: the selected candidate's held/empty/not-held/subtracted/native-denial paragraph, DATA-only rule, notice and fallback agreement. Preserve generated-scaffold and original prompt coverage; assert that the managed text cannot be replaced or reread. This row audits rendered claims; 6.3–6.5 own consumption enforcement. Requirements: [Prompt capability statements reflect the serving seat's pinned holdings][MP3]; [Capability responses are data and confer no authority][MP4]; [Library charter pins are enforced at consumption][MPL].
- [x] 8.2 Update `brokkr-cli/src/init.rs::adapter_json` to emit the same Claude native assessment as the shipped adapter. Verify `init_stacks`, `init_doctor` and library-data coverage compile generated no-grant scaffolds, keep native tools denied and report evidence limits; the generated path must not bypass the capability outcome. Requirements: [Native capability controls are adapter-owned evidence-bearing data][NC1]; [Realms v6 adds grants without changing frozen versions][RG1].
- [ ] 8.3 **Second M2:** Replace provider-blind `NativeCapability::denial` delivery tagging with shared provider-aware lowering/grammar/composition assessment using the actual harness, selection mapping and complete unheld native plan. Use it for doctor's secondary restriction/drop paragraph as well as 8.4. Distinguish supported composability, measured unsupported, unmeasured and full provider/grammar/conflict refusal; no incomplete fake Controls, model invocation or doctor-only resolver. Describe adapter-only scope without universal promises about unassessed authored commands. Requirements: [Installed native capabilities absent from grants are explicit][CD2]; [Doctor reports grants for every realm][CD1]; [Restrictions are validated, carried and pinned without engine interpretation][RG4].
- [ ] 8.4 **Second M2:** Preserve OFF-first ordering and test separate complete report lines plus matching full compile causes for Codex selection OFF under absent, scoped, empty-office, empty-tool, unused/no-ask and subtracted grants. Valid Codex argv OFF is the positive; include malformed managed argv, incompatible restriction/mapping, unconsumed DSH, supported, unsupported and unmeasured cases. Independently replace shared assessment with tag-only Delivered; each intended scoped/empty/unused equality must fail and pass after restoration. Preserve installed/absent and invalid-authority distinctions and evidence limits. Requirements: [Installed native capabilities absent from grants are explicit][CD2]; [Unknown inventories and live-control gaps remain unmeasured][CD3]; [Known native powers require a valid delivered denial or refusal][NCR].
- [ ] 8.5 Retain the full MCP-unbuilt doctor report with its capability/dialect and independent realm/inventory lines after the shared-assessment refactor. Deterministic installed/absent fixtures must prove no capability server or model request is invoked; invalid authority stays distinct from empty grants. Requirements: [Doctor explains unbuilt bindings without running capabilities][CD4]; [Doctor reports grants for every realm][CD1]; [Any MCP realm grant refuses until slice two][SC6].

## 9. Record independent removal proofs on the repaired behavior

- [x] 9.1 **M4:** Run the two separately runnable wants-only tests created in 4.4. Remove provider compatibility itself, then restriction compatibility itself in a separate experiment; leave notice recording/OFF composition intact and use otherwise-valid fixtures. Each optional test must reach and fail its whole expected notice-vector equality before any required-refusal or other substantive assertion. Restore each check and record the pass. Required and unused tests stay independent. Also rerun existing grant/scope/impossible-OFF/notice/MCP removals and grant/scope-deleted positive controls, with exact reasons/notices including optional/unused MCP. Record each mutation, test, intended assertion, actual failure and restored pass; historical M3/M4 required failures, fallback holdings and M6/M8 notice/OFF removals are not these optional proofs. Requirements: [Provider compatibility cannot expand a holding][SC4]; [Restrictions are validated, carried and pinned without engine interpretation][RG4]; [Refusal proofs assert the full reason][SC8]; [Compilation holds only the request and applicable grant intersection][SC3]; [An impossible native denial is a compile refusal][SC5]; [Any MCP realm grant refuses until slice two][SC6].
- [ ] 9.2 **Second H1–H4/M1/M3:** Independently remove each typed config/plugin/list admission, positional/boundary check, explicit nonempty/empty restriction retention, subtraction preservation and final restriction delivery. Full refusal or whole final literal must fail at the intended protection with valid admission/ON/fixture otherwise intact. M3 starts from 7.6's two green compiled fixtures and removes cold and actual-resume delivery separately at the production final boundary; historical R-H3c intermediate failure is insufficient. Restore/pass each and retain floor/plan/provenance, cold/resume/replacement OFF and always-OFF positive-control removals. Requirements: [Refusal proofs assert the full reason][SC8]; [Denial and admission have removal proofs and bounded live claims][NC6]; [Authored provider configuration cannot supply capability authority][RGR]; [Every accepted native control reaches the final command][NCC].
- [ ] 9.3 **Second H5/H6:** Independently remove canonical role containment, policy containment, actual-file comparison and library dispatch pin enforcement while leaving compile pinning/layer checks intact where applicable. Observe the intended full refusal, actual-consumed-file identity or no-provider-work assertion fail, then restore/pass. Keep permitted active-input, consulted/unconsulted-definition, all grant/dialect/restriction identity and start/resume proofs discriminating. No mutation remains before actual pin measurement. Requirements: [Active instructions and policy cannot escape bundle identity][MPI]; [Library charter pins are enforced at consumption][MPL]; [Capability authorization participates in bundle identity][MP2].
- [ ] 9.4 Audit H1–H6/M1–M3 records after removal/restoration: each names its baseline revision/test, exact intended assertion, fix, isolated mutation, intended observed failure and restored pass; M3 names separate final cold/resume delivery removals. Link every applicable launch row to actual named assertions. For L1 record rejection of embedded workflow directions and prose false residual, preserving `has_security_residual=true` and H5/H6 `spec_defect=true`; notes choose no phase. Retain narrower first-repair observations and the three removal-found regressions without relabeling historical measurements. Mark V1/final-head closure pending here; 11.4–11.5 supply and audit those later results. Requirements: [Refusal proofs assert the full reason][SC8]; [Denial and admission have removal proofs and bounded live claims][NC6]; [Digest pins are measured and their history remains truthful][MP5]; [Capability responses are data and confer no authority][MP4].

## 10. Document only the repair and measure actual pins

- [ ] 10.1 Update only affected guides after observed repair behavior: closed supported grammar/unknown refusal, preserved restrictions/subtraction, canonical layer containment, owner-bound library dispatch and provider-aware doctor scope. Preserve no-grant/abstract-request migration and all live/ambient/version/inventory evidence limits. No unsupported grammar/transport claim, later-slice machinery, release or issue-226 work. Requirements: [Denial and admission have removal proofs and bounded live claims][NC6]; [Slice-one records do not claim later-slice behavior][MP6]; [Capability responses are data and confer no authority][MP4].
- [ ] 10.2 **Second-hold final candidate:** After restored final code/data/charter bytes settle, actually compile every affected bundle witnessed by `runtime/tests/witness_digests.rs` and `bundle/compose_tests.rs`, including inherited bundles, `bundles/self` and `bundles/verify`; measure changed library charter bytes. Re-pin only observed digests, appending concrete 0065 repair reasons for each movement, including first H4 and second H5/H6 relocations/identity corrections or newly consulted definitions when applicable. Record unchanged observations too; preserve historical reasons and never invent successful pins for refused inputs. Run witness/compose/library tests against these measurements. Requirement: [Digest pins are measured and their history remains truthful][MP5].
- [ ] 10.3 **Second-hold final candidate:** Review the final scoped diff against the adopted branch and original slice base for protected/frozen paths and later-slice creep. Confirm the repository remains v3 with no grants, the researcher loses both wants with applicable native OFF, and generated/inline/inherited bundles retain explicit authority identity. Verify engine hands, reserved-hands refusal and MCP compile fence remain intact. Correct only in-scope deviations and remeasure affected pins if inputs move. Requirements: [Realms v6 adds grants without changing frozen versions][RG1]; [Legacy concrete permissions cannot grandfather a capability][SC7]; [Reserved hands preserves the existing workspace authority][TD6]; [Slice-one records do not claim later-slice behavior][MP6].

## 11. Validate and commit the repair candidate

Commit restored completed work in repository message style; never push. Run
these gates against that candidate and record the source revision for each.
A correction creates a new candidate and requires affected checks again;
external coverage/CI results must name the final source head. Evidence notes
describe observations and never instruct, waive or replace a gate.

- [ ] 11.1 **Second-hold final candidate:** Run `cargo fmt --all -- --check` and `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`; resolve failures and record actual exits. Keep dependency versions and required compiler/license constraints unchanged by the repair. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).
- [ ] 11.2 Run each crate suite with `cargo test -p <crate> --all-features --locked` for `brokkr-core`, `brokkr-store`, `brokkr-protocol`, `brokkr-runtime`, `brokkr-view`, `brokkr-bridge` and `brokkr-cli`, then `cargo test --workspace` and `cargo test --workspace --all-features --locked`. Record each actual result and obtain relevant Linux and macOS fixture/suite results on the repair candidate; Linux is not macOS evidence. Missing commands/skipped boundary execution remain pending. Attribute issue-255's known flake if observed without repairing it here or counting a failed suite green. Requirements: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation); [Refusal proofs assert the full reason][SC8] (scenario: macOS canonical fixture roots preserve exact diagnostics).
- [ ] 11.3 **Second-hold final candidate:** Run `cargo run --locked -p brokkr-cli -- compile --bundle bundles/self` and the equivalent `bundles/verify` compile; confirm final measured pins. Run `openspec validate --all --strict --no-interactive` and `git diff --check`, recording results for the final candidate. Keep repaired artifacts coherent and council/live measurement status explicit. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).
- [ ] 11.4 **Second-hold final candidate:** Run the authorized `bash scripts/coverage-exact.sh` on the final head with the unchanged pinned compiler and exact gate. Preserve the chief's failed baseline separately (34897/35073 lines, 5730/5744 branches, 3443/3453 logical functions; exit 1). Inspect uncovered regions and fix/prove in-scope omissions without assuming a regression cause; do not widen this repair into unrelated work. Preserve the fresh report/revision and report **source lines, branches and logical functions** separately as covered/total counts, each nonzero and exactly equal (literal 100%), including every added production line. If workspace restrictions prevent execution, obtain host/CI evidence outside the nested box; leave this task pending until that final-head result exists. Missing counts are unavailable, not zero/zero or rounded 100%. Do not exclude lines or lower thresholds. Requirement: [Digest pins are measured and their history remains truthful][MP5] (scenario: Validation remains a proof obligation).
- [ ] 11.5 **Second-hold final candidate:** Audit the complete finding/test/removal matrix against 9.4 and the actual gate results, leaving unavailable obligations open. Commit restored work, task ticks and observed evidence in repository message style; never push. Require clean scoped status, adopted history, no leftover mutation and all required repair proofs/gates green before a repair-completion claim. Run strict all-item OpenSpec validation, diff cleanliness and the unchanged exact-coverage gate again on the resulting final committed HEAD; record that SHA, command exits and all three fresh counts in run-local evidence/result outside tracked inputs so recording the final check does not silently create a different source head. A failure or further tracked edit reopens the affected task and requires a new committed candidate and final-head checks. Report the actual commit and coverage counts with 12.1 open and the security hold awaiting council. Requirements: [Digest pins are measured and their history remains truthful][MP5]; [Refusal proofs assert the full reason][SC8]; house commit/no-push rule.

## 12. Hold the final fold for council re-judgment

- [ ] 12.1 **Do not archive or fold in this commission.** The operator holds this final task open for council re-judgment; completed local repairs/gates do not clear the security hold. If subsequently authorized after that judgment, the dialect's final operation is `openspec archive decision-0065-capabilities-slice-one --yes`, folding the six deltas into living truth with append-only provenance, followed by archived strict validation and a commit. That later work is outside this repair visit, so leave this box unchecked. Requirements: [Slice-one records do not claim later-slice behavior][MP6]; [Digest pins are measured and their history remains truthful][MP5]; operator no-archive ruling and dialect archive operation.

[TD1]: specs/tool-dialect-contract/spec.md#requirement-a-tool-dialect-binds-one-capability-to-exactly-one-implementation-kind
[TD2]: specs/tool-dialect-contract/spec.md#requirement-capability-classes-belong-to-the-abstraction
[TD3]: specs/tool-dialect-contract/spec.md#requirement-dialects-describe-their-disclosure-using-the-existing-egress-vocabulary
[TD4]: specs/tool-dialect-contract/spec.md#requirement-the-mcp-contract-is-whole-before-its-runtime-exists
[TD5]: specs/tool-dialect-contract/spec.md#requirement-restriction-keys-are-defined-by-the-selected-dialect
[TD6]: specs/tool-dialect-contract/spec.md#requirement-reserved-hands-preserves-the-existing-workspace-authority
[RG1]: specs/realm-capability-grants/spec.md#requirement-realms-v6-adds-grants-without-changing-frozen-versions
[RG2]: specs/realm-capability-grants/spec.md#requirement-only-an-operators-realm-selects-concrete-implementations
[RG3]: specs/realm-capability-grants/spec.md#requirement-office-scopes-and-tool-subsets-only-narrow-a-grant
[RG4]: specs/realm-capability-grants/spec.md#requirement-restrictions-are-validated-carried-and-pinned-without-engine-interpretation
[RG5]: specs/realm-capability-grants/spec.md#requirement-realm-context-is-resolved-before-capability-authorization
[SC1]: specs/seat-capability-resolution/spec.md#requirement-requests-use-abstract-names-and-a-closed-strength-vocabulary
[SC2]: specs/seat-capability-resolution/spec.md#requirement-a-seat-can-subtract-but-cannot-widen-its-office
[SC3]: specs/seat-capability-resolution/spec.md#requirement-compilation-holds-only-the-request-and-applicable-grant-intersection
[SC4]: specs/seat-capability-resolution/spec.md#requirement-provider-compatibility-cannot-expand-a-holding
[SC5]: specs/seat-capability-resolution/spec.md#requirement-an-impossible-native-denial-is-a-compile-refusal
[SC6]: specs/seat-capability-resolution/spec.md#requirement-any-mcp-realm-grant-refuses-until-slice-two
[SC7]: specs/seat-capability-resolution/spec.md#requirement-legacy-concrete-permissions-cannot-grandfather-a-capability
[SC8]: specs/seat-capability-resolution/spec.md#requirement-refusal-proofs-assert-the-full-reason
[NC1]: specs/native-capability-controls/spec.md#requirement-native-capability-controls-are-adapter-owned-evidence-bearing-data
[NC2]: specs/native-capability-controls/spec.md#requirement-codex-web-search-off-uses-the-controllers-measured-fragment
[NC3]: specs/native-capability-controls/spec.md#requirement-eligible-codex-resumes-reimpose-the-capability-control
[NC4]: specs/native-capability-controls/spec.md#requirement-neither-inline-arguments-nor-fallback-can-override-native-denial
[NC5]: specs/native-capability-controls/spec.md#requirement-other-provider-declarations-preserve-the-commissioned-uncertainty
[NC6]: specs/native-capability-controls/spec.md#requirement-denial-and-admission-have-removal-proofs-and-bounded-live-claims
[CD1]: specs/capability-doctor/spec.md#requirement-doctor-reports-grants-for-every-realm
[CD2]: specs/capability-doctor/spec.md#requirement-installed-native-capabilities-absent-from-grants-are-explicit
[CD3]: specs/capability-doctor/spec.md#requirement-unknown-inventories-and-live-control-gaps-remain-unmeasured
[CD4]: specs/capability-doctor/spec.md#requirement-doctor-explains-unbuilt-bindings-without-running-capabilities
[MP1]: specs/capability-manifest-and-prompts/spec.md#requirement-a-new-manifest-version-records-capabilities-per-executable-seat
[MP2]: specs/capability-manifest-and-prompts/spec.md#requirement-capability-authorization-participates-in-bundle-identity
[MP3]: specs/capability-manifest-and-prompts/spec.md#requirement-prompt-capability-statements-reflect-the-serving-seats-pinned-holdings
[MP4]: specs/capability-manifest-and-prompts/spec.md#requirement-capability-responses-are-data-and-confer-no-authority
[MP5]: specs/capability-manifest-and-prompts/spec.md#requirement-digest-pins-are-measured-and-their-history-remains-truthful
[MP6]: specs/capability-manifest-and-prompts/spec.md#requirement-slice-one-records-do-not-claim-later-slice-behavior

[NCR]: specs/native-capability-controls/spec.md#requirement-known-native-powers-require-a-valid-delivered-denial-or-refusal
[NCC]: specs/native-capability-controls/spec.md#requirement-every-accepted-native-control-reaches-the-final-command
[RGR]: specs/realm-capability-grants/spec.md#requirement-authored-provider-configuration-cannot-supply-capability-authority
[MPI]: specs/capability-manifest-and-prompts/spec.md#requirement-active-instructions-and-policy-cannot-escape-bundle-identity

[RGP]: specs/realm-capability-grants/spec.md#requirement-known-provider-commands-have-a-closed-argument-grammar
[RGS]: specs/realm-capability-grants/spec.md#requirement-subtractive-tool-lists-never-grant-a-capability
[NCP]: specs/native-capability-controls/spec.md#requirement-prompt-values-cannot-absorb-a-composed-control
[NCT]: specs/native-capability-controls/spec.md#requirement-explicit-restrictive-tool-lists-retain-their-meaning
[MPL]: specs/capability-manifest-and-prompts/spec.md#requirement-library-charter-pins-are-enforced-at-consumption
