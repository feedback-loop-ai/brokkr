# Every action is pinned by commit SHA with its release in a trailing
# comment (issue #340); Dependabot moves both together, so a bump is a
# reviewed pull request rather than a tag that moved underneath CI.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      actions:
        patterns: ["*"]
