{
  "provider": "dsh",
  "trust_tier": "untrusted",
  "binding_grant": false,
  "routes": {
    "spark": "local"
  },
  "credentials": {
    "spark": "SPARK_API_KEY",
    "meta": "OPENROUTER_API_KEY",
    "meta-contributor": "OPENROUTER_API_KEY"
  },
  "binary": "dsh",
  "driver": [
    "{brokkr}",
    "driver",
    "dsh",
    "--"
  ],
  "models": {
    "flash": "deepseek-v4-flash",
    "flash-experiment": "deepseek-v4.1-flash-expires-on-0910",
    "glm": "dashscope/glm-5.2",
    "muse": "meta/meta/muse-spark-1.3",
    "muse-contributor": "meta-contributor/meta/muse-spark-1.3-contributor",
    "pro": "deepseek-v4-pro",
    "qwen-flash": "dashscope/qwen3.8-flash",
    "qwen-max": "dashscope/qwen3.8-max",
    "qwen-plus": "dashscope/qwen3.7-plus",
    "qwen36-flash": "dashscope/qwen3.6-flash",
    "qwen37-max": "dashscope/qwen3.7-max",
    "spark-flash": "spark/qwen3.8-flash",
    "studio-flash": "dashscope/deepseek-v4-flash-0731",
    "studio-pro": "dashscope/deepseek-v4-pro"
  },
  "judges": [],
  "model_flag": "--model",
  "efforts": [
    "low",
    "medium",
    "high",
    "xhigh"
  ],
  "effort_flag": "--effort",
  "tool_permissions": "unsupported",
  "mcp": "unsupported",
  "hands": {
    "unsupported": "dsh 0.1.0-rc.6 replaces its tool surface only through a profile plugin (presentAs/restrict/guard/register on the scoped tools service); no CLI flag disables its shell and file tools or adds an MCP server, so the one boxed tool cannot be expressed without a plugin. Re-measured on 0.1.2-rc.1 (2026-09-04): the launcher and headless flags are unchanged; the tools plugin reads a DSH_TOOLS_MODE environment value whose vocabulary the release does not document, so it is not relied on. The plugin is the next slice."
  },
  "resume": {
    "headless-work": {
      "status": "unmeasured",
      "identity": {
        "version": "0.1.5-rc.1",
        "applies_to": "0.1.5-rc.1"
      },
      "classes": [
        "work"
      ],
      "boundaries": [
        "not applicable"
      ],
      "hands": "none",
      "evidence": {
        "interface": ".forge/tasks/dsh-pair-qualification-015rc1.json (2026-09-12): isolated live cold+warm qualification of @deepseek-ai/dsh 0.1.5-rc.1 at 183f08e9 plus the repository-owned six-file adaptation of dsh-plugin-cli-session 0.2.0 at 0f487e74 under extensions/dsh/plugin-cli-session/; see controller-dsh-upstream-discovery.json and dsh-pair-incompatibility.json"
      },
      "reason": "the forward-pinned route (@deepseek-ai/dsh 0.1.5-rc.1 plus the repository-owned six-file adaptation of dsh-plugin-cli-session 0.2.0 under extensions/dsh/plugin-cli-session/) is selected but disabled: the adapted pair continues one root cold-to-warm with a result envelope and per-message usage, but exact-root, restriction, multi-message accounting and composite-digest proof are unmeasured",
      "limitations": [
        "core 0.1.5-rc.1 broke the plugin's post-turn fold (agent.session.events removed); the selected pair therefore pins the plugin's supported core generation 0.1.0-rc.6 instead, not a global DSH limitation",
        "the plugin's emitted session_id is request-derived and is not independent root confirmation; the retained depth-zero header, post-resume init event, same-root nonce continuity and absence of a fresh sibling root must be observed",
        "the plugin's collectUsage() is last-wins over firstSeq and is not proven to attribute a multi-message/retry turn's aggregate; a numeric total that cannot be attributed must stay absent",
        "the installed 0.1.2-rc.1 headless runner's one-shot entry mints a fresh root and cannot forward the launcher's TUI example; that is bounded history for that entry only, never a global DSH or extension limitation",
        "the separate hands/tools plugin deferral above is a different matter and is not what blocks this shape",
        "2026-09-10: the operator's ruling reversed this shape's earlier 0.1.0-rc.6 pin; the forward-pinned latest core with the repository-owned adaptation at 0f487e74 is the selected route, and the superseded .forge/tasks/dsh-pair-qualification-010rc6.json record is dated history, not a current route"
      ]
    }
  }
}
